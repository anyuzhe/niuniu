# 统一技术交易因子实验平台
## 总体方案、系统架构与智能体开发说明

> 文档用途：本文件用于后续交给 Codex / 智能体 / 开发代理，作为系统总体设计说明、技术边界、模块职责和开发路线依据。  
> 目标不是开发“又一个普通量化回测软件”，而是建立一个能够把 **缠论、Al Brooks Price Action、ICT、SMC、Wyckoff、Order Flow、传统量价因子、Alpha101/Alpha158** 放到同一套实验框架内进行可计算、可复现、可比较、可消融、可样本外验证的技术交易研究平台。

---

# 1. 项目定位

## 1.1 核心目标

建立一套“统一技术交易因子实验平台”，完成以下事情：

1. 把不同技术交易理论拆成统一、可计算的底层因子。
2. 把因子按照市场状态、结构、趋势、动量、波动、流动性、成交量、订单流等分类管理。
3. 支持单因子回测。
4. 支持双因子、多因子组合回测。
5. 支持条件组合。
6. 支持评分组合。
7. 支持有时间顺序的事件序列和状态机。
8. 支持市场状态 Regime 条件下的因子 Alpha 验证。
9. 支持完整交易理论与底层因子之间的增量 Alpha / 消融实验。
10. 支持样本外、Walk Forward、Bootstrap、多重检验、防数据挖掘偏差等科学验证方法。
11. 支持最终将有效 Alpha 转换为策略、组合和真实交易。
12. 允许 vn.py、Qlib、LEAN 等作为外围基础设施或后端，但研究核心必须独立。

核心问题不是：

> “某个大师的方法准不准？”

而是：

> **这个交易理论中哪些底层现象具有真实、稳定、样本外可复现、交易成本后仍然存在的 Alpha？**

进一步回答：

> **复杂理论规则是否提供了超出简单底层因子的增量 Alpha？**

---

# 2. 核心研究思想

## 2.1 从“理论”降维到“统一市场语言”

多个交易理论经常使用不同名称描述类似现象。

| 统一市场现象 | Brooks | ICT | SMC | Wyckoff | 传统技术分析 |
|---|---|---|---|---|---|
| 高点假突破 | Failed Breakout | Buy-side Liquidity Sweep | Liquidity Grab | Upthrust | 假突破 |
| 低点假突破 | Failed Breakout | Sell-side Liquidity Sweep | Liquidity Grab | Spring | 假突破 |
| 区间 | Trading Range | Consolidation | Range | Trading Range | 震荡 |
| 突破回踩 | Breakout Pullback | Retracement | BOS Retest | Backup/Test | 突破回踩 |
| 衰竭 | Climax/Wedge | Divergence/SMT | Momentum Weakening | Effort vs Result | 背离/衰竭 |

系统不应该把这些名称直接当作不可分解的“黑盒策略”，而应该首先映射到统一因子。

例如：

```text
Unified Event:
FAILED_BREAKOUT_HIGH

Aliases:
- Brooks: Failed Breakout
- ICT: Buy-side Liquidity Sweep
- SMC: Liquidity Grab
- Wyckoff: Upthrust
- Traditional TA: False Breakout
```

之后再测试：

```text
Alpha(FAILED_BREAKOUT_HIGH)
```

以及：

```text
Alpha(Brooks完整定义)
Alpha(ICT完整定义)
Alpha(Wyckoff完整定义)
```

比较它们是否真正增加新的预测信息。

---

# 3. 系统核心公式

整个研究系统可以概括成：

```text
Alpha = f(Market State, Event, Structure, Context, Sequence)
```

更具体：

```text
市场状态
+
刚刚发生的事件
+
所处价格结构
+
时间/流动性/波动等上下文
+
事件发生顺序
=
交易机会 / 预测优势
```

不要把 Alpha 理解成“一个神奇形态”。

很多因子可能：

- 单独没有 Alpha；
- 在某一 Market Regime 下有 Alpha；
- 作为过滤条件有 Alpha；
- 与另一个因子组合后产生 Alpha；
- 只在特定顺序出现时有 Alpha；
- 只改善风险，而不明显提高收益。

---

# 4. 研究对象层级

系统中的对象必须明确分层。

## 4.1 Factor

因子是一个可测量量。

例如：

```text
TrendStrength = 0.81
ATRPercentile = 0.92
Momentum20 = 0.13
RangeScore = 0.27
```

## 4.2 State

State 表示当前市场所处状态。

```text
StrongBullTrend
Range
HighVolatility
LowLiquidity
Compression
Expansion
```

状态一般由一个或多个因子综合得到。

## 4.3 Event

Event 表示某个时间点附近“发生了什么”。

```text
Breakout
FailedBreakout
LiquiditySweep
Retest
Spring
Upthrust
Climax
SecondEntry
```

Event 必须包含事件时间和真实可用时间。

## 4.4 Zone

Zone 表示一个价格区域，而不是一个单值。

```text
FVG
Order Block
中枢
Support/Resistance Zone
Volume Node
```

至少包含：

```text
lower_price
upper_price
created_at
available_at
direction
age
active
filled_ratio
invalidated
```

## 4.5 Structure

Structure 表示复杂结构对象。

```text
Swing
Fractal
笔
线段
中枢
趋势段
Trading Range
```

## 4.6 Sequence

Sequence 表示带时间顺序的事件组合。

ICT：

```text
Sweep
→ MSS
→ FVG
→ Retest
```

Brooks：

```text
Trend
→ Pullback
→ First Attempt
→ Failure
→ Second Entry
```

缠论：

```text
形成中枢
→ 离开中枢
→ 次级别回抽
→ 不重新进入
→ 第三类买点
```

Sequence 不能简单实现为：

```text
A AND B AND C
```

必须支持时间约束、失效条件和状态转移。

## 4.7 Theory

Theory 是由多个底层对象组合出的“交易理论模板”。

```text
ChanThirdBuy
BrooksH2
ICTLiquidityReversal
WyckoffSpring
```

Theory 不应该自己重新实现所有底层逻辑，而应该引用统一 Factor / Event / State / Zone / Sequence。

---

# 5. 总体系统架构

```text
┌──────────────────────────────────────────────┐
│                  Theory Layer                │
│ Chan / Brooks / ICT / SMC / Wyckoff / OF    │
└─────────────────────┬────────────────────────┘
                      │
                      ↓
┌──────────────────────────────────────────────┐
│                 Sequence Engine              │
│ State Machine / Transition / Timeout / Rule │
└─────────────────────┬────────────────────────┘
                      │
        ┌─────────────┼──────────────┐
        ↓             ↓              ↓
┌─────────────┐ ┌─────────────┐ ┌──────────────┐
│ Event Engine │ │ Regime Engine│ │ Zone/Struct. │
└──────┬──────┘ └──────┬──────┘ └──────┬───────┘
       │               │               │
       └───────────────┼───────────────┘
                       ↓
┌──────────────────────────────────────────────┐
│             Unified Factor Registry          │
└─────────────────────┬────────────────────────┘
                      ↓
┌──────────────────────────────────────────────┐
│               Factor Compute Layer           │
│ Polars / Expressions / Computed Factors      │
└─────────────────────┬────────────────────────┘
                      ↓
┌──────────────────────────────────────────────┐
│                    Data Layer                │
│ AKShare / pytdx / Tick / L2 / Parquet        │
│ DuckDB / Corporate Action / Universe         │
└──────────────────────────────────────────────┘

                       │
                       ↓
┌──────────────────────────────────────────────┐
│              Experiment Engine               │
│ Single / Conditional / Combination /         │
│ Sequence / Ablation / OOS / Walk Forward    │
└─────────────────────┬────────────────────────┘
                      ↓
┌──────────────────────────────────────────────┐
│               Alpha / Signal Layer           │
└─────────────────────┬────────────────────────┘
                      ↓
┌──────────────────────────────────────────────┐
│             Portfolio / Risk Layer           │
└─────────────────────┬────────────────────────┘
                      ↓
┌──────────────────────────────────────────────┐
│          Execution / Trading Backend         │
│               vn.py Adapter                  │
└──────────────────────────────────────────────┘
```

---

# 6. 与 vn.py 4.x / vnpy.alpha 的关系

## 6.1 总体原则

vn.py 不作为系统架构母体。

正确关系：

```text
我们的 Research Core
        ↓
vnpy.alpha / Polars / Alphalens
        ↓
vn.py Trading Infrastructure
```

即：

> **研究核心独立，交易后端可替换。**

未来即使 vn.py 替换为 LEAN 或自研交易引擎，Factor Registry、Event Engine、Regime Engine、Sequence Engine、Theory Packs、Experiment Engine 和历史研究结果都不应受影响。

---

# 7. vnpy.alpha 模块处理策略

| 模块 | 决策 | 说明 |
|---|---|---|
| Polars 计算体系 | 直接采用 | 主因子计算引擎 |
| PyArrow / Parquet | 直接采用 | 主存储格式 |
| DataProxy 思路 | 封装/借鉴 | 表达式因子 |
| ts/cs/math 算子 | 采用 | 普通量化因子 |
| register_functions | 采用 | 自定义表达式函数 |
| Alpha101 | 因子包接入 | Quant Factor Pack |
| Alpha158 | 因子包接入 | Quant Factor Pack |
| AlphaDataset | 封装 | 不作为核心领域对象 |
| Train/Valid/Test | 采用 | 数据段规范 |
| Processor | 强烈借鉴 | 防泄漏处理流程 |
| Alphalens | 采用 | 基础单因子分析 |
| Lasso | Baseline | 模型基准 |
| LightGBM | Baseline | 模型基准 |
| MLP | 可选 | 非核心 |
| AlphaModel 接口 | 借鉴 | fit/predict/detail |
| AlphaStrategy | 后期使用 | 信号到组合 |
| BacktestingEngine | 最终回测 | 不用于大规模因子扫描 |
| AlphaLab 文件目录 | 仅借鉴 | 不照搬 |
| 每股一个 Parquet | 可兼容 | 不是最终唯一设计 |
| Pickle 作为实验仓库 | 不采用 | 不适合长期版本管理 |

---

# 8. 数据层

## 8.1 推荐数据链路

```text
pytdx / AKShare / Tick / L2
            ↓
        Raw Data
            ↓
         Parquet
            ↓
     ┌──────┴──────┐
     ↓             ↓
   DuckDB         Polars
 查询/统计       因子计算
     └──────┬──────┘
            ↓
      Research Core
```

## 8.2 各组件职责

### Parquet

负责原始行情、清洗行情、因子结果、事件、Zone、实验结果导出。

### DuckDB

负责元数据查询、条件筛选、跨股票查询、实验汇总、统计分析、因子索引和结果检索。

### Polars

负责大规模因子计算、rolling、横截面 ranking、组合数据处理和高速统计。

### Pandas

不作为主要计算引擎，仅用于第三方库兼容、导出、少量分析和 GUI 适配。

---

# 9. 数据对象

## 9.1 Bar

最小字段：

```text
symbol
exchange
datetime
timeframe
open
high
low
close
volume
turnover
open_interest
```

A 股建议增加：

```text
pre_close
adj_factor
limit_up
limit_down
is_suspended
is_st
```

## 9.2 Tick / L2

Order Flow 独立数据层，不要强行塞进 OHLCV。

```text
timestamp
symbol
last_price
last_volume
bid_price_1..N
bid_volume_1..N
ask_price_1..N
ask_volume_1..N
trade_side
trade_volume
cumulative_volume
turnover
```

---

# 10. Point-in-Time Universe

必须作为一级对象实现。

不能用“今天还存在的股票”回测过去。

```text
UniverseContext
├── listed
├── delisted
├── index_membership
├── ST
├── suspended
├── limit_up
├── limit_down
├── IPO_age
├── liquidity_filter
├── price_filter
└── tradable
```

因子测试应表达为：

```text
Alpha(Factor | Universe_t)
```

---

# 11. Factor Registry

这是整个系统最核心的数据结构之一。

每一个因子必须注册。

建议字段：

```text
factor_id
name_cn
name_en
primary_category
tags
factor_type
direction
source_theory
aliases
required_fields
required_timeframes
parameters
default_parameters
warmup_bars
output_type
output_range
occurred_at_rule
available_at_rule
confirmed_at_rule
causal
lookahead_risk
version
code_hash
description
formula
references
```

示例：

```yaml
factor_id: EVT.LIQ.SWEEP_HIGH.V1
name_cn: 上方流动性扫荡
name_en: Upper Liquidity Sweep
primary_category: liquidity
tags:
  - failed_breakout
  - event
  - reversal
factor_type: EventFactor
source_theory:
  - ICT
  - SMC
  - Brooks
  - Wyckoff
aliases:
  ICT: Buy-side Liquidity Sweep
  SMC: Liquidity Grab
  Brooks: Failed Breakout
  Wyckoff: Upthrust
parameters:
  lookback: 20
  penetration_atr: 0.2
  return_window: 3
```

---

# 12. 因子分类

第一层推荐：

```text
Factor Library
│
├── 01 Market State
├── 02 Structure
├── 03 Trend
├── 04 Momentum
├── 05 Volatility
├── 06 Liquidity
├── 07 Volume
├── 08 Order Flow
├── 09 Breakout & Failure
├── 10 Pullback & Retest
├── 11 Exhaustion
├── 12 Price Zone
├── 13 Multi-Timeframe
└── 14 Time
```

一个因子允许：

```text
primary_category = trend
tags = [market_state, regime, direction]
```

不要限制一个因子只能属于一个分类。

---

# 13. 因子类型系统

传统多因子系统通常认为：

```text
(datetime, symbol) -> number
```

本系统至少支持：

```text
ScalarFactor
BooleanFactor
EventFactor
StateFactor
ProbabilityFactor
ZoneFactor
StructureFactor
SequenceFactor
UniverseMask
```

---

# 14. ExpressionFactor 与 ComputedFactor

必须支持两条因子生成路线。

## 14.1 ExpressionFactor

适合：

```text
Momentum
ATR
Return
Volatility
Correlation
Rank
Slope
```

例如：

```text
(close / ts_delay(close, 20)) - 1
```

可以复用 vnpy.alpha / Qlib 风格表达式系统。

## 14.2 ComputedFactor

适合复杂结构：

```text
Swing
Chan Center
Third Buy
Brooks H2
ICT MSS
FVG
Wyckoff Spring
Market Regime
```

由独立 Engine 计算后返回标准对象，再写入因子系统。

---

# 15. Factor Pack

建立可启停、可版本化的 Factor Pack。

```text
Factor Packs
│
├── BaseQuantPack
├── Alpha101Pack
├── Alpha158Pack
├── ChanPack
├── BrooksPack
├── ICTPack
├── SMCPack
├── WyckoffPack
└── OrderFlowPack
```

每个 Pack 需要：

```text
pack_id
version
dependencies
factor_ids
event_ids
sequence_ids
required_data
description
```

---

# 16. Market Regime Engine

Market Regime 是系统一级模块。

## 16.1 市场状态因子

```text
TrendStrength
DirectionalEfficiency
HHHLScore
ATRPercentile
RealizedVolatility
VolExpansion
VolCompression
OverlapRatio
RangeEfficiency
MeanReversionScore
Turnover
Spread
Depth
AmihudIlliquidity
MarketBreadth
AdvanceDecline
LimitUpCount
LimitDownCount
```

## 16.2 Regime 输出

```text
direction:
- Bull
- Bear
- Neutral

structure:
- Trend
- Range
- Transition

volatility:
- Low
- Medium
- High

liquidity:
- Low
- Medium
- High
```

允许复合状态：

```text
BullTrend + MediumVolatility + HighLiquidity
```

## 16.3 研究方式

必须直接支持：

```text
Alpha(Factor | Regime)
```

---

# 17. Event Engine

所有事件采用统一 Schema。

```text
event_id
factor_id
symbol
timeframe
direction
occurred_at
available_at
confirmed_at
level
strength
metadata
version
```

---

# 18. Zone Engine

用于：

```text
FVG
Order Block
中枢
Support/Resistance
Supply/Demand
HVN/LVN
```

标准 Zone：

```text
zone_id
type
symbol
timeframe
direction
created_at
available_at
invalidated_at
lower_price
upper_price
strength
age
touch_count
filled_ratio
active
```

---

# 19. Structure Engine

负责识别：

```text
Fractal
Swing
Pivot
笔
线段
中枢
Trading Range
Trend Segment
```

Structure Engine 只负责回答市场结构是什么，不负责买卖。

---

# 20. Sequence Engine

这是平台的核心差异化能力。

## 20.1 必须支持

```text
Transition
Timeout
Invalidation
Confirmation
Optional Step
Repeated Step
Nested Sequence
```

## 20.2 示例：ICT

```text
STATE_0
  ↓ Sweep
STATE_1
  ↓ MSS within 5 bars
STATE_2
  ↓ FVG created within 10 bars
STATE_3
  ↓ Retest
COMPLETE
```

如果出现失效条件：

```text
INVALID
```

## 20.3 示例：Brooks H2

```text
Trend Established
→ Pullback
→ First Attempt
→ First Attempt Fails
→ Second Attempt
→ H2 Confirmed
```

## 20.4 示例：缠论三买

```text
Center Established
→ Leave Center
→ Lower Timeframe Pullback
→ Pullback Does Not Re-enter Center
→ Third Buy Confirmed
```

---

# 21. Multi-Timeframe Engine

必须作为独立模块。

支持：

```text
1D
60m
30m
15m
5m
1m
```

核心能力：

```text
HTF State
+
LTF Event
```

## 21.1 时间对齐规则

任何高周期信息只允许在其真实可用时间之后给低周期使用。

例如当天 10:30 的 5m 策略不得使用当天 15:00 才完成的 Daily Close。

---

# 22. Causal Guard / 防未来函数系统

这是整个项目的生死线。

## 22.1 三种时间必须分离

所有复杂因子/结构/事件建议统一记录：

```text
occurred_at
available_at
confirmed_at
```

例如某高点在 t0 出现，需要 t2 后才能确认：

```text
occurred_at = t0
confirmed_at = t2
available_at = t2
```

真实策略只能从 t2 使用。

## 22.2 必须拦截

```text
Centered rolling
Future shift
未来高低点
未来确认的 ZigZag
未来确认的分型
完整日线提前给盘中
未来指数成分
未来复权信息
未来退市结果
未来财报公布
```

## 22.3 因子必须声明

```text
causal = true/false
lookahead_risk = none/low/medium/high
```

高风险因子必须经过专门 Causal Test 才允许进入正式实验。

---

# 23. Processor / Factor Pipeline

所有标准化、填充、截面处理都必须分离为 Pipeline。

支持：

```text
Fit
Transform
```

典型 Processor：

```text
ReplaceInf
FillNA
Winsorize
RobustZScore
CSRank
CSRankNorm
Neutralization
IndustryNeutralization
SizeNeutralization
Clip
```

必须保存：

```text
fit_period
parameters
version
code_hash
```

严格禁止使用完整历史数据拟合标准化参数，再回测过去。

---

# 24. Research Engine 与 Execution Backtester 分离

系统必须有两套测试引擎。

# 25. Engine A：Factor Research Engine

目标：快速判断有没有统计 Alpha。

特点：

```text
Polars / NumPy
向量化
不模拟完整订单生命周期
```

指标至少包括：

```text
Forward Return
Mean Return
Median Return
Hit Rate
IC
Rank IC
ICIR
Rank ICIR
Quantile Return
Long-Short Spread
MFE
MAE
Decay
Turnover
Coverage
Conditional Return
Regime Conditional Alpha
Bootstrap CI
Permutation p-value
```

Forward Horizon：

```text
1
3
5
10
20
60 bars
```

---

# 26. Engine B：Execution Backtester

目标：Alpha 能不能真正变成可交易收益？

可基于 vn.py adapter。

模拟：

```text
order
fill
position
commission
slippage
price tick
limit up/down
suspension
capital
position sizing
portfolio
daily pnl
drawdown
Sharpe
```

不要用事件驱动 Backtester 大规模穷举全部因子组合。

---

# 27. 单因子实验

每个因子自动生成：

```text
因子分布
覆盖率
历史稳定性
不同股票
不同时间
不同市场状态
不同参数
不同持有期
```

事件因子还要统计：

```text
Event Count
Forward Path
MFE
MAE
Recovery
Failure Rate
Time to Target
```

---

# 28. 因子组合

支持至少三种组合方式。

## 28.1 Boolean

```text
A AND B
A OR B
A AND NOT B
```

支持嵌套。

## 28.2 Score

```text
Trend = +2
Sweep = +3
Exhaustion = +2
VolumeConfirmation = +1
FVG = +0.5
```

例如：

```text
score >= 6
```

触发 Signal。

## 28.3 Sequence

```text
A → B → C → D
```

不能与 Boolean Engine 混为一谈。

---

# 29. 消融实验 Ablation

任何复杂 Theory 或多因子模型都应该支持自动消融。

```text
A+B+C+D
A+B+C
A+B+D
A+C+D
B+C+D
```

定义：

```text
DeltaAlpha(B | A)
=
Alpha(A+B) - Alpha(A)
```

最终回答：复杂理论中的某条规则到底贡献了多少新增信息。

---

# 30. Theory Mapping

统一维护：

```text
Unified Factor/Event
↔
不同理论术语
```

例如：

```text
FAILED_BREAKOUT_HIGH
├── Brooks: Failed Breakout
├── ICT: Buy-side Liquidity Sweep
├── SMC: Liquidity Grab
├── Wyckoff: Upthrust
└── TA: False Breakout
```

理论允许保留自己的附加规则：

```text
ICTSweep
=
UnifiedSweep
+
ICT-specific conditions
```

然后测试：

```text
Alpha(UnifiedSweep)
vs
Alpha(ICTSweep)
```

---

# 31. Theory Pack 设计

## 31.1 ChanPack

```text
Fractal
Bi
Segment
Center
Divergence
FirstBuy
SecondBuy
ThirdBuy
MultiScaleNest
```

## 31.2 BrooksPack

```text
Trend
TradingRange
Breakout
FailedBreakout
Pullback
H1
H2
L1
L2
SecondEntry
Wedge
Climax
MeasuredMove
AlwaysIn
```

## 31.3 ICTPack

```text
LiquidityPool
LiquiditySweep
MSS
Displacement
FVG
OrderBlock
Breaker
PremiumDiscount
KillZone
```

## 31.4 SMCPack

作为独立 Pack，但尽可能映射统一事件。

## 31.5 WyckoffPack

```text
TradingRange
Spring
Upthrust
Test
SOS
SOW
LPS
LPSY
EffortVsResult
Accumulation
Distribution
```

## 31.6 OrderFlowPack

需要 Tick/L2 数据：

```text
Delta
CVD
BidAskImbalance
Absorption
Exhaustion
FootprintImbalance
DepthImbalance
AggressiveFlow
```

---

# 32. Alpha 不只定义为“赚不赚钱”

## 32.1 Prediction Alpha

```text
E[R_future | signal] != 0
```

## 32.2 Filtering Alpha

单独预测弱，但加入其他策略后 Sharpe、Drawdown、Hit Rate 明显改善。

## 32.3 Risk Alpha

不明显增加收益，但 MaxDrawdown、TailLoss、Volatility 显著改善。

---

# 33. 统计检验与防数据挖掘

随着因子数量和组合增大，必须主动防止：

```text
Data Snooping
Multiple Testing
Overfitting
Parameter Mining
```

建议后期加入：

```text
Bootstrap
Permutation Test
FDR
Deflated Sharpe Ratio
PBO
Walk Forward
Purged CV
Embargo
Parameter Stability
Subsample Stability
Cross-Market Validation
```

系统不能只展示最好的 Sharpe，还应该提示结果有多大概率只是大量实验中的偶然发现。

---

# 34. Experiment Registry

所有实验必须可复现。

```text
experiment_id
created_at
research_question
dataset_id
data_snapshot
universe_id
universe_version
factor_ids
factor_versions
parameters
processor_pipeline
regime_version
sequence_version
forward_horizon
cost_model
train_period
valid_period
test_period
random_seed
code_commit
result_metrics
artifact_paths
```

---

# 35. Factor Lineage

任何结果都必须可以追溯。

```text
Raw Data Snapshot
       ↓
Universe Version
       ↓
Factor Version
       ↓
Processor Version
       ↓
Experiment
       ↓
Signal
       ↓
Portfolio
       ↓
Backtest
```

看到 Sharpe=1.34，应能追溯数据版本、股票池、复权方式、因子版本、参数、Regime、成本和代码 Commit。

---

# 36. Signal 与 Factor 必须分开

严格遵循：

```text
Factor
↓
Factor Combination
↓
Alpha Signal
↓
Portfolio Construction
↓
Risk
↓
Execution
```

禁止“发现 Sweep 就直接 buy()”。

---

# 37. Portfolio Layer

后期至少支持：

```text
TopK
Threshold
EqualWeight
ScoreWeight
VolatilityWeight
MaxPosition
SectorLimit
TurnoverLimit
ExposureLimit
```

---

# 38. Machine Learning 的定位

ML 不是系统第一阶段核心。

正确顺序：

```text
因子定义
↓
统计验证
↓
条件 Alpha
↓
组合
↓
消融
↓
OOS
↓
稳定性
↓
ML
```

不是“几百因子直接扔 LightGBM”。

---

# 39. 模型 Baseline

后期可以保留：

```text
Linear Regression
Lasso
Ridge
Logistic Regression
LightGBM
MLP
```

统一接口：

```text
fit(dataset)
predict(dataset, segment)
detail()
save()
load()
```

---

# 40. 推荐源码架构

```text
quantlab/
│
├── data/
│   ├── providers/
│   │   ├── pytdx/
│   │   ├── akshare/
│   │   └── orderflow/
│   ├── lake/
│   │   ├── parquet/
│   │   └── duckdb/
│   ├── universe/
│   ├── adjustment/
│   └── schemas/
│
├── factors/
│   ├── registry/
│   ├── expression/
│   ├── processors/
│   ├── base/
│   ├── market_state/
│   ├── trend/
│   ├── structure/
│   ├── momentum/
│   ├── volatility/
│   ├── volume/
│   ├── liquidity/
│   ├── breakout/
│   ├── pullback/
│   ├── exhaustion/
│   ├── price_zone/
│   └── orderflow/
│
├── events/
│   ├── engine/
│   ├── schemas/
│   └── store/
│
├── zones/
│   ├── engine/
│   ├── fvg/
│   ├── order_block/
│   ├── center/
│   └── support_resistance/
│
├── structure/
│   ├── fractal/
│   ├── swing/
│   ├── segment/
│   ├── range/
│   └── center/
│
├── regime/
│   ├── factors/
│   ├── classifier/
│   └── state/
│
├── sequence/
│   ├── engine/
│   ├── state_machine/
│   └── definitions/
│
├── multitimeframe/
│   ├── alignment/
│   ├── resample/
│   └── context/
│
├── causal/
│   ├── guard/
│   ├── validators/
│   └── audit/
│
├── theories/
│   ├── mapping/
│   ├── chan/
│   ├── brooks/
│   ├── ict/
│   ├── smc/
│   ├── wyckoff/
│   └── orderflow/
│
├── packs/
│   ├── alpha101/
│   ├── alpha158/
│   ├── base_quant/
│   └── theory_packs/
│
├── experiments/
│   ├── registry/
│   ├── single_factor/
│   ├── conditional/
│   ├── combination/
│   ├── sequence/
│   ├── ablation/
│   ├── oos/
│   ├── walkforward/
│   ├── statistics/
│   └── reports/
│
├── signals/
├── models/
│   ├── baseline/
│   ├── lightgbm/
│   └── adapters/
├── portfolio/
├── risk/
├── backtest/
│   ├── research_engine/
│   └── vnpy_adapter/
├── storage/
│   ├── metadata/
│   ├── lineage/
│   └── artifacts/
└── ui/
    ├── factor_library/
    ├── factor_explorer/
    ├── regime_viewer/
    ├── sequence_builder/
    ├── theory_lab/
    ├── experiment_center/
    └── backtest_center/
```

---

# 41. GUI 设计

一级模块建议：

```text
数据中心
因子库
市场状态
结构与事件
序列构建器
理论实验室
实验中心
组合与模型
策略回测
结果对比
```

GUI 不应以“策略回测”为唯一核心入口。

---

# 42. 因子库页面

左侧分类：

```text
市场状态
趋势
结构
动量
波动
成交量
流动性
突破与失败
回调与重测
衰竭
价格区域
订单流
多周期
时间
```

选择因子后显示：

```text
定义
公式
来源
理论别名
参数
版本
数据要求
可用时间
未来函数风险
发生次数
分布
IC
未来收益
MFE
MAE
不同 Regime 表现
不同周期表现
参数稳定性
相关因子
冗余因子
最佳组合
相关 Theory
```

---

# 43. Theory Lab

例如点击 Brooks H2，显示：

```text
组成:
- Trend
- Pullback
- First Attempt
- Failure
- Second Entry

↓
每个组件单独 Alpha
↓
组合 Alpha
↓
完整 H2 Alpha
↓
Ablation
↓
Incremental Alpha
```

ICT、缠论、Wyckoff 同理。

---

# 44. Experiment Center

支持配置：

```text
研究对象
因子
参数
Universe
Timeframe
Period
Regime Filter
Forward Horizon
Combination Rule
Cost Model
Train / Valid / Test
```

实验类型：

```text
Single Factor
Conditional
Combination
Sequence
Ablation
Walk Forward
OOS
```

---

# 45. 第一阶段开发范围

第一阶段不要做大而全 UI，不做实盘，不做复杂 ML。

目标：

```text
Data Schema
→ Factor Registry
→ ExpressionFactor
→ ComputedFactor
→ Factor Pack
→ Factor Research Engine
→ Experiment Registry
→ 基础报告
```

完成后必须能：

1. 加载股票数据。
2. 注册因子。
3. 计算因子。
4. 单因子测试。
5. 输出 forward return / IC / hit rate / quantile。
6. 保存完整实验配置。
7. 重跑实验得到相同结果。

---

# 46. 第二阶段

开发技术交易研究独有能力：

```text
Event Engine
Zone Engine
Structure Engine
Market Regime
Sequence Engine
Multi-Timeframe
Causal Guard
```

---

# 47. 第三阶段

实现理论 Pack：

```text
Chan Pack
Brooks Pack
ICT Pack
SMC Pack
Wyckoff Pack
Order Flow Pack
```

建议顺序：

```text
Base Quant
→ Brooks
→ ICT/SMC
→ Wyckoff
→ Chan
→ Order Flow
```

如果已有成熟缠论结构代码，可以调整顺序。

---

# 48. 第四阶段

科学验证：

```text
Single Factor
Conditional Alpha
Factor Combination
Sequence
Ablation
OOS
Walk Forward
Bootstrap
Permutation
Multiple Testing
Parameter Stability
```

---

# 49. 第五阶段

进入：

```text
ML
Portfolio
Risk
vn.py Backtest
Paper Trading
Live Trading
```

---

# 50. 开发原则

## 50.1 不重复造轮子

已有成熟能力优先：

```text
Polars
PyArrow
DuckDB
Alphalens
scikit-learn
LightGBM
vn.py
```

但不要让第三方库决定整个 Research Core 的领域模型。

## 50.2 组合优于继承

对 vn.py 等第三方项目优先 Adapter / Wrapper / Dependency，不要大面积复制并修改第三方源码。

## 50.3 Research Core 必须独立

核心模块不得直接依赖 GUI，也不得直接依赖 vn.py Strategy。

## 50.4 所有研究结果可重现

任何实验都必须有：

```text
dataset
universe
factor version
parameters
processor
period
cost
code version
```

## 50.5 不允许“看图定义”

任何理论规则必须最终转换成明确输入、输出、时间、参数、边界和失效条件。

---

# 51. 智能体开发约束

后续智能体/Codex 开发时必须遵守：

1. 不自行扩展无关功能。
2. 不把项目改造成普通策略回测软件。
3. Research Core 的稳定性优先于 UI。
4. 不提前实现实盘交易。
5. 不提前实现复杂深度学习。
6. 不通过未来数据简化结构识别。
7. 不为了“让测试通过”改变原理论定义。
8. 所有复杂事件必须记录 available_at。
9. 所有跨周期数据必须经过时间可用性校验。
10. 每一个重要模块至少提供最小 deterministic 单元测试。
11. 不设计无法运行的过度严格测试体系。
12. 测试重点验证关键不变量、边界和未来函数。
13. 不允许在同一次任务中无理由大规模重构无关模块。
14. 所有新 Factor 必须注册到 Factor Registry。
15. 所有 Theory 必须尽量复用统一底层 Factor/Event。
16. 新增第三方依赖必须解释必要性。
17. 任何高计算量实验必须支持缓存和增量计算。
18. 所有实验输出必须包含失败原因，不允许 silent failure。

---

# 52. 必须建立的测试类型

## 52.1 Factor Determinism

相同 data + params + version 必须得到完全相同结果。

## 52.2 Causal Test

截断未来数据后，t 时刻之前的结果不得变化。

这是验证未来函数最重要的自动测试之一。

## 52.3 Multi-Timeframe Alignment Test

保证低周期不会提前看到尚未完成的高周期信息。

## 52.4 Universe Test

确保历史成分股、停牌、ST、上市时间处理正确。

## 52.5 Sequence State Test

验证 Transition、Timeout、Invalidation。

## 52.6 Research Reproducibility

同一个 Experiment ID 重跑结果一致。

---

# 53. 缓存设计

大量实验会重复计算因子。

缓存 Key 至少考虑：

```text
factor_id
factor_version
parameters_hash
data_snapshot
symbol
timeframe
date_range
```

只有发生变化的部分重新计算。

---

# 54. 版本管理

Factor Version 与 Git Commit 不完全相同。

建议：

```text
Factor Semantic Version
+
Code Hash
```

如果改变定义，则升级 Factor Version；如果只优化性能但结果完全一致，可保留语义版本并更新 code_hash。

---

# 55. 性能设计

不要一开始进行 2^N 全组合穷举。

组合搜索应支持：

```text
单因子筛选
↓
相关性去重
↓
稳定性过滤
↓
双因子
↓
有意义组合
↓
Ablation
↓
必要时 ML / Search
```

避免组合爆炸和数据挖掘。

---

# 56. 因子去相关

后期需要：

```text
Pearson
Spearman
Mutual Information
Conditional Correlation
IC Correlation
Signal Overlap
Event Overlap
```

如果 ICT Sweep、Brooks Failed Breakout、Wyckoff Upthrust 的事件重合度极高，应提示它们可能只是同一底层因子的不同名称。

---

# 57. 最终研究目标示例

```text
150 个理论概念
↓
压缩成 30 个候选底层因子
↓
15 个具有统计预测价值
↓
去相关后 8 个独立因子
↓
5 个样本外稳定
↓
4 个交易成本后仍然有效
```

然后比较 Chan、Brooks、ICT、SMC、Wyckoff：

- 哪些理论主要在重复已有市场现象？
- 哪些理论规则真正提供额外 Alpha？

---

# 58. 核心区别

传统 vnpy.alpha：

```text
Numeric Factor
↓
Dataset
↓
ML
↓
Signal
↓
Trading
```

本项目：

```text
Market Theory
↓
Factor / State / Event / Zone / Sequence
↓
统一市场语言
↓
Scientific Experiment
↓
Incremental Alpha
↓
Model / Portfolio / Trading
```

---

# 59. 一句话定位

> **这是一个“技术交易理论科学实验平台”，不是一个普通技术指标软件，也不是一个普通策略回测器。**

缠论、Brooks、ICT、SMC、Wyckoff、Order Flow 不再分别作为孤立系统，而是作为 Theory Pack 映射到统一 Factor / State / Event / Zone / Structure / Sequence，再通过同一套实验体系进行公平验证。

---

# 60. 最终技术建议

推荐主技术栈：

```text
Python
PyQt / PySide
Polars
PyArrow
Parquet
DuckDB
AKShare
pytdx
NumPy
SciPy
statsmodels
scikit-learn
LightGBM
Alphalens-reloaded
vn.py
```

可选：

```text
PyTorch
Qlib
LEAN Adapter
```

---

# 61. 第一版 MVP 验收标准

- [ ] Parquet 数据加载。
- [ ] DuckDB 数据查询。
- [ ] Factor Registry。
- [ ] ExpressionFactor。
- [ ] ComputedFactor。
- [ ] Factor Pack。
- [ ] Point-in-Time Universe 基础接口。
- [ ] Factor Research Engine。
- [ ] Forward Return。
- [ ] IC / Rank IC。
- [ ] Quantile Analysis。
- [ ] MFE / MAE。
- [ ] Parameter Sweep。
- [ ] Experiment Registry。
- [ ] Factor Version。
- [ ] Data Snapshot。
- [ ] Causal Test 基础框架。
- [ ] 可导出 Markdown/HTML 实验报告。

只要完成这些，就已经形成平台真正的“研究内核”。

---

# 62. 第二版验收标准

- [ ] Event Engine。
- [ ] Zone Engine。
- [ ] Regime Engine。
- [ ] Sequence Engine。
- [ ] Multi-Timeframe Engine。
- [ ] Causal Guard。
- [ ] 条件 Alpha。
- [ ] 因子组合。
- [ ] Ablation。
- [ ] Theory Mapping。
- [ ] Brooks 基础 Pack。
- [ ] ICT 基础 Pack。
- [ ] Chan 接口适配。

---

# 63. 第三版验收标准

- [ ] Walk Forward。
- [ ] OOS。
- [ ] Bootstrap。
- [ ] Permutation。
- [ ] Multiple Testing。
- [ ] Factor Correlation。
- [ ] Factor Clustering。
- [ ] Incremental Alpha。
- [ ] Theory Lab。
- [ ] Factor Explorer。
- [ ] Regime Viewer。

---

# 64. 第四版验收标准

- [ ] Portfolio Construction。
- [ ] Risk Layer。
- [ ] vn.py Execution Adapter。
- [ ] 完整费用模型。
- [ ] A 股涨跌停/停牌模拟。
- [ ] 组合回测。
- [ ] Paper Trading。

---

# 65. 对智能体的最终任务理解

如果智能体收到本文件，应首先理解：

本项目不追求：

```text
“快速实现几个技术指标 + 写一个策略 + 画收益曲线”
```

真正任务是：

```text
把复杂技术交易理论
↓
转换成统一、可计算、无未来函数的研究对象
↓
建立可复现实验
↓
验证单因子 Alpha
↓
验证条件 Alpha
↓
验证组合 Alpha
↓
验证增量 Alpha
↓
验证样本外与交易成本
```

架构正确性、因果正确性和实验可复现性，优先级高于 UI 完整度、实盘交易、机器学习复杂度和功能数量。

---

# 66. 最终开发原则摘要

```text
Research First
Theory Decomposition
Unified Factors
Causal Correctness
Reproducible Experiments
Incremental Alpha
Backend Decoupling
```

即：

> **先研究，再策略；先因子，再模型；先因果正确，再追求漂亮收益；先统一市场现象，再讨论理论名称。**

这就是整个项目后续所有设计和开发决策的最高原则。
