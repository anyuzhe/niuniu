# 牛牛：跟踪基准换版与本轮收尾验收

日期：2026-09-11。项目：`/Volumes/Lexar/niuniu`。本轮基线：`358dc76`。
上轮候选对照、桌面通知及任务恢复代码已在本轮开始时核实：本地与远端均为358dc76，工作区干净。
本轮补齐上轮未完成的基准换版工作流，并更新总体方案的最新状态索引；不把整条八步路线标为完成。

## 1. 实际交付

新增宿主界面的“跟踪基准换版（保留旧历史）”。旧跟踪须先暂停，旧授权须撤销，未完成任务和待批准刷新提案须先处理。
可从旧跟踪生成相同历史区间的新版基准提案，仍通过原人工审批和唯一研究队列计算。
选择已经完成的新版基准后，程序核对当前运行环境、因子定义及实现、证券、起止日期、复权、上下文和预处理等口径。
旧新环境允许不同，但因子实现和研究规则必须一致；K线及观测表按证券和时间排序后逐列精确比较，含空值、数据类型与未来标签。
只有本次同区间归档数值一致时才允许建立版本关系。数据修订、因子代码变化或区间变化不能通过此入口被当作等值升级。

确认后创建独立的新跟踪及新基准快照，保留旧跟踪与旧快照，不拼接新旧统计。新跟踪在完成后暂停，没有继承研究授权或刷新提案。
换版回执先持久化为prepared，再落成completed；中断后重复确认可接续同一个目标，不能重复增加跟踪或快照。
已完成操作再次确认是读取原回执，不会重新暂停用户后来主动启用的新跟踪。
确认之后来源发生变化会拒绝完成回执；已经创建的候选保持暂停，需人工检查。历史回执不等于来源当前仍完整。
模型工具没有新增换版审批或直接执行权限。

## 2. 使用

重启牛牛，在“Baostock 数据”菜单打开“跟踪基准换版（保留旧历史）”。
先在原跟踪池暂停旧跟踪，撤销其自动授权并处理未完成任务。
在换版窗口选择旧跟踪，点击“生成同区间重建提案”，到原审批窗口核对并批准。
研究完成后刷新列表，选择新版基准与新名称，点击“核对换版计划”。
确认完整核对结果后勾选确认，再点击“确认建立新版暂停跟踪”。
“查看旧跟踪的换版记录”可复看版本关系。新跟踪需另行启用和授权；本轮不替用户启用正常工作区的自动研究。
## 3. 实际验收

全仓584项通过，0失败、0跳过；相对本轮开始的571项新增13项。
覆盖旧跟踪暂停及授权检查、固定提案、精确对照、并发互斥、重复确认、中断恢复、来源变化以及界面选择变更。

| 验证项 | 实际结果 |
|---|---|
| 同区间真实基准重建 | 三股2024年上半年PE研究，由原队列执行1项基准重建任务 |
| K线对照 | bars.parquet共351行，逐列精确一致 |
| 因子与标签对照 | observations.parquet共351行，逐列精确一致 |
| 换版操作新增研究任务 | 0；计算来自事先人工批准的重建提案 |
| 新跟踪 | 独立ID，1份新基准快照，完成后暂停 |
| 新自动授权 | 0；不继承旧授权 |
| 第二个进程重复确认 | 返回原换版回执，不重复建立跟踪或快照 |
| 原始来源 | 实际受检原归档及数据文件保持不变 |

旧归档运行指纹为94083c693f08aede1110e1b07eb89c2acb922d125ebc0b9d7e6fc74746b5273a；
本轮运行指纹为5d0d1df72f7fc5f489cdd6f1555ace5321ec4ec4d846f59c4c809e8be04a5a87。
这是实际旧版归档和当前代码重建结果的对照，不是只把同一份文件复制后宣称复算。

真实验收在独立工作空间内执行，批准由测试脚本调用宿主接口完成；不冒称原生人工点击或连续实盘运行。
只证明所核对历史样本在两次归档中数值一致，不证明所有输入下程序等价、未来收益或严格PIT。
本轮没有改动正常跟踪、启用自动研究、重新下载行情或新增模型侧换版权限。

## 4. 证据与持久记录

验收目录：artifacts/rebase-closeout-20260911/。
full-tests.log/.exit为完整回归结果；real-acceptance.json、real.log、real.exit与verify_real.py为真实验收。
换版关联记录位于artifacts/_watch_rebases/<new_watch_id>/receipt.json；旧新跟踪仍各自存于_tracking。
所有回执都只是本次操作的历史证据；重新核对现有来源时仍需执行来源完整性检查。
本轮仅源码、测试和说明纳入Git；数据、个人记忆、授权、模型设置和运行产物不纳入提交。
最终提交号及远端核对回执写入验收目录的git-release.json，避免把尚未推送的状态写成已发布。

## 5. 原八步进度与保留边界

第1–5步核心可用。第6步已具有持久跟踪、有限预授权刷新、自动同步、应用内及可选桌面通知、人工恢复结果核对，以及本轮的同区间基准换版。
第6步仍缺跨批次自动滚动与自动下载衔接、完整中间交易日缺口审计、迟到数据时间合同、连续多日运行验收。
本轮换版不是任意跨版本迁移：因子实现、输入数据和观测存在差异时直接拒绝，旧新历史不拼接。
第7步目前已有候选共同样本对照及原组合研究基础；受限表达式生成、隔离候选验证与审批注册未完成。
第8步标准外部Agent/MCP服务和常驻部署未完成；现有CLI不等于完整常驻服务。
严格PIT、真实每日市值和官方历史交易规则资料缺口仍保留，没有因工程测试通过而补造。
总体方案首页和核心进度文档本轮改为清晰的当前索引，下方保留各轮历史记录，不再将旧500项或旧P1状态称为最新。

## 6. 工程入口

- agent/watch_rebase.py：同区间重建提案、精确核对、宿主确认、可接续换版回执与历史查询。
- desktop/watch_rebase.py：人工换版界面；沿用原提案审批和任务队列。
- desktop/data_workbench.py：正常工作台菜单接线。
- tests/test_watch_rebase.py、tests/test_watch_rebase_desktop.py：10项核心测试及3项界面测试。
