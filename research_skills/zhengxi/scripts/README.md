# Scripts 政策

当前没有复制或信任任何外部脚本。

Research Skill 审计器只做静态路径、字节、哈希和合同检查，永不执行本目录脚本。未来若引入基金列表、净值或季度持仓工具：

1. 必须逐个文件内容寻址并接受代码审阅；
2. 默认网络权限为 `DENY`；联网抓取必须由宿主对具体任务显式授权；
3. 原响应、headers、抓取时点与来源 URL 保存到独立数据根；
4. 脚本输出默认 `RETROSPECTIVE_REFERENCE`，不能自行签发 Strict PIT receipt；
5. 不得调用交易、券商、订单、资金或主仓库写接口。
