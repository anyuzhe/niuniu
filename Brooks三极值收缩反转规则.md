# Brooks Wedge 三极值收缩反转代理 1.0.0

这是总体方案 Wedge 概念的一组有限、可复现规则：五个交替确认极值中，三次同方向新极值的末次增量缩小，再等待颈线反转。它不拟合上下趋势线，不检查时间斜率/角度或通道收敛，不等于完整主观楔形或所有三推形态。

## 极值来源与候选

复用 ConfirmedPivotEngine 的严格极值：候选 high/low 必须严格高于/低于左右邻域，同价不算。默认 left=2、right=2，只有右侧窗口完成才发布。每只证券、单周期分别处理，重复 available_at 拒绝；预热 left+right 根前输出 null，之后非事件为 0，事件为 1。

未发布形态前只保留最近五个交替极值。同侧连续极值只用更极端的新点替换候选末端，较弱或相等的点忽略；这些是尚未发布的内部候选，不重写历史事件。同根同时确认 high 和 low 时，无法确定盘中顺序，清空未发布候选。

## 形态建立

方向 UP/DOWN 指预期反转方向，而非此前推进方向：

| 条件 | UP：向下推进后的反转 | DOWN：向上推进后的反转 |
|---|---|---|
| 五个交替点 | L1,H1,L2,H2,L3 | H1,L1,H2,L2,H3 |
| 三个新极值 | L1>L2>L3 | H1<H2<H3 |
| 中间反向点 | H1>H2 | L1<L2 |
| 末次增量缩小 | 0<L2−L3≤(L1−L2)×ratio | 0<H3−H2≤(H2−H1)×ratio |
| 冻结极值 | L3 | H3 |
| 冻结颈线 | H2 | L2 |

contraction_ratio 默认 0.8，有限数 `(0,1)`。形态在第五点的确认时间发布 SETUP，不回填到第五点发生时间。每个方向只维护一个活跃形态；活跃期间忽略新极值，不替换颈线或极值。形态来源五点及其 occurred_at/available_at 全部保存。

## 后续确认与终止

必须在 SETUP 之后的另一根 K 线上推进：

1. 严格超过 max_bars（默认 40）个已观察条，发布 EXPIRED，SequenceMatch 状态为 timeout。
2. UP 当根 low 严格跌破 L3，或 DOWN 当根 high 严格突破 H3，发布 INVALIDATED。即使同根收盘也越过颈线，仍先失效，不推测盘中先后。极值相等不失效。
3. UP 收盘严格高于 H2，或 DOWN 收盘严格低于 L2，发布 CONFIRMED，状态为 completed。颈线相等不完成。

若 SETUP 当根收盘已经越过颈线，也只建立形态；后续另一根收盘仍满足阈值才确认。这里是阈值确认，不额外要求再次从颈线另一侧穿越。

max_bars、left、right 均要求整数 `[1,10000]`。超时计数按已观察条，包含跨交易日加载历史但不补造缺失条。终止当根不启动同方向新形态；清空候选，后续从新确认点重新收集，不复用旧形态极值。末尾等待形态保持 active。

## 因子、研究和审计

BrooksWedgePack@1.0.0 新增八个布尔事件：`BROOKS.WEDGE_` + `SETUP/CONFIRMED/INVALIDATED/EXPIRED` + `_UP/_DOWN`。

模板 `RESEARCH.BROOKS_WEDGE_REVERSAL` 要求向上 CONFIRMED 与 20 根动量为正。既有理论全流程展开对应方向四组件、动量、组合、消融、固定样本外、滚动及预设参数对照，共 10 项顶层任务。预设对照 max_bars=20/40，保留全部结果，不自动择优。

事件、SequenceMatch、失败/超时/末尾等待进入原有 sequence_audit。回放按 available_at 截断，元数据保留五个来源点、两段增量、冻结颈线/极值和确认条。本轮未新增趋势线或专用楔形绘图界面。

测试和真实样本证据见 [验收报告](artifacts/brooks-wedge-acceptance/report.md)。这些结果验证规则实现与研究流程，不证明预测有效性或可成交性。
