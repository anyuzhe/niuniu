# 牛牛 AI 研究助手：主动研究议程与安全 Alpha Factory 验收说明

日期：2026-09-12
项目：`/Volumes/Lexar/niuniu`

## 1. 本轮目标

原八步 AI-native 改造完成后，本轮继续把牛牛从“响应用户问题的研究助手”推进为“能主动发现研究缺口、但仍受固定计划和人工授权约束的研究员”。

新增两部分：

- **Research Agenda**：从当前工作空间的研究记忆、DSL 候选、增量证据、Alpha Factory、Watchlist 和失败任务生成确定性研究待办。
- **Safe Alpha Factory**：把一组已经人工注册的 DSL 候选、固定基准、控制因子、样本、统计槽位和淘汰规则一次冻结，再由宿主一次人工批准执行。

两者都不会绕过既有权限边界。Agenda 不运行研究；Factory 模型侧只能预览/保存/读取，不能执行、同步结果或晋级观察池。## 2. Research Agenda

Agenda 当前会主动提示以下研究缺口：

- 未验证或尚无当前结论的研究假设；
- 已通过 DSL 预检、但尚未人工注册的候选；
- 已注册但尚无完成单因子研究的候选；
- 已有候选研究、但没有增量证据/Factory 覆盖的候选；
- 固定 Factory 待人工批准、运行中待收口、或通过筛选但尚未人工进入观察池的候选；
- Watchlist 来源变化、历史修订、缺失/成熟样本提醒；
- 失败或中断的原任务。

Agenda 只读取当前工作空间的持久化证据，不调用模型、不下载数据、不创建提案、不运行研究。优先级是工作流优先级，不是收益预测。

本轮修复了候选去重：执行回测产生的同参数信号子归档不会再被误当成“新的未验证候选”。已被完成 Factory 覆盖的候选不会重复显示为“缺少增量证据”。## 3. Safe Alpha Factory

Factory 计划会在运行前冻结：名称、1–12 个候选 ID、基准因子 run、1–5 个控制因子 run、可选成本后基准账户、训练截止、成本后评价起点、持有期、family alpha、共同有限样本下限、最大信号相关阈值、配对 IC 方向规则，以及是否要求成本后净收益增量。

批准前还会冻结当前 data root 的输入签名；批准后行情文件、资格掩码、代码/依赖或基准归档变化都会拒绝执行，不能静默替换数据。

每个候选由原 JobQueue 运行固定的 DSL.RESTRICTED 单因子研究；启用成本后筛选时还运行同一账户口径的 execution。所有候选任务终态后才统一计算共同样本诊断、残差 IC 和净收益增量。

**Holm 校正在整个 Factory 的全部计划测试槽位上一次性完成。** 失败/不可检验槽位继续占原检验族位置；不能因为结果不好删除候选、换参数或缩小校正分母。

观察池推荐规则也在运行前固定：覆盖率、信号相关、配对 IC 方向、残差增量以及可选成本后增量必须全部通过。推荐仍不等于 Alpha 认证；进入 Watchlist 需要第二次明确宿主确认，且不会自动创建跟踪授权。## 4. 真实数据验收

使用既有真实 Baostock 三股 2024 H1 前复权日线，在独立工作区注册两个 DSL 候选，固定同一 BASE.MOMENTUM 基准和同一成本后账户口径，一次人工批准完整 Factory。

最终创建 4 个计划检验槽位：2 个 residual IC、2 个成本后净收益增量。四项原始检验全部完成，Factory 级 Holm 后均未拒绝零假设；两个候选都被正确判定为不进入观察池，没有自动换参数或追加候选。

父 Factory 归档使用冻结来源重新计算：基准因子、基准账户、两个候选因子、两个候选账户、四个派生检验、共同样本诊断、Factory 级 Holm 和观察池推荐规则全部重算，最终 `numerically_matched`。

修复 Agenda 去重后，同一真实工作区重新生成 Agenda：`total=0`，没有重复的 `candidate_needs_incremental` 待办，也没有创建任何新任务。

源行情文件验收前后哈希一致；这轮结果只验证工作流和统计边界，不证明候选存在 Alpha。

## 5. 测试与边界

最终全仓回归：**673 项通过，0 失败，0 跳过，exit=0**。

模型/MCP 新增的能力只有 `get_research_agenda`、`preview_alpha_factory`、`propose_alpha_factory`、`get_alpha_factory`；没有 `submit_alpha_factory`、`sync_alpha_factory`、`promote_alpha_candidate` 或任意执行入口。

严格 PIT、历史真实每日总市值、官方历史每日涨跌停/特殊上市规则仍是独立数据缺口；Factory 的显著性和 Watchlist 推荐均不能替代这些资料，也不能视为未来盈利证明。

证据目录：`artifacts/agenda-factory-finish-20260912/`。