# 牛牛 AI · 自主研究与打板情绪研究：建设规划与进度

- 文档性质：AR（Autonomous Research）主线的**权威规划 + 逐步实施进度 + 每步验收记录**。
- 建立时间：2026-09-16 18:50（Asia/Shanghai）
- 规划基线：本地 `main` 提交 `bada159`（领先 `origin/main` 18 个提交，未推送）。
- 基线测试：Linux arm64 隔离克隆逐模块运行全仓 **1025** 个测试，1022 通过；3 个失败均为环境/时序相关（见 AR-0.1）。《开发历程总档案》中记录的 1006 是之前的基线。
- 维护规则：每完成一步，在**同一个 Git 提交**内更新本文件 §9“实施进度”和 §10“验收记录”，并追加《牛牛AI交易助手_开发历程与功能变更总档案.md》；能力开放给 AI 使用时同步 `agent_memory/`。

## 1. 目标与非目标

目标：让牛牛能围绕 A 股**行情、市场情绪、打板（涨停）规律**持续做可复现、可证伪的研究：

1. 有正确的涨停、连板、炸板、一字板等基础事实，并覆盖足够长的历史。
2. 每天积累公开市场证据（涨停池、炸板池、龙虎榜、题材成分、人气榜），前瞻留证、不补历史。
3. 能以“先冻结研究规格、再计算结果”的方式做事件研究，并用保守成交模型回答“能不能买到、卖掉”。
4. AI 能调用这些数据写收盘复盘、做可验证的概率预测、提出并检验假设。
5. 在有限授权和预算内形成夜间自主研究循环，积累结论库并监控规律失效。

非目标（本主线不做）：自动下单或实盘；把研究结论直接变成 BUY/SELL；接入付费数据（只预留设计，见 AR-6）。

## 2. 已确认的决策

| 日期 | 事项 | 决定 |
|---|---|---|
| 2026-09-16 | Git 提交方式 | 每步独立本地提交，不推送远端 |
| 2026-09-16 | 付费数据（Tushare 等） | 暂不接入，只在文档中预留接口设计 |
| 2026-09-16 | 公开网页数据 | 授权**收盘后**低频归档（东方财富涨停池、龙虎榜、概念成分、人气榜）；记录抓取时间留证；不补历史；遵守站点限制 |
| 2026-09-16 | 推进方式 | 按本文档顺序连续实施；只有遇到必须由用户决定的事项才暂停 |

盘中与集合竞价时段的采集不在上述授权范围内：相关代码可以写好，但默认不启用（待决定事项 D-1）。

## 3. 与现有体系的关系和边界

- **数据资格**：本主线新增数据默认是 `research_only`（回溯观察）或“前瞻观察”（以抓取时点留证），不得冒充 `strict_pit` / `official_rule_covered`。正式 MarketRules、PIT Universe、SecurityStatus 链路保持不变。
- **事实 / 机器状态 / AI 判断分层**：情绪指标是事实派生；情绪周期是带版本的机器规则状态；AI 复盘是判断，不回写事实。
- **标签收益 ≠ 可执行收益**：事件研究必须同时报告“信号样本”和“保守成交后样本”。
- **前瞻不补档**：公开证据只按抓取时点归档；错过的交易日记录为 missed，不得用事后数据伪造当日。
- **AI 权限**：只读工具 + 预注册研究任务；不给 shell、写代码、联网、交易权限。
- **运行安全**：`com.niuniu.qimo-paper` 通过可编辑安装直接加载 `src/`，主线代码一合并即对它生效；`com.niuniu.pit-20260917` 固定在提交 `648a162` 独立运行，不受影响。因此每次合并前必须全仓测试通过。
- **存储位置**：原始归档放 `artifacts/_market_data/`（与 DailyMarket 一致）；派生研究数据放 `artifacts/_limit_research/`；二者都被 Git 忽略。

## 4. 现状差距（2026-09-16 核查代码与数据）

| # | 能力 | 现状 | 差距 |
|---|---|---|---|
| 1 | 涨停价推算 | `prep_scanner` 无官方规则时的回退、`mark_limit_closes` 都用固定比例 | 创业板 2020-08-24 前应为 10%；创业板/科创板 ST 应为 20%；沪深主板 ST 自 2026-07-06 起为 10%；新股上市初期无涨跌幅；`sh.689`、`sz.302` 未归类 |
| 2 | 涨停状态 | 只识别“收盘价等于涨停价” | 缺触板、炸板、一字板、T 字板、天地板/地天板、N 天 M 板 |
| 3 | 历史 ST/停牌 | DailyMarket 从 2026-09-15 起带 isST/tradestatus；MQC 日线湖没有 | 缺历史全市场逐日状态数据集 |
| 4 | 市场情绪 | `route_market_node` 只有 EXTREME_RISK / RETREAT_HIGH_RISK / UNKNOWN 三档 | 缺标准化情绪指标、周期状态、相似日检索 |
| 5 | 题材 | Theme 事实只能在桌面端手工录入 | 缺板块成分、题材梯队、龙头候选的自动事实 |
| 6 | 涨停明细 / 龙虎榜 / 人气 | 无 | 需要从现在开始前瞻归档 |
| 7 | 研究方式 | 因子型研究（single/holdout/walkforward/ablation/sweep/correlation）；受限 DSL 只支持单股 OHLCV 滚动运算 | 缺事件研究、横截面条件、情绪/题材/涨停状态字段 |
| 8 | 成交模型 | 回测有 T+1、涨跌停不可成交 | 缺按打板方式区分的保守成交（排板、次日开盘等） |
| 9 | 自主研究授权 | Research Session Grant 最长 24 小时、只能跑已注册因子、禁止联网 | 缺长期研究计划、周预算、结论库、失效监控 |
| 10 | 交易费用 | `ExecutionConfig` 默认 `sell_tax_bps=0`、`transfer_bps=0`；qimo 模拟盘授权配置同样为 0 | 印花税（2023-08-28 起卖出 0.05%）与过户费未计入，模拟收益偏高（待决定事项 D-2） |

## 5. 分阶段计划

每一步都包含：实现 → 测试 → 更新本文档与开发总档案 → 独立提交。状态以 §9 为准。

### AR-0 规划与基线

- **AR-0** 建立本文档，并在 README、总计划、总体架构、开发总档案中登记。
- **AR-0.1 测试稳定性（Linux）**：`test_offline_environment` 对非 macOS arm64 平台按条件跳过；`test_system_health_desktop` 把固定等待 250ms 改为等待页面渲染完成。验收：Linux 隔离克隆全仓 0 failed。

### AR-1 打板研究数据底座

- **AR-1.1 A 股涨跌幅制度表 v1**
  - 新增按交易日生效的涨跌幅规则：识别板块，返回涨跌幅比例，或返回“无涨跌幅 / 未建模”及原因码（规则见 §6.1）。
  - 接入：`prep_scanner` 无官方规则时的回退和 `mark_limit_closes` 默认比例改为按日期计算；正式 official MarketRules 与显式覆盖参数的优先级不变。
  - 验收：边界日期、各板块 ST、新股窗口、未知代码的单测；修正依赖旧口径的测试并写明原因；全仓通过。
- **AR-1.2 涨停状态标注器 v1**
  - 纯函数：输入逐日行（前收、OHLC、停牌、ST、上市日期），输出涨跌停价与形态字段（见 §6.2）。
  - 验收：典型形态夹具、停牌跨越、价格精度、无涨跌幅日排除。
- **AR-1.3 回溯全市场日线数据集 v1**
  - 抓取计划冻结日期区间、字段、`query_stock_basic`（上市/退市日期，含已退市证券）与交易日历快照；再按证券调用 Baostock `query_history_k_data_plus`（不复权日线，含 isST、tradestatus、前收、成交额、换手）逐只回溯；可断点续传，可按哈希分片多进程并行。
  - 每只证券一个目录：原始行（gzip）、类型化 parquet、带 checksum 的 manifest，整个目录原子写入；失败写入失败记录并在下次抓取时重试；损坏目录只能显式隔离后重抓。
  - 位置：`artifacts/_market_data/retro_daily/<capture_id>/`；资格 `research_only`；不覆盖北交所。
  - 回溯区间之后的交易日由现有 DailyMarket 按日归档补齐（AR-1.4 事件库合并两者）。
  - CLI：`python -m quantlab.agent.retro_daily_cli`（pyproject 同步登记 `niuniu-retro-daily`，需重新安装后才出现该命令）。
  - 验收：模拟 SDK 单测；真实数据烟测并与制度表交叉核对；补齐 2019-01-02 至 2026-09-15 并记录实际覆盖。
  - 设计调整（2026-09-16）：实测按日全市场查询每天 18–53 秒；按证券查询 7.7 年日线 2–6 秒/只；服务端总吞吐约 1,300–1,400 行/秒，6 或 10 个并发差别不大。故改为按证券回溯、默认 4 个分片。
- **AR-1.4 涨停事件库 v1**
  - 由 AR-1.3 数据 + AR-1.1/1.2 构建：凡当日触及涨停/跌停、或前一交易日收盘涨停的股票日各记一行，含特征与 T+1/T+2 结果标签。
  - 输出 `artifacts/_limit_research/limit_events/<build_id>/`；manifest 记录全部输入日的内容哈希与制度表版本，可复算。
  - 验收：夹具构建与复算一致；真实数据构建烟测并抽样核对已知涨停。
- **AR-1.5 日度情绪指标 v1**
  - 指标定义见 §6.3；每日一行；T 日收盘后可用，只能用于 T+1 及以后的决策。
  - 验收：逐指标定义单测；真实数据构建；与公开口径做量级核对。
- **AR-1.6 情绪周期机器状态 v1 + 相似日检索**
  - 以近 250 个交易日的分位数计算“情绪温度”和方向，按带版本的规则表给出阶段；相似日只在严格早于 T 的历史中查找。
  - 验收：无未来函数测试（截断未来数据前后结果一致）；规则输出可解释。

### AR-2 公开市场证据收盘后归档（前瞻积累）

- **AR-2.1 公开网页证据采集框架**：主机白名单、仅 HTTPS、大小/超时/频率限制；保存原始字节、SHA256、抓取时间和规范化表；可注入 HTTP 以便离线测试；任何校验失败即 fail-closed。
- **AR-2.2 东方财富涨停股池、昨日涨停池、炸板股池、跌停股池、强势股池**：首次/最后封板时间、炸板次数、封板资金、连板数、所属行业。
- **AR-2.3 龙虎榜**：每日上榜明细与营业部买卖明细。
- **AR-2.4 板块**：概念/行业板块列表、板块成分快照、板块日行情。
- **AR-2.5 人气榜**：东方财富人气榜快照。
- **AR-2.6 收盘后调度与健康**：macOS LaunchAgent（交易日 15:40 起，失败后重试至 20:30；龙虎榜较晚发布，单独重试）；missed 记录；System Health 组件；安装/状态/停用 CLI。
- **AR-2.7（待授权，默认不启用）**：09:25 集合竞价全市场快照、盘中涨停池定时快照。
- 验收：解析器用真实响应样本做测试夹具；真实烟测一次；LaunchAgent 需在 Mac 上安装（记录安装回执）。

### AR-3 研究能力

- **AR-3.1 事件研究引擎 v1**：研究规格（白名单条件表达式、样本范围、入场/出场方式、样本内外切分、分组维度）先冻结登记，再计算；统计按交易日聚类，给出样本外结果、分年与分情绪阶段稳定性；多重检验沿用现有试验登记与 Holm 校正。
- **AR-3.2 打板成交模型 v1**：次日开盘买入（开盘即涨停视为买不到）；涨停价排板（一字板买不到；封死时给“乐观/保守”两种情景；炸板视为成交）；卖出日开盘跌停则顺延；费用含佣金、按日期的印花税、过户费；有 5 分钟线时细化成交时点。
- **AR-3.3 题材引擎 v1**：用板块成分快照 + 涨停池生成每日题材事实（涨停数、连板梯队、龙头候选、持续天数），以机器来源写入 Theme 事实，不写 AI 判断。
- **AR-3.4 前瞻明细并入事件库**：对已有归档的日期补充首封时间、炸板次数、封板资金等字段。

### AR-4 让 AI 用数据思考

- **AR-4.1 AI 只读工具（对话 + MCP）**：情绪指标/周期/相似日、涨停梯队与事件查询、事件研究结果、题材梯队、龙虎榜。
- **AR-4.2 每日收盘复盘 v1**：结构化事实 + 机器状态 +（可选）AI 文字判断，分层保存；接入手机简报。
- **AR-4.3 可验证预测与校准**：开盘前记录概率预测（如“最高板能否晋级”“涨停家数是否增加”），收盘后自动打分（Brier 分数），进入 Agent Scorecard。

### AR-5 自主研究循环

- **AR-5.1 研究计划授权**：比 Session Grant 更长期（不超过 30 天）；周预算、允许的数据集和研究类型、锁定的样本外区间；仍无 shell、联网、写代码、交易权限。
- **AR-5.2 夜间研究任务**：在授权范围内按队列运行预注册事件研究；自动质疑清单（样本量、年份集中度、成交可行性、未来函数检查）。
- **AR-5.3 结论库与失效监控**：记录全部研究（含失败）；跟踪规律的滚动表现，衰减即降级。
- **AR-5.4 盘前简报**：汇总情绪状态、仍有效的规律、题材、预测与风险提示。

### AR-6 预留（本轮不实施）

- Tushare `limit_list_d`（2020 年起，不含 ST，需 5000 积分）、`kpl_list`（开盘啦涨停原因/题材，需 5000 积分）、`stk_auction_o`（开盘集合竞价，需单独开权限）。
- Level-2 逐笔委托/成交与委托队列（排板成交模拟的前提）。
- 公告、互动问答、新闻的文本抽取；股吧/雪球等舆情热度。

## 6. 数据合同（随实施细化）

### 6.1 涨跌幅制度表 v1

| 板块 | 代码前缀 | 生效区间 | 普通股票 | ST / *ST | 新股上市初期 |
|---|---|---|---|---|---|
| 沪深主板 | `sh.600/601/603/605`、`sz.000/001/002/003` | 至 2026-07-03 | 10% | 5% | 2023-04-10 前首日为特殊规则（未建模，排除）；2023-04-10 起前 5 个交易日无涨跌幅 |
| 沪深主板 | 同上 | 2026-07-06 起 | 10% | 10% | 前 5 个交易日无涨跌幅 |
| 创业板 | `sz.300/301/302` | 至 2020-08-21 | 10% | 5% | 首日为特殊规则（未建模，排除） |
| 创业板 | 同上 | 2020-08-24 起 | 20% | 20% | 前 5 个交易日无涨跌幅 |
| 科创板 | `sh.688/689` | 2019-07-22 起 | 20% | 20% | 前 5 个交易日无涨跌幅 |
| 北交所 | `bj.` | 2021-11-15 起 | 30% | 30% | 上市首日无涨跌幅 |

- 价格：前收盘价 ×（1 ± 比例），四舍五入到 0.01 元。前收使用交易所前收（Baostock `preclose`，已含除权除息调整）。
- 未建模并排除：退市整理期（按退市日前 30 个交易日保守标记）、恢复上市/重新上市首日、未知代码前缀。排除行不计入任何涨停统计，并单独计数。
- 规则来源：各交易所公告；沪深主板 ST 调整见 2026-07-06 新规实施报道。规则表带版本号，变更必须新增版本而不是改写旧版本。

### 6.2 涨停状态字段（v1）

`limit_up_price`、`limit_down_price`、`limit_rule_status`（NORMAL / NO_LIMIT / UNMODELED:原因）、`is_limit_up_close`（收盘涨停）、`touched_limit_up`（最高价触及涨停）、`is_broken_board`（触及涨停但收盘未封住，即炸板）、`is_one_word_limit_up`（开高低收均为涨停价）、`is_t_board`（开盘与收盘为涨停价、最低价低于涨停价）、`is_limit_down_close`、`touched_limit_down`、`is_limit_up_to_down`（天地板：触及涨停且收盘跌停）、`is_limit_down_to_up`（地天板：触及跌停且收盘涨停）、`limit_up_streak`（连续收盘涨停天数；停牌日跳过、不中断）、`limit_ups_5d` / `limit_ups_10d`（近 5/10 个交易日收盘涨停次数）、`is_first_board`（今日收盘涨停且前一交易日未收盘涨停）。

### 6.3 日度情绪指标（v1，T 日收盘后可用）

| 指标 | 定义 |
|---|---|
| `up_count` / `down_count` / `flat_count` | 可交易股票相对前收上涨/下跌/持平家数 |
| `limit_up_count` / `limit_down_count` | 收盘涨停/跌停家数（另分 ST 与非 ST） |
| `touched_limit_up_count` / `broken_board_count` | 触及涨停家数 / 炸板家数 |
| `broken_rate` | 炸板家数 ÷ 触及涨停家数 |
| `one_word_limit_up_count` | 一字涨停家数 |
| `first_board_count` / `consecutive_board_count` | 首板家数 / 连板（≥2）家数 |
| `max_streak`、`streak_2/3/4/5plus_count` | 最高连板高度与各高度家数 |
| `advance_rate_1to2`、`advance_rate_2plus` | 昨日首板今日二板的比例；昨日≥2 板今日继续连板的比例（分母为今日可交易者） |
| `prev_limit_up_avg_return` / `prev_limit_up_median_return` | 昨日收盘涨停股今日收盘相对前收收益的均值/中位数 |
| `prev_limit_up_win_rate` | 昨日收盘涨停股今日上涨的比例 |
| `prev_broken_avg_return` | 昨日炸板股今日平均收益 |
| `big_loss_count` | 昨日收盘涨停股今日收益 ≤ -5% 的家数（“大面”） |
| `limit_up_to_down_count` / `limit_down_to_up_count` | 天地板 / 地天板家数 |
| `amount_total`、`amount_change` | 全市场成交额与相对前一交易日变化 |
| `high_board_broken` | 昨日最高连板股今日是否未能继续收盘涨停 |

## 7. 实施流程与验证纪律

1. 在用户电脑的隔离克隆中开发，不直接修改运行中的主工作区。
2. 先跑聚焦测试，再在 Linux 隔离克隆中逐模块跑全仓测试。
3. 在同一提交内更新本文档 §9/§10、追加开发总档案；需要时同步 Agent Memory、README、总计划。
4. 独立提交（沿用仓库作者身份并附协作者尾注），快进合并到主工作区 `main`；不推送。
5. 若主工作区在此期间出现新提交，先变基再重新测试。

## 8. 待决定事项

| 编号 | 事项 | 当前处理 |
|---|---|---|
| D-1 | 是否授权 09:25 集合竞价与盘中定时采集（AR-2.7） | 代码可实现，默认不启用 |
| D-2 | qimo 模拟盘授权配置未计印花税与过户费，是否修改（需重新授权 `control.json`） | 暂不改动，只记录 |

## 9. 实施进度

| 步骤 | 状态 | 提交 | 测试 | 完成时间 |
|---|---|---|---|---|
| AR-0 规划文档与登记 | 已完成 | `177d3dd` | 文档变更，未改源码 | 2026-09-16 18:50 |
| AR-0.1 测试稳定性 | 已完成 | `ca1b449` | Linux 全仓 1026/0/0 | 2026-09-16 18:56 |
| AR-1.1 涨跌幅制度表 | 已完成 | `f18bc72` | 聚焦 34/34；全仓 1040/0/0 | 2026-09-16 19:06 |
| AR-1.2 涨停状态标注器 | 已完成 | `5274057` | 聚焦 8/8；全仓 1048/0/0 | 2026-09-16 19:12 |
| AR-1.3 回溯全市场日线数据集 | 进行中：代码已合并，回溯抓取中 | `04ecd9f`（代码） | 聚焦 10/10；全仓 1058/0/0 | 2026-09-16 22:37 |
| AR-1.4 涨停事件库 | 进行中：代码已合并，待回溯完成后真实构建 | `85542fc`（代码） | 聚焦 26/26；全仓 1064/0/0 | 2026-09-16 23:09 |
| AR-1.5 日度情绪指标 | 进行中：代码已合并，待回溯完成后真实构建 | `2106886`（代码） | 聚焦 9/9；全仓 1068/0/0 | 2026-09-16 23:28 |
| AR-1.6 情绪周期与相似日 | 进行中：代码已合并，待真实数据验证 | `a183c20`（代码） | 聚焦 7/7；全仓 1075/0/0 | 2026-09-16 23:45 |
| AR-2.1 公开证据采集框架 | 已完成 | `737c5c9`（与 AR-2.2 同一提交） | 聚焦 8/8；全仓 1083/0/0 | 2026-09-17 00:07 |
| AR-2.2 涨停/炸板/跌停池 | 已完成 | `737c5c9`（与 AR-2.1 同一提交） | 同上；真实归档 2026-09-16 五个股池 | 2026-09-17 00:07 |
| AR-2.3 龙虎榜 | 已完成 | `2158bdc` | 聚焦 11/11；全仓 1086/0/0；真实归档 2026-09-16 | 2026-09-17 00:20 |
| AR-2.4 板块与成分 | 已完成（成分全量抓取待 AR-2.6 调度在 Mac 上执行） | `acdb771`（与 AR-2.5 同一提交） | 聚焦 15/15；全仓 1090/0/0；真实归档 2026-09-16 板块列表 | 2026-09-17 00:34 |
| AR-2.5 人气榜 | 已完成 | `acdb771`（与 AR-2.4 同一提交） | 同上；真实归档 2026-09-16 | 2026-09-17 00:34 |
| AR-2.6 收盘后调度与健康 | 代码已完成；LaunchAgent 待用户在 Mac 上安装 | 本提交（下一步回填） | 聚焦 21/21；全仓 1094/0/0 | 2026-09-17 00:44 |
| AR-2.7 竞价/盘中采集 | 待授权 | | | |
| AR-3.1 事件研究引擎 | 未开始 | | | |
| AR-3.2 打板成交模型 | 未开始 | | | |
| AR-3.3 题材引擎 | 未开始 | | | |
| AR-3.4 前瞻明细并入事件库 | 未开始 | | | |
| AR-4.1 AI 只读工具 | 未开始 | | | |
| AR-4.2 每日收盘复盘 | 未开始 | | | |
| AR-4.3 可验证预测与校准 | 未开始 | | | |
| AR-5.1 研究计划授权 | 未开始 | | | |
| AR-5.2 夜间研究任务 | 未开始 | | | |
| AR-5.3 结论库与失效监控 | 未开始 | | | |
| AR-5.4 盘前简报 | 未开始 | | | |

## 10. 验收记录

### AR-0 规划文档与登记（2026-09-16 18:50）

- 内容：新建本文档；在 README、总计划、总体架构中登记 AR 主线入口；追加开发总档案条目。
- 依据：逐项核查了 `prep_scanner`、`playbook_reconstruction`、`daily_market_archive`、`theme_state`、`restricted_dsl`、`research_session_grant`、`ExecutionConfig` 等代码，以及 Baostock 当前股票代码前缀分布（`sz.302`、`sh.689` 均已出现）。
- 测试：纯文档变更，未修改源码；规划基线全仓结果见文首。

### AR-0.1 测试稳定性（2026-09-16 18:56）

- 问题：Linux 隔离克隆中 `test_offline_environment` 两个用例被“仅支持 macOS arm64 / Python 3.13.5”的平台前置检查拦截，未测到校验和与目标目录保护；`test_system_health_desktop` 固定等待 250ms，而健康数据异步聚合在较慢机器上首次约 0.8 秒，导致偶发失败。
- 修改：离线环境测试在用例内把平台身份固定为受支持平台（只替换该模块引用的 `platform`），使校验和、目标目录保护在任何开发机都走同一路径，并新增“不受支持平台在任何写入前被拒绝”的反向用例；System Health 桌面测试改为最多 10 秒轮询等待页面渲染出 Runtime 卡片。
- 未改源码行为；只改测试。
- 测试：两个模块聚焦 5/5 通过；Linux arm64 隔离克隆逐模块全仓 **1026 个测试，0 failed / 0 errors / 0 skipped**（较基线新增 1 个反向用例）。
- 已知现象：17 个桌面测试模块单独进程运行时，测试全部 OK 后在 Qt 退出清理阶段以非零码结束；合并运行桌面测试时正常退出。不影响结果，暂不处理。

### AR-1.1 A 股涨跌幅制度表 v1（2026-09-16 19:06）

- 新增 `src/quantlab/trading/price_limit_regime.py`（版本 `a-share-price-limit-regime-v1`）：`board_of` 识别主板/创业板（含 `sz.302`）/科创板（含 `sh.689`）/北交所/未知；`limit_rule` 按交易日返回 NORMAL（带比例）、NO_LIMIT 或 UNMODELED 及原因码；`limit_prices` 按前收×(1±比例) 四舍五入到分；`CHANGES` 列出 5 个制度变更日。
- 覆盖 §6.1 全部规则：主板 ST 2026-07-06 起 10%；创业板 2020-08-24 前 10%/ST 5%、之后 20%；科创板 2019-07-22 起 20%；北交所 2021-11-15 起 30%；注册制新股前 5 个交易日（北交所首日）无涨跌幅；主板/创业板旧制度新股首日、临近退市（30 个交易日）、未知板块、开板前日期均为 UNMODELED。旧制度上市的创业板股票在改革后不获得新股无涨跌幅窗口。只给上市日期不给交易日序号时，距上市 20 个自然日以内一律 UNMODELED，保守排除。
- 接入：`prep_scanner` 在未提供 MarketRules 时的回退改用 `limit_rule(symbol, session, is_st)`，非 NORMAL 的交易日不推断涨跌停价；`mark_limit_closes` 默认比例改为按日期推算，显式 `limit_rate` 与 `special_rates` 优先级不变；`GROWTH_PREFIXES` 补 `sz.302`/`sh.689`。正式 MarketRules 路径、扫描输出字段、`source_hash` 组成均未改变。
- 已知限制：PREP 回退与 `mark_limit_closes` 不掌握上市日期，新股无涨跌幅窗口未解析（`listing_window_checked=False`）；事件库（AR-1.4）将用 stock_basic 与交易日历精确解析。
- 测试调整：`test_verified_security_status_fills_status_gap_without_certifying_inferred_limits` 原用 2026-09-09 ST 按 5% 涨停（11.55），与 2026-07-06 起主板 ST 10% 的现行规则不符，改为 12.10 并注释原因。
- 新增测试：`tests/test_price_limit_regime.py` 12 项（板块前缀、各制度边界、ST、新股窗口、旧制度首日、上市窗口未解析、临近退市、未知板块、舍入、非法参数）；PREP 新增“ST 2026-07-02 按 5%、2026-09-09 按 10%、`sz.302` 按 20%”用例；重建模块新增创业板改革日与未知板块用例。
- 验证：聚焦（制度表、PREP、重建、编排器、qimo、快照扫描、Playbook forward/paper plan）全部通过；Linux 隔离克隆逐模块全仓 **1040 个测试，0 failed / 0 errors / 0 skipped**。
- 运行影响：合并后 qimo 模拟盘下一次 tick 即使用新回退规则；它的 PREP 证据要求正式 MarketRules，正常路径不走回退。

### AR-1.2 涨停状态标注器 v1（2026-09-16 19:12）

- 新增 `src/quantlab/trading/limit_states.py`（`limit-state-v1`）：`annotate_limit_states(panel)` 输入逐证券逐交易日面板（`date/code/open/high/low/close/preclose/tradable/is_st`，可选 `listing_date/sessions_since_listing/sessions_to_delisting`），输出 §6.2 字段，另加 `board`、`limit_rule_status/reason`、`limit_rate`、`listing_window_checked`、`limit_price_violation`（最高/最低价越过推算涨跌停价，提示数据或制度不一致）以及 `prev_is_limit_up_close/prev_limit_up_streak/prev_is_broken_board`。
- 计算口径：制度判断由 `regime_columns` 向量化实现，与 AR-1.1 标量 `limit_rule` 在 9,120 个边界组合上逐项一致（单测）；价格一律换算为整数“分”比较，涨跌停价按 前收分×(10000±比例基点)+5000 整除 10000，等价于四舍五入到分，避免浮点误差；连板、近 5/10 日涨停次数、前一交易日状态只在可交易交易日上计算，停牌日既不增加也不中断连板；非 NORMAL 规则日与停牌日不推断任何涨跌停状态。
- 失败即拒绝：缺字段、类型不符、同证券同日重复、代码非法、可交易行价格缺失或非正、OHLC 关系无效、上市/退市序号非法均抛错。
- 性能：Linux arm64 VM 上 125 万行（5,000 只 × 250 日）约 1.2 秒。
- 测试：新增 `tests/test_limit_states.py` 8 项（向量化与标量制度一致性、收盘涨停/触板/炸板/一字/T 字、跌停/天地板/地天板、分位舍入、ST 与创业板改革前后、越界标记、新股无涨跌幅与未知板块与停牌行、跨停牌连板与滚动计数、多证券独立、非法输入）。Linux 隔离克隆逐模块全仓 **1048 个测试，0 failed / 0 errors / 0 skipped**。
- 本步未接入任何现有流程；由 AR-1.4 事件库使用。

### AR-1.3 回溯全市场日线数据集 v1：代码与真实烟测（2026-09-16 22:37）

- 新增 `src/quantlab/data/retro_daily.py`（`retro-daily-baostock-v1`）：
  - `create_plan(start, end)`：一次登录抓取 stock_basic 与交易日历，校验字段、日历完整覆盖、代码唯一；筛选 type=1 的沪深证券中上市不晚于 end 且未在 start 前退市的证券（保留已退市证券，避免幸存者偏差）；capture_id 由区间与两份参考内容哈希确定，重复创建返回同一计划。
  - `fetch(capture_id, shard, shards, max_seconds)`：只抓本分片未完成证券；每只证券校验字段合同、计划区间、交易日、日期严格递增、代码一致、不复权、状态取值、可交易行价格为正与 OHLC 关系；成功则原子写入目录，失败写失败记录（连续 5 次失败停止本次抓取）；启动时清理本分片中断遗留的临时文件。
  - `status`（可选逐文件哈希复核）、`read_panel`（校验 parquet 哈希后合并面板，默认要求抓取完整）、`reference`、`trading_days`、`quarantine_corrupt`（须显式确认）。
- 新增 CLI `quantlab.agent.retro_daily_cli`（list/plan/fetch/status/verify/quarantine），pyproject 登记 `niuniu-retro-daily`。
- 测试：新增 `tests/test_retro_daily.py` 10 项（计划冻结与参考文件篡改、供应商/日历/代码重复/空结果失败、断点续传与状态统计、分片划分、非交易日与供应商错误写失败记录后重试成功、连续失败与截止时间、面板哈希校验与隔离后重抓、行合同违规、临时文件按分片清理、CLI JSON 输出）。Linux 隔离克隆逐模块全仓 **1058 个测试，0 failed / 0 errors / 0 skipped**。
- 真实烟测（在隔离目录，不写入项目）：计划 2019-01-01～2026-09-15 得到 **5,455 只证券、1,870 个交易日**；抓取 135 只共 239,159 行，0 失败。用 AR-1.1/1.2 标注后：收盘涨停 3,998 次、炸板 2,095 次、一字板 397 次，最高 25 连板；ST 在 2026-07-06 前按 5%、之后按 10% 识别的涨停均与价格吻合；最高/最低价越过推算涨跌停价的只有 5 行，全部是股价不足 1 元的退市整理期证券（AR-1.4 将按退市日期标为 UNMODELED）；收盘价比涨停价低 1 分的 5 行正确地未被判为涨停。
- 回溯运行方式：`--call plan --start 2019-01-01 --end 2026-09-15` 后，对 `--shard 0/4` 至 `3/4` 分别运行 `--call fetch`，重复直到 `status` 显示 `complete: true`；每次调用的外部超时应比 `--max-seconds` 多留 60 秒以上。
- 状态：代码已合并；正式回溯写入 `artifacts/_market_data/retro_daily/`，完成后在本节补记实际覆盖并把 AR-1.3 标为已完成。

### AR-1.4 涨停事件库 v1：代码（2026-09-16 23:09）

- 新增 `src/quantlab/trading/limit_events.py`（格式 `limit-event-library-v1`，构建器 `limit-event-builder-v1`）与 CLI `quantlab.agent.limit_events_cli`（list/build/get/verify/summary，pyproject 登记 `niuniu-limit-events`）。
- 输入：一个或多个**完整且日期首尾相接**的回溯日线 capture（不完整或有缺口即拒绝）；上市/退市日期取最新 capture 的 stock_basic，交易日历为各 capture 日历拼接。
- 计算：
  - 按交易日历精确算出 `sessions_since_listing`（上市日为 1）与 `sessions_to_delisting`，再调用 AR-1.2 标注器；新股无涨跌幅期、临近退市 30 个交易日、未建模制度与价格越界行不进入事件。
  - 事件触发：当日触及涨停或跌停，或前一可交易日收盘涨停或炸板；前一日价格越界时不作为触发（该日状态不可靠）。“前一日炸板”是对规划的补充。
  - 特征（T 日收盘已知）：板块、比例、ST、上市序号、开高低收、量额、换手、涨跌停价、日收益、开盘缺口、振幅、按可交易日复利的 5/20 日收益、当日成交额分位、全部涨停形态与连板字段。
  - 标签（严禁作特征）：T+1 开/高/低/收相对前收收益、开盘即涨停/跌停、T+1 涨停/触板/炸板/跌停与连板数、T+2 开收日收益，以及收盘→T+2 收盘、T+1 开盘→T+1 收盘/T+2 开盘/T+2 收盘复利收益。均以交易所前收为基准（含除权调整），不计费用与成交可行性；次日停牌则相应标签为空。
- 可复现：build_id 由输入 capture（区间、面板 manifest 摘要、参考内容哈希）、构建器/制度/标注器版本和三个源文件的 SHA256 确定；重复构建返回同一结果；`verify` 从输入重算并逐值比较，源码变化时报告需要重建。
- 同步修正制度表：只知道上市日期、不知道交易日序号时，如果该板块在上市日之后没有多日无涨跌幅窗口（注册制前主板/创业板、北交所），上市次日起不再标为“窗口未解析”。标量规则与向量化实现同步修改，一致性网格测试仍全部通过。
- 测试：新增 `tests/test_limit_events.py` 5 项（40 个交易日夹具覆盖首板、一字、炸板、炸板次日、创业板 20% 与次日停牌、科创板新股第 6 个交易日、跌停、临近退市排除、越界排除且不触发次日事件、标签数值、manifest 统计、幂等与重算校验、篡改检测、两段首尾相接 capture 与单段结果完全一致、不连续与不完整输入拒绝、CLI）；制度表新增 1 项。Linux 隔离克隆逐模块全仓 **1064 个测试，0 failed / 0 errors / 0 skipped**。
- 状态：代码已合并；正式构建须等 AR-1.3 回溯抓取完成（本次提交时已完成 981/5,455 只）。

### AR-1.5 日度情绪指标 v1：代码（2026-09-16 23:28）

- 新增 `src/quantlab/trading/market_sentiment.py`（格式 `market-sentiment-daily-v1`）与 CLI `quantlab.agent.market_sentiment_cli`（list/build/get/verify/read，read 单次最多 500 个交易日；pyproject 登记 `niuniu-market-sentiment`）。
- 输入与事件库相同（完整、首尾相接的回溯 capture），先经共享的 `prepare_states` 标注，再按交易日汇总；输出 `artifacts/_limit_research/market_sentiment/<build_id>/daily.parquet` 与 checksum manifest，manifest 内含全部 40 个指标的中文定义和“T 日收盘后可用、仅用于 T+1 及以后”的使用约束。
- 口径：
  - 涨跌停类统计只计可交易、制度 NORMAL 且价格未越界的行；未建模与越界行单独计数（`unmodeled_count`/`violation_count`）作为数据质量指标。
  - 涨跌家数按“分”比较收盘价与交易所前收。
  - “前一交易日”严格取全市场日历前一日，个股前一日停牌不进入昨日类统计。
  - `high_board_broken`：前一日最高连板（≥2）的股票今日均未收盘涨停为真，没有高度板或均停牌为空。
- 实现上把事件库的标注与收益计算提取为共享函数 `prepare_states`；事件库行为不变（因源码指纹变化，今后构建会得到新的 build_id）。测试夹具提取到 `tests/limit_research_fixtures.py`。
- 测试：新增 `tests/test_market_sentiment.py` 4 项。手工构造 4 个交易日、6 只股票：首板/二板/炸板/一字/跌停、创业板次日停牌、2026-07-06 前 ST 按 5%、大面、高度板断板。逐项核对晋级率、昨日涨停收益均值与胜率、炸板率、成交额变化；另含库构建、幂等、重算校验、CLI 读取、篡改检测。Linux 隔离克隆逐模块全仓 **1068 个测试，0 failed / 0 errors / 0 skipped**。
- 状态：代码已合并；真实构建与“量级核对”待 AR-1.3 回溯完成（本次提交时已完成 1,579/5,455 只）。

### AR-1.6 情绪周期机器状态 v1 与相似日：代码（2026-09-16 23:45）

- 新增 `src/quantlab/trading/sentiment_cycle.py`（规则版本 `sentiment-cycle-rules-v1`，来源标记 `host_engineering_policy_not_expert_rule`）与 CLI `quantlab.agent.sentiment_cycle_cli`（rules/cycle/similar，cycle 单次最多 500 个交易日；pyproject 登记 `niuniu-sentiment-cycle`）。
- 温度：对 8 个日度指标分别计算当日值在**此前** 250 个交易日（不含当日）中的分位数，至少 60 个历史日才计算。正向指标取分位数，反向指标取 1 减分位数，平均后乘 100；少于 5 个指标可用时温度为空。
  - 正向指标：非 ST 涨停数、最高连板、1进2 晋级率、昨日涨停今日收益、上涨比例。
  - 反向指标：非 ST 跌停数、炸板率、大面数。
- 阶段规则（按顺序）：温度为空 → UNKNOWN；≤20 冰点；≥80 高潮；3 日温度变化 ≥+10 时温度 <50 为修复、否则发酵；≤-10 时温度 ≥50 为分歧、否则退潮；其余按温度是否 ≥50 分为偏暖/偏冷震荡。阈值是工程设定，不是专家规则或交易信号，调整必须新增规则版本。
- 相似日：以 8 个分位数为特征，用均方根距离找目标日之前、并排除最近 20 个交易日的最相近 k 天，同时给出这些日子**下一个交易日**的涨停数、最高连板、昨日涨停收益、1进2 晋级率、炸板率。只用目标日之前已知的数据。
- 周期由已校验的情绪指标 build 即时计算，不另存文件；结果可由规则版本和 build_id 复现。
- 测试：新增 `tests/test_sentiment_cycle.py` 7 项（阶段规则顺序、历史不足为 UNKNOWN、温度与分位数边界、截断未来数据前后逐行一致、分位数不含当日且只用窗口、非法输入、相似日严格历史且按距离排序并与目标水平相近、CLI）。Linux 隔离克隆逐模块全仓 **1075 个测试，0 failed / 0 errors / 0 skipped**。
- 状态：代码已合并；真实周期分布与相似日核对待 AR-1.3 回溯完成（本次提交时已完成 2,077/5,455 只）。

### AR-2.1 公开证据采集框架 + AR-2.2 东方财富股池（2026-09-17 00:07）

- 两步耦合紧密（框架需要至少一个真实来源验证），合并为同一提交。
- 新增 `src/quantlab/data/public_evidence.py`（`public-evidence-capture-v1`）：
  - HTTP：只允许 `push2ex/push2/datacenter-web/emappdata.eastmoney.com` 的 HTTPS，重定向不得离开白名单，单响应 ≤10MB，超时 15 秒，请求间隔 ≥0.5 秒，单次抓取 ≤800 个请求。
  - 时点：抓取时间在交易日 15:00 前或早于交易日 → 拒绝（`NOT_AFTER_CLOSE`）；当日收盘后 → `SAME_DAY_AFTER_CLOSE`；下一工作日 09:15 前 → `BEFORE_NEXT_SESSION`；更晚 → `LATE`，只有显式 `allow_late` 才抓，资格标为 `research_only_late_capture`。周末直接拒绝；若提供交易日历则记录 `calendar_verified`。
  - 存储：`artifacts/_market_data/public_evidence/<source>/<交易日>/<capture_id>/`，含原始响应字节、请求描述、规范化 parquet 与 checksum manifest（每个响应的 SHA256、抓取时间、行数、警告、限制说明）；首个 capture 自动 accepted；内容相同不重复写；内容变化进入 `revision_review`，须显式确认才切换。任何请求失败、结构变化或解析错误都不写入。
- 新增 `src/quantlab/data/eastmoney_sources.py`（解析器 `em-pool-v1`）：涨停股池、昨日涨停股池、炸板股池、跌停股池、强势股池。
  - 字段：首次/最后封板时间（HHMMSS 转 `HH:MM:SS`）、封板资金、炸板/开板次数、连板/连续跌停数、板上成交额、涨停统计（N 天 M 板）、行业等。
  - 校验：`tc` 必须等于返回条数（可发现排序参数错误导致的空股池）；缺字段、类型错误、时间越界、证券重复均拒绝；多出的新字段只记警告；BSE 代码按 4/8/92 开头识别为 `bj.`；行按代码排序保证内容哈希稳定。
  - 强势股池中 `ztf/nh/cc` 语义未公开，保留原值并以 `em_` 前缀命名。
- 新增 CLI `quantlab.agent.public_evidence_cli`（sources/capture/capture-all/list/get/read/accept-revision/overview），pyproject 登记 `niuniu-public-evidence`。
- 实测：股池接口只返回近约一个月数据（2026-08-03 已为空），印证必须前瞻归档。
- 首次真实归档（2026-09-17 00:00 前后，时点 `BEFORE_NEXT_SESSION`）：2026-09-16 涨停 89、昨日涨停 32、炸板 11、跌停 4、强势 107，全部解析无警告，写入 `artifacts/_market_data/public_evidence/`。
- 测试：新增 `tests/test_public_evidence.py` 8 项（时点分类含周末与周一 09:15 边界、BSE 代码映射与主机白名单、首个 capture 写入原始字节并被接受、重复抓取不重复写、修订需确认、盘中/过期/周末拒绝与显式补抓、tc 不一致/时间越界/错误码/非 JSON/缺字段/重复/网络异常均不写入、新增字段警告、篡改检测、其余四个股池解析、CLI）。Linux 隔离克隆逐模块全仓 **1083 个测试，0 failed / 0 errors / 0 skipped**。

### AR-2.3 龙虎榜（2026-09-17 00:20）

- 在 `eastmoney_sources.py` 新增通用数据中心来源 `EastmoneyDatacenterSource`（解析器 `em-datacenter-v1`）和三个来源：`em_billboard_daily`（每日上榜明细）、`em_billboard_buy_seats`（买入营业部席位）、`em_billboard_sell_seats`（卖出营业部席位）。
- 请求按交易日精确过滤，每页 500 条并自动翻页（最多 40 页）；校验每页页数一致、页数取全、总行数等于 `count`、每行交易日等于请求日，缺字段即拒绝。
- 数据尚未发布（接口返回 9201“数据为空”）时报 `NOT_READY` 且不写入，由调度稍后重试，避免把“未发布”存成空归档。
- 防未来函数：供应商的 `D1/D2/D5/D10/D20/D30_CLOSE_ADJCHRATE` 未来收益列一律丢弃；`EXPLAIN` 解读与席位 3 日上涨概率等以 `em_` 前缀保留，limitations 注明只是供应商统计。
- `is_a_share` 标出榜单中的可转债等非 A 股行；已知但暂不使用的字段列入忽略名单，只有真正的新字段才产生警告。
- 真实归档 2026-09-16（时点 `BEFORE_NEXT_SESSION`）：上榜明细 73 行、买入席位 365 行、卖出席位 365 行，无警告；抽查原始响应，全部行交易日为 2026-09-16。
- 测试：新增 `tests/test_eastmoney_billboard.py` 3 项（翻页与未来列丢弃、非 A 股标记、未发布/行数不符/交易日不符/缺字段均不写入、新字段警告、席位表与代码解析）；公开证据 CLI 来源数更新为 8。Linux 隔离克隆逐模块全仓 **1086 个测试，0 failed / 0 errors / 0 skipped**。

### AR-2.4 板块与成分 + AR-2.5 人气榜（2026-09-17 00:34）

- 两步同属“只能抓当前快照”的来源，合并为同一提交。
- 框架增强：`Source.snapshot_only` 标记快照类来源，过了下一交易日 09:15（`LATE`）即使显式允许也拒绝，因为无法代表该交易日；`Source.max_requests` 允许单来源放宽请求上限（成分来源 3,000）。
- 新增来源（解析器 `em-board-list-v1`、`em-board-members-v1`、`em-popularity-v1`）：
  - `em_concept_boards` / `em_industry_boards`：板块代码、名称、指数点位、涨跌幅、成交量额、换手、涨跌家数、领涨股及涨幅；每页 100 条自动翻页；总数与分页结果不一致即拒绝；“-”等停牌值记为空。
  - `em_concept_board_members` / `em_industry_board_members`：先取全部板块列表，再逐板块取成分并对大板块翻页；逐板块核对成分数量等于接口总数，成分重复或与板块列表不一致即拒绝。支持 `board_limit` 仅用于烟测，结果带 `BOARD_LIMIT` 警告。
  - `em_popularity_rank`：人气榜前 100 名（接口第 2 页无数据），核对排名连续、证券不重复。
- 主机：实测 `push2.eastmoney.com` 当前对本机出口拒绝连接（curl 与 urllib 均断开），延时行情主机 `push2delay.eastmoney.com` 正常。收盘后两者数值一致，故板块来源改用 push2delay 并加入白名单，limitations 注明不可用于盘中实时。另提醒：现有三源实时行情中的东财来源同样使用 push2，若持续被拒将只剩腾讯/新浪两源。
- 真实归档 2026-09-16（时点 `BEFORE_NEXT_SESSION`）：概念板块 504、行业板块 496、人气榜 100。烟测（隔离目录）：概念成分前 25 个板块共 3,763 行、57 个请求、40 秒。全量约 1,000 个板块、1,100 多个请求、十余分钟，超出本机虚拟机单次命令时限，全量成分抓取交给 AR-2.6 在 Mac 上执行。
- 测试：新增 `tests/test_eastmoney_snapshots.py` 4 项（板块翻页与总数校验、停牌值为空、领涨股代码；成分分阶段请求与大板块翻页、成分数量不符拒绝、board_limit 警告；人气榜解析；快照来源 LATE 拒绝）；来源总数更新为 13。Linux 隔离克隆逐模块全仓 **1090 个测试，0 failed / 0 errors / 0 skipped**。

### AR-2.6 收盘后调度与 System Health（2026-09-17 00:44）

- 新增 `src/quantlab/agent/evidence_scheduler.py`（调度版本 `evidence-schedule-v1`）与 CLI `quantlab.agent.evidence_scheduler_cli`（enable/pause/tick/status/install-agent；pyproject 登记 `niuniu-evidence-scheduler`）。
- 授权与开关：必须 `--enable --confirm` 并写明授权依据，控制文件带 checksum；`--pause` 暂停；未启用时 tick 什么也不做。
- tick 逻辑：
  - 只处理仍在“当日收盘后或下一交易日 09:15 前”窗口内的最近交易日。每天先用 Baostock 交易日历确认一次并缓存，节假日跳过；日历查不到时本轮不抓。
  - 各任务开始时间：五个股池、人气榜 15:40；板块列表 15:45；龙虎榜三个来源 17:30；Baostock 当日全市场日线（DailyMarket）18:00。
  - 概念/行业成分每周一次：周五执行，或距上次成分快照已满 7 天时执行。
  - 已接受的来源不重复抓；失败冷却 15 分钟后重试，每个来源每天最多 8 次，之后标记 GAVE_UP。
  - 用文件锁防止并发；状态文件保留最近 30 个交易日；只有实际执行了动作才输出日志，避免日志无限增长。
- macOS 安装：`--install-agent` 写入 `~/Library/LaunchAgents/com.niuniu.evidence-archive.plist`（每 600 秒一次、登录即运行，日志在 `artifacts/_market_data/public_evidence/_scheduler/`），并用 launchctl 加载。只能在 Mac 上执行，本会话的 Linux 虚拟机无法安装。
- System Health：新增 `public_evidence` 组件，未启用为 NOT_CONFIGURED；暂停、超过 24 小时无 tick、有来源 GAVE_UP 时为 WARN。不计入运行/研究两条总轴，桌面系统中心同步显示“公开证据归档”。
- 测试：新增 `tests/test_evidence_scheduler.py` 4 项，覆盖：
  - 未启用与确认/授权要求。
  - 15:40 前不抓；15:45 抓股池/人气/板块；龙虎榜未发布时失败冷却，15 分钟后重试成功。
  - 次日 08:00 补做 DailyMarket，09:20 后不再处理前一日。
  - 节假日跳过；周五抓成分、下周一跳过；8 次失败后 GAVE_UP 并在 System Health 报警。
  - 并发锁与 CLI。

  Linux 隔离克隆逐模块全仓 **1094 个测试，0 failed / 0 errors / 0 skipped**。
- 状态：代码已合并；启用与 LaunchAgent 安装见下一条记录。
