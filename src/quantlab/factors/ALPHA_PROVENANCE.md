# Alpha formula provenance

Frozen feature expressions extracted from the installed vnpy==4.4.0 Alpha101 and Alpha158 dataset constructors. The adjacent ALPHA_LICENSE contains the upstream MIT notice. Upstream negative-shift research labels are deliberately not extracted. The computation adapter accepts only these packaged formulas, with no user expression evaluation.

Alpha101Pack has 82 enabled upstream formulas, **not 101**. Missing upstream entries: 48, 56, 58, 59, 63, 67, 69, 70, 76, 79, 80, 82, 87, 89, 90, 91, 93, 97, 100. They must not be replaced with invented industry classifications or other formulas. Alpha158Pack has 158 features. These are upstream rule implementations, not independently asserted paper-exact replications.

Price fields retain the requested adjustment basis. VWAP = turnover / shares × adj_factor; a missing adjustment factor is an error. Zero-volume VWAP and non-finite results are null. Cross-sectional operations use all supplied symbols at the same close, before research universe filtering. For PIT universe cross-sectional research, supply the correct historical cross section; a later mask does not change upstream rankings. Upstream warmup behavior is retained; no fitted preprocessing or ML model is invoked.
