# 威克夫 A–E：可复现 OHLCV 规则链 v1

本模块对应总体方案 31.5 的 TradingRange、Spring、Upthrust、Test、SOS/SOW、LPS/LPSY、EffortVsResult、Accumulation/Distribution，并接入状态、结构、事件、序列、规则评分和独立成交回测。原有 `WYCKOFF.*`、`WYCKOFF.PHASE_*` 保持原行为；新因子前缀为 `WYCKOFF.CLASSIC_`，版本 1.0.0，规则配置 `wyckoff_ohlcv_ae_v1`。

理论顺序参考 [Wyckoff Analytics 官方方法说明](https://www.wyckoffanalytics.com/wyckoff-method/)：吸筹／派发 A–E、量价关系、Spring／UTAD 可选。以下数值阈值及状态机是本平台明确规定的工程化解释，来源没有给出这些统一数值。它覆盖这里定义的完整可计算链路，不声称穷尽主观判图、机构意图或所有威克夫流派；日线没有逐笔订单流证据。

## 因子目录（34 项）

| 层级 | 组件（均加 `WYCKOFF.CLASSIC_`） | 输出 |
|---|---|---|
| 市场／周期 | MARKET_DIRECTION、CYCLE | 滞后趋势 -1/0/+1；初始周期 ±1、延续周期 ±2 |
| 结构 | PHASE、DIRECTION、RANGE_LOW、RANGE_HIGH | A–E 编码 ±1…±5、吸筹／派发方向及区间上下沿；无链路为 0 |
| A 阶段 | PS、PSY、SC、BC、AR_UP、AR_DOWN、ST_UP、ST_DOWN | 初步支撑／供应、高潮、自动反弹／回落、二次测试 |
| C 阶段 | SPRING、UTAD、TEST_UP、TEST_DOWN | 扫出及其测试，或无扫出的缩量测试 |
| D 阶段 | SOS、SOW、LPS、LPSY | 强弱突破、最后支撑／供应 |
| E 与终止 | MARKUP、MARKDOWN、EXIT、INVALIDATED、EXPIRED、TARGET_HIT | 趋势确认、退出、失效、超时、目标耗尽 |
| 量价／测量 | SUPPLY_DEMAND、EFFORT_RESULT、CAUSE_COLUMNS、PRICE_TARGET | 供需、努力结果代理、固定格点列数与目标 |
| 模型／策略 | SCORE、POSITION | 有符号评分 ±0…100；满足向上 E、评分≥80 的持仓 1，否则 0 |

一般区间 Upthrust 仍由既有 `WYCKOFF.UPTHRUST` 表达；新链路内的成熟派发扫出用 UTAD 表达。普通区间波动不会被直接命名为最终派发确认。

## 固定规则

所有输入仅取当前已收盘 K 线和更早数据。每根最多推进一个阶段；PS 可选，若初次触发已满足高潮直接记 SC／BC，不补造此前 PS。以下写吸筹方向；派发使用 `(high,low,close)→(-low,-high,-close)` 的相同规则。

- 预热 `context=60`；趋势及均量／均振幅／ATR 使用前 `lookback=20` 根，不含当根。前 20 根首末收盘涨跌超过 `trend_threshold=5%` 才有方向。下跌背景下，当根量≥前均量×1.2 开始 PS。
- SC：创新低，量≥前均量×1.8，振幅≥前均振幅×1.3，收盘处于当根振幅下沿 35% 以上。SC 后下沿被再破且尚未 AR，链路失效。PS 后再遇 SC 会更新高潮边界。
- AR：后续收盘离 SC 低点≥SC 时滞后 ATR×2，冻结 AR 当根最高价为区间上沿。此后不会用未来高点重画区间。
- ST：后续低点距区间下沿≤区间宽度×25%，收回下沿，量<SC 量×80%；进入 B。超出下沿容忍幅度则失败。
- B：至少 5 根后才能进入 C。低点刺破下沿、收盘回到区间，确认 Spring（派发为 UTAD）。过早扫出、同根穿越上下两沿或未测试便收盘离开区间，均失效。未扫出的路径是成熟 B 内靠近下沿、量<SC×80% 的测试，直接进入 TEST。
- C 后必须另一根缩量测试：靠近原下沿、收回区间，量<扫出量×80%。TEST 后另一根收盘突破区间上沿，量≥前均量×1.2，确认 SOS（派发为 SOW），进入 D。
- D 后另一根低点靠近上沿且量<SOS量×80%，收盘保持上沿，确认 LPS／LPSY；再后一根收盘超过 SOS 当根高点，量>LPS量，进入 E。C／TEST／D／LPS 破坏已知极值失效；D／LPS 收盘跌回上沿下方失效。
- E 入场确认时若已达到测量目标，直接记 TARGET_HIT 并不产生新持仓。E 中以收盘确认目标、跟踪止损或 120 根持有超时；止损为前止损与“已知最高收盘－当前滞后 ATR×3”的较高值，初始止损为区间上沿。收盘触发退出后交给下一根开盘撮合，绝不假设成交在止损价。
- A–D 等待超过从 PS／直接 SC 开始的 180 根失效超时。终止以后不会在同根重新开始新链路。派发 E 保存负评分与事件，A 股多头研究持仓为 0，不模拟裸卖空。

默认参数可在单因子参数编辑器查看／修改；模板使用冻结默认值，改参数时须使用显式组合配置。每个序列保存 episode ID、累计 event IDs、available_at、阶段、边界、原因和当时量价输入，结束状态区分 completed／invalidated／timeout，样本末尾未结束为 pending。

## 三定律的可计算代理与评分

`SUPPLY_DEMAND = (2×(close-low)/(high-low)-1)×当根量/前均量`；零振幅输出 0。

`EFFORT_RESULT = (close-前收盘)/前ATR/(当根量/前均量)`；零量输出 0。它是量价诊断量，不等于主动买卖订单量。

原因／结果测量使用**仅收盘的固定格点代理**：PS／直接 SC 时冻结格长 `close×1%`，从该收盘开始，每反向移动至少 3 格增加一列；只走完整格，不推断盘中高低先后。到 D 时冻结列数，方向目标为 `区间突破边界 + 方向×列数×格长×3`。这不是完整手工 P&F 水平计数；派发理论投影可能为负值，不声称可达，也不据此卖空。

评分基础分为 A/B/C/D/E：10/25/40/60/80，量价供需方向一致加 10，滞后市场方向中性或一致加 10，再乘链路方向。`POSITION=1` 仅在向上 E 且分数≥80；本默认值允许所有有效向上 E，评分仍保留支持程度。`RESEARCH.WYCKOFF_CLASSIC_POSITION` 显式组合 POSITION=1 与 SCORE≥80，未训练、未寻优。

## 持久化、客户端与验证

研究归档 `observations.parquet` 保存全部 34 个 `wyckoff_*` 列，另含市场状态；组合模板也保留。客户端“观测数据”可读完整列，“序列审计”可逐事件查看，“K 线回放”按游标截止信息重建当时区间和阶段；已结束区域不延伸到后续日期。因子家族共享同一份最近输入的计算矩阵，避免每个组件各留一份百万行矩阵。

研究价格默认前复权，数据源只读；成交引擎使用相同冻结 qfq 行情，下一根开盘、整手、T+1、佣金和滑点，不重复处理分红送股。500 股试验固定 100 万初始资金、最多 20 股、单股目标≤5%、总体≤100%、佣金 3bp 最低 5 元、滑点 2bp、卖出税费假设 5bp。税费为固定研究假设，不宣称精确历史税制；未加载历史可交易／停牌／涨跌停规则，不作为真实可成交收益保证。

十年完整覆盖样本是事后筛选，存在生存与完整覆盖偏差；使用当前固定 qfq，不是历史时点发布的复权版本。前 60 根为预热、无信号。收益不用于修改本次默认规则。合成测试用于验证双向阶段、分支、结束、逐前缀一致、跨证券隔离、回放截止、组合归档和次日成交；最终业务验收使用真实数据并由客户端提交。
