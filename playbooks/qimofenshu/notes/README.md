# Notes

这里只保存公开资料的合法摘要、人工标注和研究问题，不把复制的原帖正文当作 Git 规则源。

每条笔记建议记录：

- 对应 `ExpertSource.source_id`（正式登记后）；
- 来源定位和内容 SHA256；
- 发布时间 / `available_at`；
- 哪些句子支持某个规则假设；
- 哪些内容仍然模糊或存在相反案例；
- 是否只是后来的总结，而不是当时可见资料。

任何笔记都不能直接把 `definition.json` 从 `SOURCE_REQUIRED` 晋级；正式晋级通过 Playbook Lab 的宿主对象和独立版本完成。
