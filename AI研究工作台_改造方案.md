# 牛牛 AI 研究工作台：架构、开发顺序与实施方案

版本：1.0｜日期：2026-09-11｜项目：`/Volumes/Lexar/niuniu`｜改造基线：`c6cb2fd`

> 定位：一个人长期使用、既供人操作，也供智能体调用的因子研究工作台。模型负责理解问题、提出假设、安排研究和解释证据；确定性程序负责数据、计算、规则、统计、预算、权限与归档。
>
> 本文是整体建设合同，不代表全部能力已经实现。首批实际交付见《AI研究助手_首批实施与验收.md》。原始行情只读；保留现有 PyQt、研究引擎、实验归档和任务恢复，不推倒重写。
>
> 首批范围调整：涉及提案工具的源码写入被工具执行层拦截，未提供具体原因，没有改换通道重放。当前落地 P1A 的六项只读工具、JSON CLI 和原生查询入口；模型对话、提案、审批和执行仍待实施。以下目标架构不能当作已完成功能。

## 最新实施状态：P1A 提案与批准闭环（2026-09-11）

已在原六项只读工具之上新增预算预检、固定提案、人工批准、原队列幂等提交和任务结果入口。模型侧共九项工具，不包含批准和直接执行。桌面可使用原业务表单生成草稿，再核对固定记录后批准。

全仓 400 项测试通过；三股真实前复权日线 351 根，在三个独立进程中验证了提案、执行和重复批准后的原回执读取，始终为同一 job/run。详细证据、代码入口、使用方法与限制见 [P1A 开发与验收](AI研究助手_P1A提案与审批验收.md)。

P1B 模型对话尚未连接；P2–P7 未因此提前完成。批准时不冻结行情字节，预算为前置规模估算与合作式时限，不冒充严格数据认证或硬资源隔离。本文下方“首批只读”文字保留为前轮记录，以本段为最新状态。

## 1. 最终要解决什么

工作台的目标不是让 AI 说得像量化研究员，而是让它成为可检查、可中断、能持续积累证据的研究搭档。用户可以提出宽泛问题，助手先查已有资产和数据条件，再把问题转成有限、可执行的研究计划。

| 用户意图 | 助手应完成的动作 | 不允许冒充的结果 |
|---|---|---|
| 已经研究过哪些反转因子？ | 查定义、版本、实验与失败记录，列出处 | 仅凭模型记忆描述本地项目 |
| 找与现有动量低相关的候选 | 定义比较对象、共同样本、成本与验证路径 | 把低相关直接认定为 Alpha |
| 因子早年有效、后来失效了吗？ | 冻结时段与容许差异，检查成熟标签并做子样本检验 | 未拒绝均值相等就认定等效 |
| 三个方向都研究一下 | 固定比较族、预算和停止条件，审批后分阶段跑 | 不断追加参数直到出现显著性 |
| 中断的研究继续 | 找原 job、原计划和可复用子任务，走既有恢复 | 用新任务冒充原任务无损恢复 |
| 追踪最近有没有衰减 | 更新因子值与已成熟标签，记录缺数、延迟和修订 | 把尚未到期的未来收益填成零 |
| 为什么值得保留此候选？ | 指向具体 run、字段、日期、数据快照及局限 | 模型自信程度代替统计证据 |

不建设多租户、组织角色、计费系统或复杂运营后台。必要的本地执行边界用于防止误操作、无限实验和研究污染，不是把个人软件做成企业审批平台。

## 2. 改造原则与范围

### 2.1 四个不可交换的角色

人决定目标、预算和重大选择；模型提出计划并选用工具；工具校验合同并调用确定性代码；归档保存实际发生的证据。

同一模型可以先规划、再分析、再复核，但这不是三个独立科学证人。第一版不用多 Agent 互相辩论，也不引入新的任务调度框架。

### 2.2 继续保持的既有边界

默认研究使用已保存的前复权行情，交易成本继续保留。精细账户是可选路径，不强迫每次探索等待完整公司行动资料。严格 PIT、真实行业/市值中性化及官方历史规则认证，必须由真实资料和指定覆盖审计支撑。

连续 Paper、机器学习训练、Tick/L2、OrderFlow、券商 Gateway、实盘下单不在本轮范围。因子追踪是研究监控，不等于恢复连续 Paper，也不自动交易。

375 项基线回归通过，不代表所有历史资料真实、所有客户端操作完成或策略存在盈利能力。上一轮数据缺口继续进入能力与限制清单。

### 2.3 人和 AI 共用能力，不共用点击动作

PyQt 按钮和 AI 工具最终调用同一应用服务。AI 不靠模拟点击研究，不直接操作数据库、不导入任意 Python 对象，也没有任意路径或 Shell 工具。原生点击仅用于客户端验收。

## 3. 目标架构

```text
用户 ── PyQt 手动研究界面 ──────────────┐
  └── 内置研究助手                     │
       ├── 会话 / 计划 / 证据 / 批准    │
       └── 模型适配器                   │
            ↓ 结构化工具调用           │
       Tool Registry / 参数校验         │
            ↓                         │
       预算 / 范围 / 幂等 / 审批 ────────┤
                                      ↓
                  QuantLab 应用服务（唯一业务入口）
                    ├── FactorRegistry / 数据资格
                    ├── prepare / JobQueue
                    ├── ExperimentRunner 与父研究
                    ├── ExecutionStudy 与统计工具
                    ├── ArtifactCatalog / 复算
                    └── 研究记忆 / 追踪调度（后续）
                                      ↓
                   Parquet / DuckDB / 既有归档
                   本地助手 SQLite 日志与提案
```

内部首先是普通 Python API，稳定后增加 CLI/JSON 和 MCP 适配器。MCP 只是协议入口，不是新研究内核；外部 MCP 不能绕开相同预算和审批。

## 4. 现有代码复用与改造位置

| 现有模块 | 处理方式 | 改造要求 |
|---|---|---|
| `app.default_registry`、`factors/registry.py` | 直接复用 | 分页检索、版本、默认参数，不复制因子表 |
| `workbench/jobs.prepare` | 直接复用 | 所有提案同一配置校验；助手层只增加更严格边界 |
| `workbench/jobs.JobQueue` | 直接复用 | 同一目录单写入队列，不另启第二个 worker |
| `experiments/runner.py` 与各父研究 | 不改计算口径 | 返回 job/run 引用，模型不代算收益 |
| `experiments/trial_registry.py`、`trial_layout.py` | 后续接研究族服务 | 冻结、登记、绑定成功/失败结果，不只留显著项 |
| `stability.py`、`temporal_stability.py` | 后续暴露比较工具 | 保留标签边界、兼容性和等效性定义 |
| `ArtifactCatalog` | 复用并逐步下沉 | 索引慢就优化这里，不给 Agent 再建数值事实库 |
| `storage/` | 直接复用 | 新研究继续能在原客户端查看与复算，不修改旧结果 |
| `desktop/app.py` | 增加助手入口 | 初期保留导航索引，不大改所有表单 |
| 新 `agent/` | 新增薄层 | 工具合同、服务、记忆、模型适配、有限循环、CLI |
| 新桌面助手面板 | 新增 | 对话、调用日志、提案、批准、任务状态和结果入口 |

中期将通用应用服务从 workbench 下沉到 services，采用兼容包装逐个迁移。不要第一批同时搬动全部模块。

## 5. 核心业务对象

### 5.1 标识分开

`factor_id + version` 是定义，`experiment_id` 是确定配置/输入身份，`run_id` 是一次执行，`job_id` 是队列身份。另增 conversation、proposal、hypothesis、campaign、finding 和 tracking 标识。

同一因子多次研究不等于独立发现。代码变更形成新版本，参数变更形成新候选/试验；不能覆盖旧结果来代表新规则。

| 对象 | 核心字段 | 用途 |
|---|---|---|
| ResearchIntent | 目标、市场、约束、不可做事项 | 模型可整理，不扩大授权 |
| Hypothesis | 机制解释、候选公式、反例、失效条件、来源 | 初始为待验证 |
| ExperimentProposal | 原 spec、解析配置、预算、限制、代码/工作空间指纹 | 工具冻结，用户批准后绑定唯一 job |
| ExperimentPlan | 试验族、训练/验证/保留集、数据版本、成本、停止规则 | 冻结后变更需新版本 |
| EvidenceRef | run、文件/字段定位、快照/哈希、观察时刻 | 工具生成，不由 LLM 自造 |
| Finding | 结论、支持/反对证据、适用样本、统计/交易状态 | 模型草拟，服务核对来源 |
| Campaign | 有限研究图、预算、节点状态、失败/未运行名额 | 确定性编排，不无限增加分支 |
| WatchItem | 定义版本、参数、股票池、基线、调度、水位、报警规则 | 人批准加入，版本变化不得静默延续 |

### 5.2 状态不是单个通过/失败

分别记录实现是否可运行、数据是否满足口径、统计证据、成本后效果、跟踪状态。区分 computed、unavailable、inconclusive、supported、contradicted。无数据不等于零，无显著差异不等于等效。

生命周期：draft → validated_implementation → exploratory → confirmatory_candidate → watchlist → degraded / retired。进入跟踪池不自动表示有可投资 Alpha。

## 6. 工具层设计

### 6.1 合同与返回值

每项工具提供名称、版本、说明、JSON Schema、副作用级别、预算、分页方式、错误码和证据类型。模型只看到当前真正实现的工具，不把未来能力放入可执行列表。

```json
{
  "ok": true,
  "tool": "get_experiment",
  "data": {"run_id": "实际UUID", "status": "completed"},
  "evidence": [{"kind": "experiment", "run_id": "实际UUID"}],
  "warnings": [],
  "error": null
}
```

错误结构化为 INVALID_ARGUMENT、NOT_FOUND、UNSUPPORTED_SCOPE、BUDGET_EXCEEDED、APPROVAL_REQUIRED、STALE_PROPOSAL、PROVIDER_ERROR 等。不把栈、密钥和整份行情塞给模型。过大结果分页或明确缩减，不默默丢掉失败样本。

### 6.2 分批开放

| 批次 | 工具组 | 动作 |
|---|---|---|
| 首批已落地部分 | capabilities、search/describe factor、list/get experiment、get job | 真实只读查询与引用 |
| 首批原计划但待实现 | preview/propose experiment、数据请求检查、审批服务 | 校验、预算、保存提案；审批不暴露给模型 |
| 第二批 | comparison、trial family、reproduction | 比较、冻结、绑定与复算 |
| 第三批 | campaign、research memory | 预算内整套研究与结构化结论 |
| 第四批 | watchlist、tracking refresh、alerts | 标签成熟驱动的研究监控 |
| 后续 | factor DSL / sandbox validation | 隔离创建候选，不授予任意生产代码执行权 |

### 6.3 执行分级

只读查询自动运行；提案可自动生成；真实研究默认经用户批准。模型工具集合根本不提供批准接口，不能只靠提示词说“不要批准自己”。

后续增加会话级授权：在指定股票池、日期、因子族、资源预算和最多 N 项任务范围内自动推进，超范围停下。删除数据、覆盖归档、改生产源码、实盘不属于该授权。

个人体验是一口气确认有限研究包，不是每次只读查询都弹确认。

## 7. 助手运行方式

### 7.1 单模型有限循环

用户意图 → 查能力 → 必要只读工具 → 规范提案 → 等待批准 → 原队列运行 → 查 run_id → 总结证据和下一步。

限制模型轮数、工具调用数、输入/上下文预算、输出 token、网络超时、累计失败数。不允许“直到找到好因子”作为停止条件。到上限保存当前证据和待办，不绕过预算。

停止对话循环与取消研究 job 是两个动作。停止助手不暗中撤销已批准任务；取消走原队列。网络调用仍在进行时，停止在请求返回/超时后生效，之后不再调用工具。

### 7.2 模型适配

显式提供 responses 与 chat_completions 两种协议。接口地址、模型 ID 和密钥不进入研究逻辑；用户配置实际可用模型，不假定账户有某个最新型号。

OpenAI 官方当前文档说明 GPT-6 Astra 工具调用使用 Responses API，不能把全部模型统一塞进 Chat Completions。[S1] Ollama 兼容接口作为另一适配目标，但以所选模型与请求实测为准。[S2][S3]

初期不强制引入 Agent 框架，简单可检查的 Python 循环足够。复杂图编排真实出现后再评估额外框架。

### 7.3 密钥与数据外发

Key 使用专用密码框或环境变量，不写聊天、工具参数、Git 或实验配置。首批可以只在内存保存；之后接系统钥匙串，不明文落 JSON。

不自动调用用户未选择的模型。首次向非回环服务外发前展示目的地址并确认。默认只传对话、因子定义、有限配置与结果摘要，不外发整表行情。

网页、论文、报告备注是数据，不提升为指令。模型输出按文本显示，不执行 HTML/脚本；参数、工具名、引用和批准范围由程序校验，不能只靠一句防注入提示。

## 8. 研究记忆：事实、结论、偏好分开

第一阶段存会话、消息、工具事件和提案；第二阶段分事实索引、研究结论和用户偏好，不能混成一个向量库。

权威事实仍是归档。SQLite 保存索引/状态，DuckDB/Parquet 继续存观测。先做因子/版本/参数/样本/时期过滤和全文检索，向量检索只是以后文本召回补充，不作数值事实权威。

Finding 必须引用证据，保留相反结果与局限。旧结论可 supersede 但不删除；区分实际计算、模型推测、用户决定。“未找到显著证据”不能升级为“理论永远没有 Alpha”。

去重分精确配置、因子族近似候选和既有事件链/样本重叠。自然语言相似但数据版本不同的研究，不能自动当重复丢弃。

## 9. 研究编排与统计纪律

### 9.1 有限研究图

数据条件 → 前缀检查 → 探索 IC/分位 → 冗余/消融 → 预设样本外 → 参数/子样本 → 成本后执行 → 结论 → 跟踪候选。

节点有输入引用、输出类型、资源上限、恢复标识、阻断原因。失败保留，只走冻结计划的后继。临时新想法形成新提案，不修改已冻结原计划。

### 9.2 防止 AI 加速数据挖掘偏差

探索集可反复看，但标探索性。最终保留集的访问登记；看过后再调参，后续结果不能继续叫首次样本外确认。实际比较进入家族/探索账本，失败和不可检验项按原规则保留名额。

中性化只用当时可用资料；标签须在样本截止前成熟；不从之后区间补标签。收益标签不是成交收益、低相关不是独立性证明、漂亮回测不是增量 alpha 证明。

不写死通用接受门槛，例如 Rank IC > 0.02。阈值由问题、市场、频率、样本、成本、预设容许差异共同决定；教学数字标示例。

### 9.3 数据资格分层

对具体请求标 research_only、retrospective_reference、strict_pit、official_rule_covered，而不是授予项目永久认证。

严格任务缺行业、市值、历史时间或官方价格界限就返回具体阻断。用户可另建明确放宽口径的探索任务，模型不能静默降级。

## 10. 因子追踪

确定性任务先更新因子和已成熟标签；只有新证据、异常或用户问询时才调用模型解释，不每天反复把全数据发给 LLM。

### 10.1 三个水位

分别保存行情 available_at、因子最后完成时点、标签成熟时点。h 根收益需后续完整 h 根到齐；未收盘、停牌缺根、节假日与延迟时，不能简单用自然日推算。

水位与结果一起可恢复。迟到/修订数据标记受影响窗口，新建 revision 和差异记录，不覆盖旧统计。不同频率分开维护，不把日线拆分钟。

### 10.2 指标与报警

窗口按可用数据定义，20/60/120 交易日仅为可配置例子。追踪覆盖率、成熟标签数、IC/Rank IC 与区间、事件数、换手/成本后代理、相关性变化、风格暴露与状态分布。

预警分数据异常、样本不足、衰减迹象、执行假设变化；设最小样本、连续窗口、冷却和去重。持续显著性判断须另立序贯检验合同，不能把单次检验反复跑还声称错误率受控。

关机/休眠时不承诺运行。先手动刷新，再接本机常驻或 NAS worker；显示最后成功时间及待补期间。这不是实盘或连续 Paper。

## 11. 新因子生成

顺序是已有因子参数/组合 → 受限表达式 DSL → 隔离 Python 候选，不一开始就给助手写生产源码的权限。

DSL 限定算子、窗口、字段、除零/缺失、截面/时序作用域、日历和可用时间。AST 白名单编译，不 eval；表达式、参数、字段与编译器版本进入 identity。

Python 候选放独立目录/子进程/资源限制，默认无网络、无生产写入。完成语法、参数、前缀、未来扰动、缺失/常数、证券隔离和重启一致性后才申请注册。代码测试不是 alpha 认证。

源码批准与研究批准分开；改算法必须新版本并保留变更证据，不直接覆盖已注册版本。

## 12. 界面设计

首批增加入口，不替换既有导航。完整助手未来包含连接设置、会话历史、调用记录、待批准提案与配置、预算、原任务状态和打开结果按钮。

模型文字与确定性状态分区。已提交、已完成、统计支持等标签由程序生成，不由模型说了算。结果链接使用真实 run_id 打开原有实验页面。

后续首页增加今日研究摘要、数据问题、候选池、追踪异常和原计划中断点。传统页面保留为人工精细编辑与证据检查入口。

## 13. 开发与改造顺序

| 阶段 | 内容 | 主要产物 | 依赖 | 验收门槛 |
|---|---|---|---|---|
| P0 | 核对现状、固定边界与基线 | 本方案、能力清单 | 无 | 不覆盖旧修改、不改变研究口径 |
| P1A | 统一工具、预算、提案、审批与幂等 | 工具服务、store、CLI | P0 | 无模型也能查找→提案→批准→队列→结果 |
| P1B | 内置对话与适配 | providers、runtime、对话面板 | P1A | 协议测试与真实模型实测分开记 |
| P2 | 结构化研究记忆与证据 | finding、索引、来源核验、失败记忆 | P1 | 重用真实旧证据，不补造数字 |
| P3 | 有限研究包与登记 | intent→plan 编译、campaign、授权、族绑定 | P2 | 多阶段恢复、全后代对照、失败名额保留 |
| P4 | 因子追踪与标签成熟 | watchlist、水位、刷新、修订、报警 | P2/数据条件 | 跨交易日、迟到、修订、重启，无成熟标签不评价 |
| P5 | 组合候选与增量 | 相关性/消融/残差/成本后比较 | P3 | 同切分成本，保留失败，不自动优化保留集 |
| P6 | 受限因子工厂 | DSL、隔离验证、版本审批 | P3/P5 | 无任意生产执行，每种规则单独因果验证 |
| P7 | 外部 Agent 与常驻部署 | MCP、单写入服务、可选 NAS worker | 合同稳定 | 不绕过权限，不启动两个同库 worker |

实际顺序：工具与提案 → 对话入口 → 记忆 → 固定研究包 → 追踪 → 因子生成。MCP 可在 P2 后提前增加只读/提案入口，但不抢先重写底层。

真实资料补全独立并行，不阻塞所有探索，也不取消严格验收。粗略相对工作量：P0 小、P1 中、P2 中、P3 大、P4 大、P5 中、P6 大、P7 中。不承诺固定天数；授权、模型兼容和真实跨日验证不能按代码量确定。

## 14. 第一批实施合同与当前差异

原优先目标为 P0+P1A+P1B 最小闭环：工具 Schema、预算与提案、会话日志、用户批准、共享队列、模型适配、聊天面板、CLI、边界与回归测试。

本轮实际收窄为 P0 与 P1A 的只读部分：`agent/catalog.py`、`agent/__main__.py`、`desktop/agent_catalog.py` 和主窗口入口。仅有六个只读工具；没有模型、持久会话、提案、审批、研究提交或追踪调度。涉及提案工具的源码写入被拦截，不能冒充已经实现。

下一步先补提案和审批，再接模型。不能为演示聊天效果跳过精确配置、幂等 job_id、代码/工作空间变更检查和预算。审批时冻结数据字节也是独立待办；此前先由原引擎在执行时生成快照，需明确这一边界。

## 15. 工程验收清单

| 层次 | 必须核验 |
|---|---|
| 工具 | 未知工具、额外参数、非法 UUID、超预算被拒；无任意路径/命令 |
| 审批 | 无模型自批；精确提案；重复批准不重复跑；变化不能静默执行 |
| 数据 | 原行情只读；校验和加载区别；小样本不冒充全市场覆盖 |
| 队列 | 复用 worker/锁；真实失败取消恢复；会话关闭不丢批准任务 |
| 模型 | 循环有上限；异常 JSON/未知工具/非有限数/大响应受控；密钥不落日志 |
| 证据 | 真实 run/job 引用；模型文字不当完成状态；限制随结果展示 |
| 桌面 | 无模型也能打开；关闭/换目录不操作销毁控件；后台不阻塞 UI |
| 回归 | 隔离夹具与真实行情分开；旧全仓通过，不降断言 |
| 回退 | 关闭新层后旧客户端/CLI 照常；源码与产物分离 |

## 16. 防止跑偏

不把聊天当事实库；不靠提示词承担权限/计算；不同入口不另起成本规则；不因模型接口强改全部业务；不把全数据塞上下文代替索引；不把自我复核当独立复现；不只记成功；不把本地客户端叫 24 小时服务；不把工具已实现说成真实模型已验收。

## 17. 官方接口参考

[S1] OpenAI Function calling：<https://developers.openai.com/api/docs/guides/function-calling>，核验日期 2026-09-11，用于核对工具协议与 Responses/Chat Completions 差异。

[S2] Ollama OpenAI compatibility：<https://docs.ollama.com/api/openai-compatibility>，兼容目标，不代表各模型支持全部参数。

[S3] Ollama Tool calling：<https://docs.ollama.com/capabilities/tool-calling>，用于工具调用结果回传设计。

参考仅支撑接口选择；项目能力、研究结果和验收仍以当前源码、测试日志与实际归档为准。

## 最新实施补充：P1B 最终集成（2026-09-11）

已完成模型可配置、Codex CLI 现有 ChatGPT 登录、内置对话、九项工具调用、待批准提案及会话重开。当前工作台默认 `codex_cli / gpt-5.5 / medium`，以本机实际模型列表和真实请求为依据，不修改用户全局 Codex 默认配置。

最终全仓 **440 项通过、无失败无跳过**。真实模型三次调用完成定义读取、预检和保存；另一进程一次调用读回同一 `pending` 提案，新建研究任务为 0。新旧对话接口已统一到追加式会话记录，早期 SQLite 会话可只读迁移并保留原文件。

使用入口：顶栏「AI 研究接口」→「打开内置研究助手」，或项目内 `启动牛牛AI研究助手.command`。模型、协议、推理强度和连接地址可以在设置中更改；API 协议经过模拟服务测试，实际外部 API 模型尚未逐一验收。

详细证据和限制见 [P1B 最终验收](AI研究助手_P1B模型与对话验收.md)。以上更新覆盖前文“P1B 尚未连接”的历史状态；P2 结构化研究记忆及后续阶段仍未完成。当前仅保存会话，不能据此宣称具备完整科研知识库、因子追踪或自主因子工厂。
