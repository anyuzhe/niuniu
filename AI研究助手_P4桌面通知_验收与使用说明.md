# 牛牛：P4 桌面通知、去重与免打扰

日期：2026-09-11。改造基线：`9814ac9`。对应原八步计划中的第6步（因子追踪）。

## 1. 实际交付范围

增加独立于研究执行器的桌面通知入口，读取现有应用内未读提醒，不创建研究、授权或下载任务。
默认关闭；设置按工作空间保存在本机 Qt 设置中，不写入 Git。正常工作区没有被本轮验收启用。
支持本机时间22:00至次日08:00免打扰，默认勾选；免打扰期间不占用投递号，结束后再汇总。
通知只展示类别和数量，不在锁屏横幅中展示股票、指标、原始备注或完整任务配置。
通知图标/消息点击回到原受控跟踪提醒面板，原应用内提醒不会自动标为已读。

本轮最初计划开发跨批次滚动数据源，但该新模块写入被执行层拦截，未提供原因。
已确认 `src/quantlab/data/managed_series.py` 不存在，未重复提交该被拦截写入，也未声称数据滚动已完成。
后续改为推进原计划中独立的通知功能，没有改变已有数据源、行情或研究口径。

## 2. 使用入口

正常退出并重新启动牛牛：菜单「Baostock 数据」→「桌面通知」。
勾选「允许桌面通知（当前工作空间）」后，打开期间每分钟检查未读；不会因此获得研究执行权限。
「免打扰22:00–08:00」使用电脑本机时区，与交易日历的北京时间约定相互独立。
「检查未读并尝试通知」遵守同样的开关、免打扰和系统能力检查，不绕过设置。
「查看桌面通知投递记录」展示最近100条记录；点击托盘消息可返回应用内提醒。
未选择行情目录不妨碍读取既有提醒；工作空间切换后读取新空间独立设置，旧回调不能弹出旧空间消息。

## 3. 投递语义与限制

投递标识包含跟踪、授权、提醒条件和该条件最后一次通知时刻；继承原提醒的六小时冷却规则。
先把投递号提交到SQLite，再调用界面通知，保证同一投递号最多尝试一次；并发、重启不会重复尝试。
每次最多汇总20条尚未尝试的提醒为一条系统通知。每空间最多10000条投递记录，满额报错而不删历史重发。
崩溃发生在登记与接口调用之间时，通知可能没有发出；不会自动重放这类不确定投递，原提醒仍留在应用内。

| 记录状态 | 含义 |
|---|---|
| reserved | 已保留投递号，是否调用过系统接口未知，不自动重试 |
| handed_to_qt | 已交给Qt接口，不代表横幅显示，更不代表用户已读 |
| dispatch_failed | 调用接口出现异常，应用内提醒仍保留 |
| suppressed | 显示前被关闭设置、切换工作空间或免打扰等条件抑制 |

系统配置、权限、专注模式等可能阻止显示。程序不能据接口返回认定用户收到通知。
官方Qt说明明确表示消息可能完全不显示，不能把它作为关键提醒的唯一渠道：
https://doc.qt.io/qt-6/qsystemtrayicon.html#showMessage

## 4. 验收结果

- 全仓550项测试通过，0失败、0跳过；比534项基线新增16项。
- 18项定向测试通过，其中包含两项原跟踪界面测试，不重复计入总数。
- 覆盖默认关闭、并发领取、跨进程重启、六小时冷却、未来/已读提醒、失败/抑制、免打扰、系统不支持和工作空间切换。
- 使用此前真实三股历史跟踪验收留下的1条实际未读提醒，在隔离副本中验证：第一进程领取1条，第二进程0条，原控制记录字节不变。
- 本轮没有运行新金融实验、启用正常跟踪授权、联网抓行情或向操作系统发送测试弹窗。
- 本机只读能力探测：Qt 6.11.0，cocoa；托盘可用、supportsMessages=true；正常工作区通知开关false。
- Qt界面行为由隔离测试及模拟通知传输验证；未完成肉眼确认系统横幅的原生验收，不宣称操作系统实际送达。

## 5. 存储与证据

投递记录：`artifacts/_notification_delivery/receipts.sqlite3`。研究授权、行情、快照和应用内未读状态独立保留。
日志及脚本：`artifacts/desktop-notices-20260911/`，包括`targeted.log/.exit`、`full-tests.log/.exit`、`saved-notices-acceptance.json`与`host-capabilities.json`。
源码指纹：`458a52d60ec37d5e1560be510713b23b85c9b9e527d1aa1ee557340cebcd642a`。

## 6. 仍未完成

跨批次滚动数据源与自动下载衔接未交付；当前仍使用明确选定的本地数据目录。
操作系统横幅的实际可见性、连续多日无人值守、关机期间执行、邮件/手机推送、迟到数据合同、跨版本迁移仍未验收或实现。
本轮不修改交易策略，不认证Alpha、严格PIT、每日市值或官方历史交易规则。
第7步受限因子生成、第8步外部智能体/常驻部署仍按原顺序保留。

## 7. 本轮 Git 收尾状态

提交与推送请求被工具安全检查拦截，没有重放该写请求。随后只读核对：HEAD仍为9814ac9，本轮8个源码/测试/文档文件保持未提交状态。
550项全仓测试和其他验收的退出码均为0；测试通过与Git交付状态分开记录。本轮不能称为已经提交或推送。
