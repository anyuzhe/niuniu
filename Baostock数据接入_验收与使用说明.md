# 牛牛：Baostock 数据接入、估值因子与日历检查

日期：2026-09-11。项目：`/Volumes/Lexar/niuniu`。改造基线：`deee563`。
本轮收尾上轮未提交的数据模块，并接入原研究队列、正常桌面启动和冻结数据复算。
全仓 **521 项通过，0 失败、0 跳过**。真实数据及真实 Codex 查询验收通过。

## 1. 本轮已完成

有限批次导入、原始响应留存、数据集校验、人工选择数据集、四项模型查询工具、五个滞后日频因子和跟踪到期候选检查。
正常启动牛牛或原 AI 助手脚本后，都能看到「Baostock 数据」菜单；不再要求用户改用独立开发入口。
原 `data/mqc.py` 与本轮开始时的 Git 版本逐字节一致；普通 MQC 目录仍使用原读取器。
带 `baostock-dataset.json` 的管理数据集使用专用校验适配器；损坏标记不静默退回普通读取。
研究包输入签名、原研究队列、信号数据和可选 raw 成交源遵守同一适配器选择规则。

## 2. 数据类别与真实批次

导入支持原始/前复权日线、日频估值及换手、交易状态、交易日历、证券资料、指定日期行业/成分/全证券列表、六类季度财务、复权事件及每日全市场接口。
每次显式联网请求写入新批次；不覆盖 `MQC-DATA`。空响应、接口失败、抓取取消、标准化失败分别保留，未取得的数据不会填成零。

原始真实批次：`402a77b6-19f5-4bb4-95c9-79879a725d5e`，位于：

```text
artifacts/_market_data/baostock/402a77b6-19f5-4bb4-95c9-79879a725d5e/
```

本轮复用上轮已抓取的该批数据，没有再次下载同一批次。已核对 **59 份原始响应、24 份标准化数据文件**。
三股为 `sh.600000`、`sh.600519`、`sz.000001`；2024年上半年原始和前复权日线各351根，每股117根；日历182天。
六类季度财务每类6条；当日行业3条；指数成分50/300/500条；全证券5657条、全A股日线5117条、每日复权事件110条。
2024-06-28的ETF响应为空，仍记为 `no_data`，不拿其他日期冒充。

## 3. 新增五个因子

| 因子 ID | 原始字段 | 行为 |
|---|---|---|
| `BAO.PE_TTM` | `peTTM` | 滞后滚动市盈率 |
| `BAO.PB_MRQ` | `pbMRQ` | 滞后市净率 |
| `BAO.PS_TTM` | `psTTM` | 滞后滚动市销率 |
| `BAO.PCF_NCF_TTM` | `pcfNcfTTM` | 滞后滚动市现率 |
| `BAO.TURN_PCT` | `turn` | 滞后换手率，保持百分数单位 |

版本均为1.0.0；唯一参数 `lag_bars` 为1–252，默认1，不接受0、负数或布尔值。
估值、换手字段来自不复权响应，再与所选价格序列按证券和日期对齐。缺值不前填、不填零；负市现率等原始负值保留。
这五项是待研究的数值特征，不自动把“低估值”转成买入指令，也不宣称已经发现Alpha。
滞后一根实际日线仅防止直接用本根值回填前一根；无法证明供应商的历史财务版本、实际发布时刻和修订记录可靠。
目前没有把季度财报自动拼接成逐日因子，没有用季度股本计算所谓真实每日市值。

## 4. 使用入口

重启牛牛，在菜单栏选择「Baostock 数据」→「导入、查看和选择数据集」。
先选择证券、起止日期、数据类别、快照日期和财务季度，预检有限查询数量，再勾选本次联网许可。
修改证券、日期或数据类别会清除确认勾选。取消标记已设置时，不会启动下载进程。
在已有批次列表核对来源、查看表格，再点击「人工选择为本次研究行情目录」。有正在读取或执行的任务时不能切换。
此选择只改变本次工作台的数据目录，不移动文件、不修改默认启动脚本里的MQC目录；重启后需要重新选择或显式传入路径。
然后在原新建实验表单中搜索 `BAO.`，选择明确版本和滞后参数，继续使用原提案、队列和实验中心。
「Baostock 数据」→「打开带数据工具的研究助手」可以直接读取批次和财报；普通内置助手按钮也进入该面板。

外部调用可使用 `python -m quantlab.agent.market_data_tools --output artifacts --call get_baostock_import --arguments '{"import_id":"402a77b6-19f5-4bb4-95c9-79879a725d5e"}'`。
四项新增工具为 `list_baostock_imports`、`get_baostock_import`、`read_baostock_table`、`get_watch_refresh_readiness`；模型没有下载、批准或直接执行数据任务的工具。

## 5. 跟踪到期检查

菜单「日历驱动的跟踪到期检查」读取指定完整日历和带时区的检查时间，返回候选日期和落后证券。
日历缺日、超出覆盖、来源变化和旧运行环境分别处理，不回退到“周一到周五必然交易”的猜测。
名义更新时间加缓冲是检查约定，不证明上游已到货；当前规则以原始17:30、前复权18:00北京时间加30分钟为基准。
候选仍需先取得行情，再生成刷新提案并人工批准。代码或跟踪算法与基准不匹配时返回 `baseline_rebuild_required`。
本轮没有启用常驻定时任务、自动批准、主动通知、连续显著性报警或跨版本迁移。

## 6. 验收证据

全仓运行 `QT_QPA_PLATFORM=offscreen PYTHONPATH=src:tests .venv/bin/python -m unittest discover -s tests -q`：521项通过，无失败无跳过。相对500项基线新增21项；定向测试组不能再重复加总。
真实批次构建PE、PB、成本后模拟研究包，3节点全部完成，固定族8项检验。
禁止普通MQC和管理数据读取器后，仅凭冻结输入完成整包复算：5个归档、12份Parquet及完整关系和统计核对一致。
真实PE跟踪从174根推进到351根，保留2份快照；再次同步不重复创建，另一Python进程重新核对来源一致。
日历检查从 `candidate_for_refresh` 变为 `up_to_date`。这是对所选2024年历史区间的流程验收，不是实时数据已更新的证明。
真实Codex CLI/gpt-5.5执行两项查询：批次校验和profit表读取，正确取得59份响应、24份数据文件及两条实际财报日期，模型未新增研究任务。
界面采用隔离Qt功能测试；真实研究批准由验收脚本调用宿主服务，不冒称全部原生人工点击验收。

本地证据目录：`artifacts/baostock-finish-20260911/`，其中 `full-tests.log/.exit`、`real-acceptance.json/.exit`、`model-acceptance.json/.exit` 保存实际结果。
代码指纹：`94083c693f08aede1110e1b07eb89c2acb922d125ebc0b9d7e6fc74746b5273a`。
原始真实导入批次及验收副本在运行前后逐文件校验一致；验收研究和模型会话保存在独立工作空间，没有混入正常研究目录。

## 7. 明确保留的边界

当前管理数据入口只提供日线；行业与成分仅覆盖查询日，季度财务只是带来源的报告表。每日真实总股本/总市值、历史首次可用时刻、完整修订历史及官方每日涨跌停价仍缺可靠资料。
新因子、研究包和毛收益统计的功能通过，不代表存在Alpha、盈利或真实可成交性；本轮成本节点为研究价格模拟。
正常工作台的数据选择是人工且会话内有效。SDK仍为已有可选依赖，本轮未升级CLI、未修改全局Codex配置、未新增第三方依赖。
官网参考为用户提供的Baostock首页及其每日更新、日频估值、证券元信息、季度财务接口说明；实际可用性以本地保存的响应和核验结果为准。
代码与说明纳入Git；行情、导入批次、模型配置、研究快照、验收日志与个人记忆不上传。
