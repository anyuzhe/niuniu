# 当前状态与后续路线

[文档导航](../README.md) · [总体架构](../architecture/overview.md) · [开发总档案](changelog.md)

状态初始整理日期：2026-09-17；后续增量按各条目日期记录。初始代码基线：`5a381e8`（文档整理之前的 HEAD）。本页是**当前人类可读状态的统一入口**，区分代码实现、实际数据、部署和授权。历史文档中的“下一步”只代表当时判断，不再作为当前待办。

## 1. 已具备的工程能力

| 主线 | 当前代码范围 | 仍不能据此声称 |
|---|---|---|
| Trading Desk P1–P7 | 导航、Decision Ledger、Stock Dossier、Theme Matrix、Frame、策略意图与驾驶舱 | 已形成实盘成交 |
| P8 / P8.5–P8.8 | AI Team、Playbook、多类型 StrategySource、DailyMarket/Scanner/Orchestrator、动态 Paper 与复盘 | 已积累足够长期前瞻样本或已证明 Alpha |
| P9–P12 | 任务分类 Scorecard、隔离 Dev Studio、System Health、同源移动只读入口 | 自动调权、自动修复、无人审查发布 |
| P13-A / P13-B0 | 只读券商证据、Shadow 对账、fail-closed readiness | 真实券商已连接、密钥/订单/资金权限已开放 |
| Research Lab | 因子/理论、实验、统计、执行、归档、Campaign/Factory/Watch | 全流派覆盖或所有算法支持通用续算 |
| 研究安全 | approval-time 实际输入冻结、有限 Research Session Grant、Watch 序贯监测 | 任意下载、任意研究或自动改参数授权 |
| Strict PIT 工程 | statement archive、Universe receipt、SecurityStatus v2、Coverage、MarketRules receipt | 全市场历史资料已经齐全 |
| 外部知识 | Git archive、选择性策展、Git-clean 只读 Research Skill Library | 外部知识天然正确、可执行或具 Alpha |

## 2. AR 自主研究与打板情绪线

2026-09-16 的旧总体架构把 AR 写成规划；随后代码与提交已持续推进，不能再照抄“尚未实施”。

| 阶段 | 已有实现 | 运行边界 |
|---|---|---|
| AR-1 | 日期制度、涨停状态、回溯日线 pack、事件库、情绪及周期、前瞻增量 | 供应商/回顾性口径不升级为 Strict PIT |
| AR-2 | 公开证据、股池、龙虎榜、主题/成员、人气、可续抓调度及授权时段快照 | 代码存在不等于当前 Mac 定时任务已成功运行 |
| AR-3 | 事件研究、保守成交模型、题材事实与前瞻事件明细 | 候选收益、成交访问、账户收益分开；严格保留预登记方向 |
| AR-4 | AI 只读研究工具、收盘复盘、可验证预测与校准 | 预测样本仍需后续数据检验 |
| AR-5 | 研究计划授权、受预算约束的夜间任务、结论库/失效监控与盘前简报 | 实际计划、授权、晋级、预算与部署需分别核验 |

源码入口：`trading/auto_research.py`、`research_conclusions.py`、`daily_review.py`、`limit_forecasts.py`、`premarket_brief.py`，以及 `agent/evidence_scheduler.py`。均可从 [代码地图](../development/code-map.md) 定位。

完整阶段清单、原始数量、研究结论、Git SHA 和运行记录保留在 [AR 原始规划与验收](../archive/autonomous-research/牛牛AI交易助手_自主研究与打板情绪研究_规划与进度.md)。原文中的未安装/待授权事项有各自时点；本轮未重新检查后台部署，不替用户作授权决定。

## 3. 最近应覆盖旧说明的变化

**扶摇（`5a381e8`）**：已接入宿主白名单聚合工具和个股临时报价，主源为扶摇、公开网页共识负责交叉校验/回退；无凭证保持原链路。不是正式快照或交易权限。[专项验收](../archive/integrations/牛牛AI交易助手_扶摇MCP接入与验收说明.md)

**回溯日线 pack（`b0ba3bf`）**：已有合并与读取接线，旧小文件迁移过程保留在 AR 记录。此后不能照抄“待决定是否合并”，也不能为了整理目录再删除一次数据。

**盘中证据采集（`023bf6b`）与 qimo 费用（`63e3f4d`）**：代码与合同已更新；真实 LaunchAgent 安装、既有控制重新授权仍应以本机实际状态核验。不得因文档整理重新开启采集或改写模拟成交历史。


### 2026-09-17 后台自主候选实测补充

后台聊天已新增显式 `--allow-granted-research` 队列接线；缺 Grant、超范围、预算不足仍拒绝。真实 GPT-5.5 已在独立两证券样本中完成“读数据摘要 → 自拟三个DSL候选 → 预览/冻结 → 授权内提交 → 逐一读结果”；三次归档复算数值匹配，测试授权已撤销。

这是**单批次工程验收**，不是有效 Alpha 认证。每个时点只有两只股票，低于引擎的三证券 IC 门槛，Rank IC 均为空，候选有效性尚不可判断。正式工作空间的 Grant/DSL注册没有因此增加；每日唤醒模型、自主生成新提案的持久运行尚未部署。详见 [实际结果、脚本错误与证据](../archive/testing/20260917-自主因子后台实测.md)。

### 2026-09-17 冻结三候选的100股时间分割验证

本地数据固定为四个板块各25只、共100只股票（167,537行）；公式和原方向不变。2020–2023样本内、2024已见年份诊断、2025至2026-09-04历史样本外。三个候选×两期限×三阶段的18个Rank IC检验统一Holm校正；振幅/日内实体两个负向候选通过预登记的回顾性信号门槛，放量候选原正向假设未获支持，不反向改写。

当前是**回顾性预测关联，不是已确认Alpha或可成交收益**。生产MQC仍拒绝空量；本次独立研究适配保留164行空量和全部时点、屏蔽相关信号，未填零或换股，缺失价格等仍拒绝。全量重算数值匹配，新增10项及相关回归共47项通过。未调用新模型、注册正式因子、部署常驻研究或下单。下一步先做冻结候选的控制因子/独立成交与成本验证；完整证据见[原实测文档第二阶段](../archive/testing/20260917-自主因子后台实测.md)。

### 2026-09-17 原生助手功能优先

用户要求研究由牛牛自己完成，外部开发者只观察和补数据/流程缺口。已补正式本地行情发现与检查工具、DSL合同、纯本地行情开关和字段错误说明。原生CLI真实模型完成一次自主选样本/选已有因子/记录假设/授权内执行/保存结论，并在新进程新会话里读回核验。没有临时注入研究工具或由宿主代交研究任务。详见上述实测第三阶段。

上一阶段100股确定性统计是宿主验证，不作为自主助手里程碑。后续继续从牛牛实际任务中观察缺口，不由开发者代选控制因子或追加统计。持续唤醒模型的无人值守流程仍未部署；本地新工具暂不覆盖管理批次/通道发现，必须使用对应现有入口。

### 2026-09-17 QM50原始规格监督测试

用户指定下载目录的QM50-SCLA v0.2原始MD/JSON后，由牛牛正式助手读取global和全部11组；60项原始定义一致。新增不可变规格导入、版本绑定和禁止替代的后台测试入口。牛牛实际运行34项合成组件测试及自行选取5只主板股票的125行P07原始公式诊断，监督者独立逐值核对一致。

**尚非60项完整回测：59项缺本规格精确原始数据适配器；P07只完成原始值诊断，历史可用时点/候选全集仍缺资格。竞价、1分钟/逐笔、历史题材和规则等需分别核验，原文件预算为空，真实Q/排名/交易/净值均未生成。**旧qimo-source-rules-v2没有替代本规格。详见 [完整来源与监督记录](../archive/testing/20260917-QM50-SCLA-v0.2-严格规格验收.md)。

### 2026-09-18 QM50基础依赖检查已接通

牛牛自己提出先补证券状态、有效涨跌停价和候选资格依赖。已新增原生只读基础覆盖工具和P01/资格纯组件合同；既有get_strict_pit_coverage也已在真实聊天入口显式注册并由模型调用验证。不是新增一套替代因子研究。

牛牛自行选择4只股票、2026-08-25至2026-09-04的9个决策交易日，36行真实覆盖矩阵全部保留BLOCKED、候选和高度为null。全局14条稀疏状态、1份7条正式规则、1份7条回顾性参考不等于此请求覆盖；完整Universe与完整逐日状态回执仍为0。现有MarketRules还缺参考价、tick、历史板块/品种、退市整理、新股无限制标志及正常制度字段，不能按名称假定完整。

最终25项合成基础组件检查通过；新增17项、相关116项回归通过。真实字段输入、历史可用时点、候选全集和任意长连板前缀仍需逐一接入，P01尚不能作为真实因子生产输出。详见 [QM50验收第二阶段](../archive/testing/20260917-QM50-SCLA-v0.2-严格规格验收.md)。

### 2026-09-18 已有原始归档接入（不是新增行情下载）

发现日常工作空间已有包含preclose/turn/isST/tradestatus的5,455证券回溯capture，而此前规格入口只看到旧MQC目录。现已增加明确宿主来源工作空间绑定、pack/原目录双读、原始JSON与Parquet全值校验、D-1输入接入及原始字节独立复算。

牛牛自行选4股、2024-01-01至2025-01-05，完成976行输入物化；其中D-1供应商ST112行、停牌1行、交易非ST863行。P07算术955行、21行保留不可用。首次replay因JSON字典排序改变股票行序而false；修复后新进程对同一结果equal=true，原记录不重写。原始12个输入字段共11,712值监督核对一致。

这是实际数据接入进展，不只是新增合成测试；但供应商历史字段不是官方回执，尚缺有效涨跌停价/tick、流通股本、题材/竞价等。P01/P06/候选/Q仍未生成，严格原版回测仍阻断。详见 [同一验收第三阶段](../archive/testing/20260917-QM50-SCLA-v0.2-严格规格验收.md)。

### 2026-09-18 TDX新增数据线索已实测，尚未生产集成

用户提供pytdx/eltdx截图后，在独立个人研究环境使用PyPI eltdx3.2.2实际取得1m K线、历史成交、开盘/收盘竞价过程、正式09:25撮合、五档、财务股本、权息事件、个股题材和两天涨跌停/炸板列表。2025年的竞价日期样本非空、2024样本为空，不能推为全历史完整。pytdx1.72在两个主站跳过SetupCmd3后恢复小样本K线。

这是来源可行性，不是牛牛已完成新数据集成或因子回测。ELTDX当前Research-Only许可限制生产、商业和自动交易服务；因此未加入主环境依赖、常驻采集或订单链。后续应在适用授权内优先建立1m/正式竞价的明确原始字段输入与归档，由牛牛检验覆盖。详见 [实际响应、失败与许可边界](../archive/integrations/20260918-TDX-数据源可行性实测.md)。

### 2026-09-18 TDX个人研究数据已入原数据库，并启用断点自动恢复

用户明确要求保存新增全部类型后，已增加13个tdx_*数据库视图、原始响应/Parquet与可恢复队列；既有日线/5m不覆盖。实际全市场计划为5,573个当前供应商A股标识及236个已有历史标识，共5,809个；起始任务58,091项。已取得的先前21个有效响应页亦带原时间迁入，所有类型均有真实数据，不只是建空表。

2026-09-18早间检查发现旧采集器在64,338个请求后因一串 `ConnectionClosedError` 停止；数据页没有发现“中间失败、后页继续”的分页断链。现已把瞬时错误改为精确请求内有限重试+两个核验主站轮换，连续失败进入递增冷却并只恢复瞬时错误；协议坏包继续显式ERROR。短时真实恢复从同一plan继续248请求、0恢复轮，因90秒测试预算正常退出。

本机已加载用户级LaunchAgent：登录后自动恢复未完成active plan，退出后至多5分钟再次尝试。手动STOP永久优先；多轮恢复耗尽、访问限制或磁盘保护写AUTO_HALT并停止自动重启，manual resume才清除。当前仍是**全市场全历史未完成**，自动恢复只解决中断续采，不把供应商缺失/协议错误变成完整数据。24项TDX测试及10模块118项相关回归通过。此为个人研究采集，不是商业/生产或订单服务。详见 [入库记录与操作命令](../archive/integrations/20260918-TDX-数据源可行性实测.md)。

### 2026-09-18 TDX调度优化复核与正式应用

沿用 `82de01b` 上未提交的scheduler-policy v2，增加真正的只读预览、精确队列快照绑定、审计保留和显式apply；默认请求间隔回到三机初始每节点0.35秒。沪深竞价保留调度下界为2025-07-22，北交所不设该下界，trades只受生命周期裁剪。上市/退市日期不是Strict PIT资格。

真实计划5,809标识中5,801已有生命周期、8未知保守保留。逐日族理论symbol-day从50,700,952降至18,546,396（63.42%），竞价进一步至1,777,768；不是分页请求数或剩余工期。备份后正式标记237个沪深竞价待采任务为SKIPPED_POLICY，并建立26个有效断点，未删除旧任务，原240条ERROR逐字段未变。随后60任务真实续采按预算结束、无恢复轮，STOP已恢复。

原28项基线、扩展39项TDX专项与11模块133项相关回归通过。修正Windows文件锁兼容与恢复时只重试瞬时错误；Windows实机尚待部署验证。三机分布式采集仍在开发，不能把本次单机短测写成三机在线。证据位于 `artifacts/tdx-distributed-20260918/`。

### 2026-09-18 TDX分布式核心接续与传输验收

Mac Tunnel恢复后确认五个第二阶段未提交文件保留，最后CLI创建未落盘，旧canonical STOP仍有效。现已补齐分布式CLI、固定分片/最小断点、逐页与分页链校验导入、总状态、loopback-only文件传输和有回执的worker循环；未提交页不进入正常视图，上传成功不算入库，断线保留outbox。

Mac完整TDX profile 70项通过，13模块相关回归164项通过。Windows路径保护新增真实junction拒绝验证，不开启开发者模式或提权；完整应用多处fcntl依赖不冒充已移植，独立worker profile显式区分那一个原生应用接线测试。节点实机结果、bootstrap与长期服务仍需各自验收，不把单机测试写成三机在线。Mac/601已有SSH认证实测可用，HomePc现有公钥认证未通过；没有创建新密钥或修改服务端授权来绕过。详见[运行与安全合同](../guide/tdx-distributed.md)，真实证据继续保存在 `artifacts/tdx-distributed-20260918/`。

### 2026-09-18 TDX正式分片与节点部署结果

功能提交 `555ca6451f3cb167323709804293779a5b96061c` 已推送，三台均fast-forward到同一SHA。cluster `fbe957f3…` 固定分配Mac 1,951、HomePc 1,953、601 1,905只，共5,809；三份bootstrap分别携带6,872/6,864/6,629页必要checkpoint，不是全部历史复制。

Mac worker独立根 `/Volumes/Lexar/niuniu-data/workers/worker-0` 已逐页核验安装。真实60请求从原offset 800/1191/1600/2290等继续，21.592秒、0网络错误；首次汇总66页（含空响应审计和撮合子页），重复导入0新增/66重复，237条canonical原错误未变。实际LaunchAgent带STOP启动处理0请求，然后显式启用worker 0持续循环；旧全量LaunchAgent已卸载并保留原plist，canonical STOP仍在。

持续运行随后实测完成第一轮264请求/120.615秒、0网络错误，自动导出并汇总288页，累计两份结果包均已ack，冲突0；下一轮出现新的RUNNING心跳。采集窗口约2.19请求/秒，导出/逐页验证/汇总另外耗时，不能把该值当含同步的总吞吐或三机聚合速度。

两台Windows各69项独立worker实机测试通过，完整应用接线仍明确不在该profile。两台未安装bootstrap、未启动采集：HomePc现有SSH身份未获中继认证；601创建计划任务遭Windows拒绝访问，其SSH隧道启动另被平台安全检查拦截，未换方式执行。闲置Mac中继转发已卸载；本地文件服务仅监听127.0.0.1。当前不是三机上线，不报告三机提速或全历史完成。

Mac真实验收、STOP实测和动态进度见同一artifacts目录的 `mac-shard-live-acceptance.json`、`mac-launchagent-stop-proof.json`、`deployment-progress.json`；Windows各自 `tdx-control/deployment-checkpoint.json` 保留实际阻塞层。操作入口与节点路径见[运行指南](../guide/tdx-distributed.md)。

### 2026-09-18 Windows传输身份和当前用户监督器增量

增加独立非Shell中继账号，仅允许既有公钥访问127.0.0.1:18943的本地转发；不开放公网文件端口，原root/ollama-tunnel有效配置核对不变。601原生隧道实际取得Job并通过cluster健康核对，正在传输原bootstrap。HomePc调用仍被平台安全检查拦截，保留操作员本机启动入口；不得改走监督器自动启动被拒绝的隧道。

新增当前用户Windows监督入口和8项边界测试；Mac完整TDX78项、相关172项回归通过。独立worker STOP/AUTO_HALT、监督器停止、缺bootstrap和错机器/分片均有明确保护；Windows计划任务及实际数据回传待后续回执，不能据此宣称三机全部完成。

初步容量/工期测算保存于artifacts/tdx-distributed-20260918/capacity-forecast.json：仍有约1,854万trades股票日和177万auction股票日，分页数不能从PENDING数量推断；三节点吞吐仅为条件情景，当前空间不覆盖测算的全历史保留容量，完整性错误仍保留。

### 2026-09-18 两台Windows计划任务与小型checkpoint交付

两台均以当前用户Interactive/Limited成功注册计划任务，STOP启动实测为0子进程；各77项独立worker测试通过。HomePc操作员本机启动后，传输health也已核对正确cluster。两台原始大包下载慢，现生成保留canonical全部原始页的小型前驱校验凭据包（4.76/4.61MB）；原plan/policy/shard/frontier均不变，旧完整包保留。

新凭据不进入正常视图；worker重复页/断链保护与canonical实际原始前驱校验两层保留。Mac完整88项TDX、相关182项回归通过，真实节点安装和回传继续按部署回执更新。容量审计确认当前Lexar小文件每个至少512KiB、单页目录和三文件约2MiB；不解决存储布局就不能按逻辑内容大小预测全历史完成时间。

### 2026-09-18 Windows真实采集与回执响应

两台Windows现已安装原计划的独立worker，小包校验完成；各87项独立worker测试通过。真实计划任务分别验证USER_STOP与AUTO_HALT为0子进程。HomePc60请求21.102秒、源端错误0，64页结果MERGED/ACK且重复发送不增加数据，原76条错误不变。

601亦完成60请求21.071秒、源端错误0，初次回执查询超时、后续临时SSH到预算退出；同一结果包保留待确认，不重新采集。已将文件服务HTTP接收与单一合并线程分离，避免慢合并阻塞health/receipt；仍取canonical writer锁，91项TDX/185项相关回归通过，后续部署与回传恢复以实际回执更新。

### 2026-09-18 Windows长期模式改为本地采完后物理转移

用户明确不要边采边传。两台Windows真实60请求验收完成后保留的STOP/SUPERVISOR_STOP是人为验收保护，不是采集故障；原监督器又把Mac传输health作为启动前置，因此不会进入长期采集。

现已把Windows监督器增加`offline_only`模式：长期计划任务只运行固定shard的autoresume，不启动传输隧道或每轮export，STOP/AUTO_HALT与30GiB磁盘保护仍优先。601 D盘约1.57TiB空闲；HomePc没有外接大盘，E盘约381.76GiB空闲，预计不足以容纳完整历史成交分片，因此允许先跑并由磁盘门安全停机，不能虚报可在现有容量下完成。

最终数据可等各worker停止后用移动硬盘一次性带回Mac，再走原有SHA、source_id、分页前驱、重复页与冲突fail-closed校验汇总；不通过物理拷贝绕过canonical导入规则。

### 2026-09-18 Mac请求节流提速

同一批100个真实历史成交请求、不写正式队列的受控基准：0.35/0.30/0.25/0.20秒分别达到2.815/3.296/3.938/4.896 req/s，四档均100/100成功、0网络错误。随后0.20秒直接运行正式Mac shard 300个任务，74.845秒完成、300网络请求、0网络错误、0恢复轮，原ERROR记录逐字段不变。

长期运行不直接把scheduler policy从0.35改写成0.25，以免改变policy_id和分片绑定；新增独立运行时request interval override，仅改变请求节流。Mac正式LaunchAgent采用0.25秒作为当前长期档位，0.20保留为已通过短测但尚未长时间观察的候选。运行时progress/result显式记录实际request_interval_seconds。

### 2026-09-18 当前最终采集范围：Mac单机三逻辑shard、无K线/无trades

用户进一步明确：既然K线由其他渠道提供，也不需要为K线/成交量研究服务的逐笔成交历史。HomePc与601计划任务已禁用并保留SUPERVISOR_STOP/worker STOP，实机核对无TDX进程；当前只让Mac运行。

Mac保留原0/1/2三个固定shard和独立SQLite，shard1/2使用既有校验witness安装到`/Volumes/Lexar/niuniu-data/workers/worker-1/2`，并从canonical接续已经合并成功的非K线页，避免重下。当前三个LaunchAgent均在Mac运行，每片`workers=2`、运行时interval=0.75秒，聚合约3.5–4 req/s；观察窗口内网络错误0。

collection-scope现排除`bars_1m`、`bars_5m`、`bars_daily`、`trades`、`opening_match`。已有SAVED/EMPTY/CHECKPOINT/ERROR全部保留，不删除；三片本轮分别将1882/1877/1813个待采trades审计标为SKIPPED_POLICY。代码在autoresume恢复后再次应用scope，防止被排除族因重启/瞬时错误恢复重新进入网络请求。实际心跳已显示excluded_families，后续只剩auction为主要历史长任务，shard0另有极少全局limit_ladder。

### 2026-09-21 Playbook未入选对照组与AI产业雷达外部方法

Playbook选择结果复盘已接入：同一冻结CandidateSet内“选中vs未选中”的D0（仅PREP）与D1/D2/D3/D5/D10信号收益对照，由宿主CLI `niuniu-selection-outcomes` 生成，AI可用3个只读工具查看汇总、列表与单条。缺日线阻断、数据修订冲突不覆盖、少于3个样本标INSUFFICIENT_SAMPLES；不是可成交收益、Alpha或显著性检验，不自动调权、不写Decision/Intent/Paper。真实工作空间尚未运行生成。

AI产业雷达以 `ai_industry_radar` Research Skill 入库（PUBLIC_METHOD）：2个DRAFT假设为“热度即拥挤度报警”（反向）和“产业上游+未来确认时刻”，21个claim按原话、推演、博主自述分层。题材年龄的前视偏差、缺PIT确认事件日历与产业链映射都登记为阻塞；未写StrategySource/Playbook，检验需牛牛自行预注册。策展包已在用户授权后写入 `/Volumes/Lexar/niuniu-data`：176个文件逐一SHA256核对，真实数据根上Library的list/get/search/excerpt均通过，郑希包仍为VERIFIED。Git archive现接受0字节tracked blob，但空文件不能成为策展资源。

### 2026-09-21 功能回归与选择复盘修复（不涉及数据治理）

按用户要求仅用隔离合成样本测试功能，未访问或修改正式数据根、TDX采集/存储及既有数据治理改动。选择复盘已修正正常日历延长误报冲突、同实例缓存不刷新、坏记录静默漏入汇总、并发覆盖和CLI失败状态；旧v1记录保留原字节/哈希/创建时间，真实窗口修订仍拒绝。CLI支持完整明细分页，AI与桌面显示归档不完整；桌面引用补齐selection_id，发布兼容Lexar卷不支持硬链接的情况。

本轮按模块完成22个功能测试模块、131项不重复测试（含新增11项）；最终受影响后端/入口26项回归通过且源码指纹稳定。另有Lexar卷隔离目录的文件系统与Qt离屏验证，不冒充真实桌面、付费模型或策略盈利验收。完整过程、超时与复核限制见[开发总档案](changelog.md)，原始输出位于 `artifacts/functional-validation-20260921/`。

代码能力盘点为441个因子/组件注册项、16个可解析的组合研究模板；专门封装的选择/持有/退出模拟流程主要为旧Qimo规则代理，另有通用成交研究与AR事件研究。它们不构成441或16套成品策略；不读取正式Playbook数据库，不能据此推断用户创建的策略数量。区别见[使用指南](../guide/user-guide.md)。

### 2026-09-21 顺序推进：代码同步、跨层验收与策略目录

用户授权按顺序执行，数据治理保持独立。本次完成度账本如下；状态以实际测试/远端回执更新，不把开发测试当作正式自主研究或策略收益证明。

| 顺序 | 交付与验收条件 | 当前状态 |
|---|---|---|
| F1 代码同步 | 普通推送三笔积压提交，随后独立核对远端SHA | 本轮开工已独立读回origin/main=8f7e23b并与本地一致；前轮同步已收尾 |
| F2a 入口联通 | 本地聊天/MCP能读取同源Playbook及选择复盘；不增加执行权限 | MCP补只读Playbook层，进程内/stdio重开与权限检查通过；相关8模块52项通过 |
| F2b 跨层生命周期 | 研究→有限授权→真实队列→冻结归档→记忆→重开；选择→模拟→复盘 | 3个新增跨层场景通过；新进程重开验证同一证据/预算/订单身份；最终14模块88项回归通过，源码稳定 |
| F3a 策略能力可发现 | 区分因子、组合模板、专门策略；助手读取精确模板并使用原提案预检 | 新增list_research_templates/get_research_template，16个模板逐项与源定义及preview核对通过 |
| F3b 策略统一封装 | 固定信号版本、证券范围、资金/持有/退出/费用/成交与授权合同，复用既有引擎 | v1已实现版本化配置包、纯编译、CLI/助手/MCP/桌面导入、原审批执行与复算身份绑定；仅research_only和现有目标权重生命周期，不代定参数或增加成品策略 |
| F4 策略编辑与对照 | 可视化编辑同源包、逐字段版本差异、同口径归档结果对照；保留人工审批与数据边界 | 已接入原提案面板及宿主CLI；13模块81项功能回归通过，比较仅描述性且先核对归档口径；不创建第二份策略库或自动执行 |
| F5 策略归档发现与助手查询 | 从名称/版本目录选择结果，目录与深验分层，助手/MCP只读接入 | 结果页有界查找、左右选择和归档核验已接入，失败记录/分页/错误显式；14模块91项通过，新增20项；无新执行权限，不涉及数据治理 |
| F6 历史策略改版 | 核验历史来源、载入当前可编辑副本、明确新版本并重走原审批 | 已实现，历史与当前编译指纹分开；取消/失败不覆盖、关闭拒绝迟到回调；8模块71项通过（新增13项），无新执行权限 |
| F7 改版来源持久化 | 直接父引用随包/提案/归档保存，重开核验，重新批准，不继承权限 | 已完成，复核发现的未批准冻结残留展示/复用问题已修；14模块110项通过（新增17项），来源与自身数值复算分层，无新增数据库、执行权限或数据治理动作 |
| F8 通用归档读取 | 原生Chat/MCP发现和核验已存retro日线、查询TDX；与专用入口同源 | 已实现，旧规格别名/权限保留、坏记录与输出超限明确；12模块140项通过（新增21项），不改正式数据或自动接通回测Provider |
| F9 有限归档研究输入 | 宿主预检/确认生成独立raw日线包，原Provider/审批冻结/执行/复算与只读助手接入 | 已完成；13模块136项通过（新增27项），另在Lexar隔离验证2场景；仅1–10证券/371自然日，不补缺日停牌、不自动切根授权，未生成正式治理数据包 |
| 治理交付代码复核 | 审核3b0dd21的raw-tail增量与数据侧G0–G6结果，修复读源/范围/证据链 | 已修复；14模块144项通过（新增20项），正式指针一次只读7行smoke通过且未改动。数据报告须纠正事件时间、竞价单位/版本和G3冻结状态；G5/G6仍待执行，F10进展见下行。详见开发史与artifacts/governance-review-20260921。 |
| F10 归档输入工作台 | 预检/显式生成/已有包深验与宿主选择，沿用原审批及ChatRuntime | 已完成；12模块111项通过（新增34项），含真实F9面板与宿主队列联动；旧任务/有效授权与迟到回调保护保留，不改正式数据或自动运行研究。 |
| F11 输入与研究衔接 | 所选F9包进入空提案，明确草稿与实际输入只读核对；不自动修改或执行 | 已完成；13模块142项通过（新增25项），真实MCP/stdio及原审批链验证；汇总范围/价格/背景阻塞，草稿/显示对象/根变化和关闭清旧状态。检查不代替冻结、严格资格或统计充分性。 |
| F12 提案任务跟踪 | 固定提案关联持久任务/冻结清单/结果头部，显式有界窗口刷新与结果回查 | 已完成；12模块137项通过（新增34项），真实队列/MCP/新进程与Qt离屏回归；无新执行权限，不把running日志当在线或头部核对当完整结果验收。 |
| F13 结果入口一致性 | 旧任务按钮复用固定提案回查，点击重核；策略目录/对照的关闭、查询和空间切换保护 | 已修复并通过12模块136项回归（新增19项）；预检/输入核对清旧链接，缺失/错配不打开，旧窗口不接受迟到结果。不重建任务或扩授权，测试及复核范围以开发史为准。 |
| F14 完整流程与结果详情验收 | 输入→策略审批/冻结/执行→改版/对照→结果详情/导出/恢复/新进程复算 | 已完成；最终23模块218项通过，新增28项，Lexar隔离3场景复跑通过。结果窗口固定原空间与伴随文件身份，导出ZIP实字节校验、恢复占位不覆盖；不扩研究/交易权限，不参与正式数据治理，不代替真实模型或全项目验收。 |
| F15 治理资料只读消费第一批 | R1/R2真实覆盖与日期轴；R13/R14/R15候选来源/冲突/qfq风险，沿原Chat/MCP与只读CLI | 已完成；最终14模块162项通过，新增35项，代码及测试指纹稳定。固定来源、有界查询、缺税基/未知/修订冲突阻断，不将治理分类当最终真值；不改正式数据、不重建factor，R3及R12/R13正式重建仍待合同。 |
| F16 显式日历与日期集合核对 | R7/R8有界消费：来源/内容身份、日历缺日阻断、上市区间、完整日期差集、停牌与未知分列 | 已完成；21模块224项回归通过，新增29项，源码及测试指纹稳定。Chat/MCP/CLI同源，MCP多余字段过滤问题已修复；不扩F9/批准/PIT，不改正式数据，不代表正式历史覆盖已验收。 |
| F17 指纹绑定的配股候选读取 | 宿主双文件/双SHA显式绑定；分类、公式和汇总复核；Chat/MCP/CLI只读分页 | 已完成；14模块148项全部通过，新增40项；完整分页读取已交付878条候选且源字节未变，代码/测试指纹稳定。Chat/MCP/CLI显式宿主绑定，不改治理资料/正式数据、不默认选源、不接重建发布，首轮Reviewer和QM50原规格权限不扩展。 |
| F18 有界候选重建预览 | 固定F17版本与完整列内范围、逐事件来源草案、所有阻断与单事件算术，纯只读 | 已完成；16模块188项通过，新增40项，源码/测试指纹稳定；独立只读复核未发现范围内阻断缺陷。已验收候选包3事件×2假设预览验证，源文件不变；不发布因子、不读取正式行情、不隐式解开待决或未知，不改F9、授权和原复算。 |
| F19 未决配股补充证据消费 | S1 JSONL+summary 双SHA绑定父F17 conflict全集；Chat/MCP/CLI查询并可选附加到F18 | 已完成；修后联合回归19模块209项全部通过，真实S1只读smoke为19/19 UNRESOLVED。独立复核发现的diagnostics布尔计数与非规范日期两项已修并补回归；S1不解除EVENT_REQUIRES_SEPARATE_ADJUDICATION，不产生计算/裁决/重建/发布权限，未绑定S1时F18保持原结构。 |
| F20 F9停牌状态输入合同v2 | 保留tradestatus=0完整session，OHLC不填充；研究排除、成交禁止、估值mark与冻结身份显式分层 | 已完成；固定源码分3组139项回归全部通过，新增9项。v1仍默认拒绝停牌；v2合同进入包/preview/dataset/冻结snapshot身份。停牌目标不fill、持仓沿最后真实close估值；无历史mark才用vendor_previous_close且绝非成交价。Reviewer发现的停牌除权重复估值风险已改为开/收盘生效均fail-closed，v2冻结snapshot绑定冻结SHA/source snapshot/contract；不改正式数据、采集或权限。 |

本轮证据保存于 `artifacts/functional-lifecycle-20260921/`；独立只读复核未发现本轮4个产品文件的权限、版本或接线缺陷（不等于全仓正确性证明）。跨层测试以脚本化Provider驱动正式接口，只证明工程链路，不冒充真实模型自主研究。只触及功能代码、测试和文档，不改TDX、正式数据、部署或用户授权。

### 2026-09-21 F3b 策略配置包 v1

新增 `niuniu-strategy-package-v1`：固定信号/模板版本、证券日期范围、显式资金/费用/仓位和既有目标权重生命周期。纯编译返回规范包及完整spec，包内声明与实际运行配置逐项核对，审批继续冻结实际输入；执行父归档及复算保留包身份。模型只读预览，CLI仅预览/导出/待批准提案，桌面在原人工提案面板导入，不自动执行。

v1明确只支持research_only，不以新包声明Strict PIT或官方规则已覆盖；严格请求直接拒绝，旧严格研究入口不变。持有/退出仍是每根完结bar重算目标、目标减少退出、期末不强平；不支持独立止损止盈、固定持有期、自动Paper部署或真实交易。package_hash表示配置身份，compiled_spec_hash同时固定信号源码/模板解析，CLI提交前两者都须与预览一致。修改后需重新编译，不能只改单侧展开spec。

主控已修复边界测试发现的自签范围与实际执行不一致、价格口径错配；独立只读复核提出的资格声明与预览源码绑定问题已分别通过收紧v1口径和完整编译指纹处理。测试和提交记录见[开发总档案](changelog.md)，原始证据在 `artifacts/strategy-package-20260921/`。未访问正式数据根、变更TDX治理或部署服务。

新增5个测试模块25项；复核修复后受影响后端62项、其余入口43项及Qt离屏2项共18模块107项通过，0失败/错误/跳过，源码指纹一致；16模板配置全部通过编译与原提案预检。完整测试统计保留于本轮artifacts；不以工程通过替代真实模型、历史数据资格、策略盈利或实盘验收。

### 2026-09-21 F4 策略工作台与归档对照

原人工提案面板新增策略工作台，支持精确信号、股票日期、资金费用和仓位可视化编辑，保留未展示的高级字段；不改动往返保持配置身份，修改后重新预览，另存不覆盖，填入草稿重验完整编译指纹并清除旧批准选择。版本比较逐字段展示，同标识同版本不同内容明确警告。

结果比较只读两个指定UUID的完成execution归档及其信号子实验，读取前后复核文件树；按实际全部冻结输入（含背景）、Universe、价格、时间、运行环境、初始资金/费用/规则确认可比，配置变化单列。不同口径只显示原因，delta为空；损坏、缺失零费用字段、重复估值时间或包声明错配拒绝，不回读数据根、不运行复算、不声明Alpha或赢家。UI和CLI复用同一服务，未新增模型权限。功能测试全部使用临时合成数据；过程证据在 `artifacts/strategy-workspace-20260921/`，最终统计与复核限制见开发史。

### 2026-09-21 F5 策略归档发现与读取

策略工作台结果页可按名称/版本/标识查找带包execution归档，使用真实next_offset分页，失败条目可见但不能用作有效结果；目录仅metadata_only，详情和比较继续复用完整归档核验。ChatRuntime/MCP提供list_strategy_runs/get_strategy_run/compare_strategy_runs，CLI同源list-runs/get-run；根符号链接在解析前拒绝，错误/阻塞不裁剪。最终后端74+离屏17共91项通过，源码稳定；不增加数据库、批准/执行或实盘权限。详见使用指南和开发史，原始证据在artifacts/strategy-catalog-20260921。此共享status中的数据治理增补保持原样，不混入F5功能提交。

### 2026-09-21 TDX主数据湖归并与大文件归档

三份本机worker的已发布页已按原结果包/MERGED回执链归并到Mac主数据湖，采集继续保持STOP。主库共有319,961条publication、62,915,859行；`catalog/tdx_page_archive.sqlite3`也有319,961页，按source_id逐条核对family/rows为0缺失、0错配、0额外。`storage-compaction.json`于16:06记录`COMPLETE`，13类`tdx_*`标准查询视图逐类行数与publication求和一致。供应商原始响应和派生Parquet原字节保留在大文件归档库；并不表示全市场全历史完整或Strict PIT合格。

worker页副本的清理与主库可查询是两件事。2026-09-21 19:29前，三份worker均完成完整预检和逐页清理，macbook/homepc/601各自的`retired-to-canonical.json`均为`RETIRED`，进程正常退出；合计涉及241,671条已发布页副本和20,365个checkpoint记录。worker-1/2的虚拟checkpoint witness、各worker队列库及错误记录仍保留。清理后重新只读核对主库：319,961 publication = 319,961 archive页，0缺失/错配；13类`tdx_*`查询共62,915,859行，逐类与publication记录一致。Lexar卷占用从清理前约838 GiB降至约353 GiB，约释放485 GiB；这一容量变化还包含同期其他磁盘活动，不能全部当作精确的本任务回收量。主库已可查询，但仍不表示全市场全历史完整或Strict PIT合格。

## 4. 真正剩余的工作按门槛处理

| 门槛 | 后续应做 | 本轮没有做 |
|---|---|---|
| 数据证据 | 按 session/证券补足可验证的官方 Universe、状态、行业、市值和规则 | 重新审计数据根、补签 receipt、回填过期窗口 |
| 前瞻运行 | 核对采集部署、授权、失败恢复，积累 Paper/预测/复盘样本 | 新建定时任务、扩大预算、调用付费模型 |
| AR 运行授权 | 根据真实计划范围确认 D-4；核查调度和 qimo 控制兼容 | 代用户签发或续期授权 |
| 真实券商 | P13-B1 先要有明确可用的实时只读通道；其后再评审风险和订单门 | 真实登录、下单、撤单、资金操作 |
| 工程质量 | 保留 Qt 原生崩溃证据，继续真实客户端验收；按需求处理未覆盖算法 | 无关代码重构或为通过测试放宽规则 |

## 5. 文档与测试状态

本轮完成代码/目录全量清点与 Python AST 扫描，并针对入口、数据路由、运行时 Markdown 读取、行情链和 Frame 合同核查内容。此范围不是对全部业务逻辑的逐行安全或数值正确性证明。

旧架构中的 `1006 tests` 等数字归入历史，不再冒充当前基线。此次实际测试结果、Qt 崩溃限制、文件校验与 Git 状态统一记录在 [文档整理验收](../development/documentation-cleanup.md)，避免多个说明重复抄写测试数量后再次失同步。

## 6. 更新规则

新增能力只在本页更新状态摘要；稳定架构写总体架构，操作步骤写指南，事件记录追加开发史。复杂验收按主题存档并标日期，不再新增另一份“总计划/最新进度”与本页竞争。
