# 2026-09-17 QM50-SCLA v0.2 原始规格监督验收

[文档导航](../../README.md) · [当前状态](../../project/status.md)

## 结论与适用范围

本轮按用户下载目录中最新的两份原始文件验收牛牛助手，而不是由外部开发者代做其他因子研究。牛牛通过正式 `quantlab.agent.chat_cli` 读取完整规格、调用审计和固定测试、自己选取原始日线样本，并读取真实结果。开发者独立核对原文、工具轨迹与计算产物。

**已经核验：60项来源定义一致；34项合成组件逻辑检查通过；P07原始字段125行诊断完成并独立逐值匹配。没有完成：60项完整历史时点因子计算、完整候选池排名、全策略回测、撮合账户与盈利验证。**

当前完整模型仍为 BLOCKED。59项尚无此规格的完整精确原始数据适配器；P07有原始数值诊断，但历史可用时点和候选全集没有通过验证，严格状态仍为 MISSING_SOURCE。组件函数存在不等于对应字段的市场数据适配已实现。

## 1. 锁定的两份来源

| 原文件 | 下载目录修改时间（北京时间） | 字节数 |
|---|---|---:|
| qimo50_factors_v0.2.md | 2026-09-17 19:49:26 | 50,254 |
| qimo50_factor_dictionary_v0.2.json | 2026-09-17 19:49:33 | 56,631 |

原文件均位于 `/Users/anyuzhe/Downloads/`。这两份比9月14日旧HTML新，未混用旧HTML或既有qimo-source-rules-v2。

```text
MD SHA256:
dc61f1d3edeeaf29b5a7d5b87d04bf4b189a4708eb89a0deff25e8ba270712ad
JSON SHA256:
762557fc8d1dfb7f16b297ee5e85732d181bb39b0e70fcf7f24b0f36d97c5edc
规格ID:
1c5f4718b709afdd122cd6498b14bd84503b1ad10b7837ee9bec30a34d768ee9
```

两份文件分别与原件哈希匹配，**不是两份不同文件的哈希互相相等**。60项ID、名称、字段名、定义、最早可用时点、拟定方向、用途、数据需求与说明逐项比对；每项保留MD行号、JSON定位和定义哈希。全局公式/参数由完整原文件哈希绑定，没有重新整理后替换原件。

P9、S5、A6、LP5、LR4、C4、T6、N6、E6、D5、H4，合计60项。文件自身定位为工程研究规格，未认证本人公式或Alpha；导入不创建正式StrategySource、Playbook或交易授权。

## 2. 牛牛实际执行轨迹

使用现有Codex CLI配置下的真实 GPT-5.5，medium。没有注入测试专用模型工具，也没有外部脚本代交研究。新增功能属于正式产品API，并由原生CLI调用。

| 阶段 | 实际动作 |
|---|---|
| 完整读取 | 14次工具调用：global、全部11组（N重复核对一次）、60项适配审计 |
| 测试与读取结果 | 7次工具调用：重新审计、运行组件测试、两次本地目录查询、运行P07诊断、分别读取两份测试记录 |
| 被禁止的替代 | 未调用通用研究提交、动量回测、DSL替代、旧qimo策略或自动交易工具 |
| 数据发送 | 模型读取规格和受限工具摘要；未把原始整表行情发送到模型 |
| 生命周期 | 仅这两轮明确授权的后台调用；结束后没有持续研究服务或新增通用Grant |

第一轮牛牛明确指出完整适配器和资料资格不足，没有谎报可以跑完整模型。针对原生文档读取/版本绑定和固定逻辑测试的缺口，本轮补代码后由牛牛继续调用测试。它自行从真实目录选取5只主板股票和2026-08-01至2026-09-04，不是开发者预先代选样本。

## 3. 合成逻辑检查：34/34

测试ID：`7e1dae2d-52a0-462e-a4f2-febd6c2fabde`。状态 PASSED，34通过、0失败。

这批纯组件检查覆盖原文18种必须检查的逻辑情景，并增加数值边界案例。检查包括竞价结果不倒灌、未满3分钟不生成盘中A、未来LR不能参与此前买点、A05删失、Top1不可成交不替补第二名、候选不完整不重排、首板属于1进2、昨日末次封板不回填当日早盘、D02只用已结束事件区间、T+1、无买盘保留头寸、部分成交、历史题材时间、零成交不识别一字板、确定性、B独立于C/Q、冷启动与缺失分离。

另外检查P07排除昨日、分母异常不加epsilon、分位并列中点、LP02收缩、A06平盘中性、LR04负响应封顶、缺项不重分配、E不进入Q、Q的0.93分母、无对手中性、锁定目标不更换、1tick触发、300/301秒回封边界、退出59/60秒边界。

**这些是合成输入上的组件函数测试，不是60个原始数据字段已实现，也不是完整真实盘口/账户引擎通过验收。测试中合成分数的Q运算不代表产生过真实行情Q。**

## 4. P07真实原始字段诊断

测试ID：`021fee3b-f07b-47fe-ac7a-1b0a1eb1de9e`。状态 DIAGNOSTIC_COMPLETED_NOT_STRICT。

牛牛选取：`sh.600000`、`sh.600004`、`sh.600006`、`sz.002163`、`sz.002164`。日期2026-08-01至2026-09-04，读取原始未复权日线中的实际成交额和已保存交易日历，合计125个证券/决策日。

原文公式：

```text
P07[D] = amount[D-1] / median(amount of 20 valid trading days strictly before D-1)
```

只在此前21个已知交易日都有实际正成交量、正成交额时输出原始比值。没有跳过不明缺日或零成交日，没有填零、填均值或凭极小数补分母。缺历史/缺量分别保留不可用状态，不用删行换成看似完整的结果。

本轮125行原始算术均可计算。监督复核用另一条按交易日对齐的计算路径：`amount.shift(1) / amount.shift(2).rolling_median(20)`，逐行核对125个输出和窗口边界，误差小于1e-12。该复核只检查同一已运行任务，不产生新假设、新候选或新收益研究。

示例：sh.600000的2026-08-03决策，分子为7月31日，分母为7月3日至7月30日的20个交易日；没有把7月31日混入分母。

所有行的 `historical_decision_value`、`score` 保持null，`asof_qualified=false`，严格状态MISSING_SOURCE。未验证当时可得的逐日证券状态、有效涨停价和完整昨日涨停候选池，因此这5只股票不被冒充为QM50候选池。

没有生成真实Q、排名、收益、持仓、交易次数、净值或订单。

## 5. 尚未通过的完整模型条件

- 竞价真实成交、同条件250交易日历史、历史流通股本和训练样本可用时点没有完成资格核验。
- 盘前冻结的历史题材、完整候选全集与逐证券逐日有效涨跌停价、状态、交易规则没有完成本版本接线与验证。
- 标准目录中的1分钟/逐笔数据没有通过本轮检查；5分钟和日线不能替代秒级封板、1分钟速度、排队位置。未断言所有其他目录都不存在相关资料。
- 大部分原始字段仍缺精确适配器；题材/盘口/事件/账户输入不能只凭已有近似值映射同名字段。
- 原文件 `entry_notional_budget` 与 `cost_budget_bps` 均为空。行情过期阈值、回归窗口细节等尚需明确参数合同，不由开发者自行补数后仍称原版。

对于这些项目，不能输出“没有信号”等价于“已测完”，更不能说策略没有Alpha。只记录UNSUPPORTED、MISSING_SOURCE或未核验，并保留原始原因。

## 6. 产品改动与隔离边界

新增不可变规格导入、原文分组读取、60项一致性矩阵、精确哈希适配器绑定、固定组件与P07诊断工具。后台 `--research-spec` 绑定后屏蔽通用研究提交，`--allow-spec-tests` 仅许可本会话最多3次固定检查，不创建无限授权或修改产品原有Grant。

原来的 `qimo-source-rules-v2`、qimo_paper、Playbook包、因子库和统计引擎未改动。原下载文件与使用过的行情、日历字节都核对未变化。业务测试没有可见GUI、行情下载、正式策略注册、订单或资金操作；联网仅用于已授权模型服务及Git同步。

## 7. 工程回归和证据

新增14项自动化测试；12个相关模块共107项通过，0失败、0跳过。包括原qimo_paper兼容回归；其通过仅代表旧功能未破坏，不能当作QM50完整模型已验证。

本地证据目录：`artifacts/qimo50-supervised-20260917-231023/`，不提交原始数据到Git。

| 文件 | 含义 |
|---|---|
| requested-sources.json | 原件路径、修改时间、大小与SHA256 |
| workspace/_research_specs/ | 原始MD/JSON副本、精确版本和60项定位 |
| 01-read.log / 01-niuniu-source-review.md | 牛牛原文读取与首轮阻塞判断 |
| 02-execute.log / 02-niuniu-test-report.md | 牛牛实际调用测试及自己的结论 |
| native-events.json | 原生会话调用和返回证据，非模拟 |
| fidelity-matrix.json / fidelity-matrix.csv | 60项逐项定义绑定和状态，不漏项 |
| workspace/_research_spec_tests/ | 两项不可变测试请求、结果与125行原始值 |
| independent-p07-check.json | 监督者对同一125行的独立数值核对 |
| regression.log | 107项自动化回归 |
| delivery.json | 最终Git提交、推送及远端SHA核对 |

本报告和代码在本次独立提交中维护；推送是否成功以最终工具回执和delivery.json为准，不把本地完成写成已部署。


## 第二阶段：基础规则依赖与原生工具接线（2026-09-18）

本阶段由牛牛先提出优先级：不是继续扩大P07或换别的因子，而是先补证券状态、实际涨跌停价和候选资格的共同基础层。它通过原生CLI读取global/P/N/E和实际能力，指出绑定规格入口只能检查行情目录，不能深入核对本地已有的官方回执。外部开发者据此补产品功能，然后仍由牛牛选取范围、调用测试并解释结果。

### A. 新增的可用功能

`inspect_qm50_base_rules_coverage` 在原始规格绑定下，只读调用原有PIT Evidence、PIT Universe、SecurityStatus v2、Official MarketRules和回顾性规则参考的深度验证器。检查一到十只明确沪深证券、一到三十一自然日；从已归档交易日历求D/D-1/D-2，不按自然日减一、减二。全局回执完整性与具体证券/日期覆盖分开记录，不创建官方回执，不改变任何源数据。

固定测试新增 `BASE_RULES_COVERAGE`（保存实际覆盖矩阵）和 `BASE_RULES_GUARDS`（合成基础资格/P01组件合同）。P01组件检查未知连板前缀、实际中断和时点，不以缺少上一天资料补出0板或1板。它没有接到真实候选生产端，完整原始证据未满足时仍保持UNSUPPORTED/MISSING_SOURCE。

### B. 牛牛实际选择与执行

本阶段三轮真实模型调用分别为8、12、5次，共25次。第一轮确定依赖；第二轮自行查询真实目录，选择 `sh.600000`、`sz.000001`、`sz.000002`、`sz.000006`，以及2026-08-25至2026-09-04；第三轮从新进程新会话复核修复后的入口和既有结果，没有更换样本或重新做收益研究。

| 记录 | 结果 |
|---|---|
| BASE_RULES_COVERAGE | `5659e81b-6171-4420-8d8f-30bd42ed9da6`，AUDIT_COMPLETED_INPUTS_BLOCKED |
| 实际检查范围 | 4只股票 × 9个决策交易日 = 36行，包含对应D/D-1/D-2依赖 |
| 早期组件记录 | `e14e7a05-306c-4e27-90f9-55ac0021b82d`，22项通过，保留原结果 |
| 最终组件记录 | `9e2f8867-f95c-463a-938d-1ee1ba0e5f61`，增加3项显式制度/品种检查后25项通过、0失败 |
| 原生全局覆盖查询 | 新进程实际调用get_strict_pit_coverage成功，不只是检查schema |

22项和25项是同一组检查的前后版本，不合计成47项。36行真实依赖检查的candidate_for_D、prev_limitup_height全部为null，不把未知当作不入选，更不当作“策略没有信号”。没有生成真实Q、排名、收益或成交。

### C. 实际资料结果

以下是本轮调用原有深度校验器取得的结果，不是引用旧文档的覆盖数字：

| 证据层 | 已有有效记录 | 是否满足本请求 |
|---|---:|---|
| 稀疏PIT证券状态statement | 14条，涉及7只深市证券 | 不能代表完整逐日状态，不在本请求范围内 |
| 完整PIT Universe v1 | 0份 | D/D-1/D-2股票池证据缺失 |
| 完整逐日SecurityStatus v2 | 0份 | 无法证明全Universe的当日状态 |
| Official MarketRules | 1份回执、7条规则 | 全局存在，但不覆盖选定4股/日期 |
| 回顾性官方规则参考 | 1份、7条 | 严格资格记录仍为0，不转成可用规则 |

回执“无效数量为0”与“数据齐全”不是一回事：Universe和完整状态的回执总数本来就是0。现有14条稀疏事实也不向前或向后扩散成其它日期的正常状态。回顾性参考不会进入正式规则选择器。

### D. 新发现的字段合同缺口

现有MarketRules代码包含上下限、ST/停牌、费用和有效时间，但**没有**reference_price、tick_size、board_asof、instrument_type_asof、delisting_arrangement、new_listing_no_limit_flag、normal_price_limit_regime。这是现有数据结构与QM50输入合同的差异，不是模型看到“官方规则”名称就可以推断这些字段已齐全。

基础组件因此要求证券种类、主板、正常制度、无涨跌幅新股标志等显式给出。涨停识别用原始价格和显式上下限/tick，不通过涨幅四舍五入或固定1.1推断。真实数据还缺历史bar available_at及足以结束连板前缀的事实；只检查D/D-1/D-2不能证明任意长的完整P01高度。

本轮将这些缺口连同来源入口和接受条件输出为结构化data_requests，未自行下载、回填、补签历史receipt或填入未知字段。

### E. 接线问题如何被发现并修复

第一次把get_strict_pit_coverage加入规格白名单后，使用模拟inner的测试通过，但真实ChatRuntime继承链并没有注册该工具。牛牛在第二轮明确报告“实际工具不存在”，没有谎报已经调用；它改用新基础覆盖检查中的global_archives取得证据。

随后将该工具作为显式原生API接线，增加真实headless_chat_runtime回归，而非只测试模拟inner；第三轮新进程实际调用成功。原始失败阶段和回答保留。第二轮另有一次误填规格ID的audit调用被拒绝，牛牛自行改正；没有绕过版本绑定或扩大权限。

### F. 验收与保留边界

新增17项自动化测试，14个相关模块共116项通过、0失败、0跳过。独立监督核对36行矩阵、每行原因和汇总、来源SHA、整批受检回执/原文指纹、日历与四份行情字节，以及三份测试结果读回一致。原始两份QM50说明SHA不变、60项定义绑定不变。旧qimo规则、Playbook、因子和原PIT验证器未改。

仍未完成：真实基础字段数据接入、完整候选池与P01输出、60项完整历史因子、全策略回测、收益或Alpha证明。新进展是让牛牛能准确检查依赖并报告可追溯缺口，而不是通过降低证据要求使回测强行运行。

本阶段证据：`artifacts/qm50-dependencies-20260917-235738/`。

| 文件 | 用途 |
|---|---|
| 01-niuniu-plan.md | 牛牛自己提出的优先依赖、字段和工具需求 |
| 02-niuniu-test.md | 牛牛真实选样本、运行覆盖/组件检查及发现接线问题 |
| 03-niuniu-recheck.md | 修复后新进程实际调用和25项检查复核 |
| native-events.json | 三轮原生工具调用、参数及返回证据 |
| base-rules-coverage-matrix.csv | 完整36行依赖矩阵，未知值不填零 |
| verified-global-coverage.json | 实际全局证据审计结果，不是数据资格证书 |
| workspace/_research_spec_tests/ | 三份不可变测试记录与coverage.parquet |
| final-regression.log | 116项最终回归 |
| verification.json / delivery.json | 独立监督及Git提交、推送回执 |

完成后依用户约定提交并普通推送origin/main；具体提交和远端SHA以delivery.json及工具结果为准，推送不代表部署。


## 第三阶段：接入已有packed原始日线，不再只检查旧MQC目录（2026-09-18）

### 1. 找到的真实缺口

用户要求继续实际推进，不只增加合成测试。第一轮牛牛仍只能读取旧MQC目录和官方回执，因而再次建议P07。开发者检查现有存储实现后发现：日常工作空间 `_market_data/retro_daily/` 已有一份完整字段的回溯日线capture，包含preclose/turn/isST/tradestatus等原始列，但当前隔离规格入口没有宿主明确绑定的跨工作空间归档读取能力。

这次补的是**已经有数据但助手读不到**的接线，不是新下载，也没有把供应商历史字段升级为官方规则。

实际capture：`0b774158-3a3b-561f-9c18-23fd895db4aa`，计划包含5,455只沪深证券，2019-01-02至2026-09-15，共1,870个交易日，已合并为pack文件。这个数字是capture计划覆盖，不宣称本轮重新深验全部5,455只证券，更不是历史完整可交易市场认证。

### 2. 新增正式功能

新增只读来源工作空间参数 `--spec-source-workspace`；用户/宿主显式指定来源，模型只能使用实际capture_id，不接收任意文件路径。新增来源发现、证券分页、原始数据检查、日期对齐输入物化与冻结字节复算工具。默认不传参数时仍读取当前工作空间，不猜测兄弟目录，也不扩大普通聊天权限。

归档读取同时支持原逐证券目录和pack切片；对选定证券核对typed Parquet及原始压缩JSON的SHA、原始行重新规范化后的全表一致性。冻结本次读取的原始字节、plan、calendar和各证券manifest；读取归档不创建或修改源文件，不调用下载函数。

原始14列全部接入：date/code/open/high/low/close/preclose/volume/amount/adjustflag/turn/tradestatus/pctChg/isST。按真实交易日历映射D-1，保留逐行供应商状态和缺失。P07继续原有保守算术检查，其他未实现因子不伪造输出。

重要语义：preclose保留为vendor_previous_close，不偷偷映射成QM50有效reference_price；turn保留为vendor_turnover_percent，不当成交额、显式历史流通股本或已实现P06。isST/tradestatus是供应商按日字段，不是官方逐日状态回执，不从最新名字/状态回填。

### 3. 牛牛实际选择并完成接入

通过原生 `quantlab.agent.chat_cli`，牛牛自行发现capture，使用has_st/has_suspension元数据筛选**功能边界样本**，检查两个时间区间后自行选定：

- sh.600053、sh.600078、sh.600006、sh.600008。
- 2024-01-01至2025-01-05，实际244个决策交易日。
- 仅1次真实ARCHIVED_DAILY_INPUTS任务：`403b2beb-b04d-4b98-8af0-f6b479bfdbb7`。

这些股票不是QM50合格候选池；故意包含历史ST/停牌用于验证数据处理，不能把筛选样本用于收益结论。

| 实际结果 | 数量/状态 |
|---|---:|
| 对齐输入 | 4股 × 244日 = 976行 |
| D-1供应商交易且非ST标记 | 863行 |
| D-1供应商ST标记 | 112行 |
| D-1明确停牌标记 | 1行 |
| P07原始比值可计算 | 955行 |
| P07因窗口缺失/停牌等保留不可用 | 21行，不填0、不静默删除 |
| P01/P06/候选/Q/有效价格上下限 | 均未生成，保持null |

D-1对齐示例：2024-01-02使用2023-12-29，而不是上一自然日。没有将当天最终收盘或当日最终状态当成当天盘前已知。

### 4. 牛牛发现真实复算问题，修复后再由它验证

第一次牛牛调用replay后得到 `equal=false`，并明确报告未通过，而非把工具外层ok=true当作数值一致。

监督检查发现原始数值、字段、空值和逐日映射一致，差异仅在股票行顺序：原请求顺序为600053/600078/600006/600008；JSON序列化按key排序manifest，旧复算错误地按该字典顺序输出600006/600008/600053/600078。

修复为严格使用冻结请求中的symbols列表，而不是manifest字典顺序。增加非排序股票顺序、逗号分隔输入两个回归场景；没有重写原任务、换样本、改公式或修改数值。原始false结果保留在第二轮日志。

随后新进程新会话、没有绑定外部来源工作空间，牛牛读取同一test_id并仅从冻结字节复算，返回976行 `equal=true`。证明同一完整输入表可独立复现，不要求临时连着原始pack源。

### 5. 独立监督与功能边界

监督者对976行的D-1日期及12个原始输入字段逐项核对，共11,712个字段值与重新读取的原始归档一致；955个P07数值和21个空值状态分别核对。候选、P01、P06、Q和reference_price均为空，没有无依据的资格升级。

原下载的两份规格SHA不变，60项定义未改；实际使用的四只证券原始/typed字节与冻结副本一致，交易日历与plan字节一致。旧PIT验证器、旧qimo策略、Playbook包与因子计算核心未改。

仍缺真实有效涨跌停价/tick、合格历史参考价、历史流通股本、历史板块/退市整理/无涨跌幅特殊制度、题材和竞价等资料及对应实现。**本轮推进是回顾性真实输入接入与可复算，不是完整P01候选池、60字段策略或Alpha验证。**原严格覆盖检查继续阻断，不因另一个来源有更多字段就放宽。

### 6. 工程验收与记录

新增16项回归，15个相关模块共151项通过、0失败、0跳过。初始单元测试另捕获macOS临时目录别名导致的路径规范化问题，以及测试期待值自身的分母窗口偏移错误，均在真实模型接入前修正并保留初始日志；真实模型捕获的排序问题如上单独保留。

本阶段目录：`artifacts/qm50-data-integration-20260918-010358/`。

| 文件 | 内容 |
|---|---|
| 01-niuniu-source-plan.md | 原生入口最初只查旧目录的实际结论 |
| 02-niuniu-data-integration.md | 牛牛发现capture、选样本、接入原字段并报告首次replay失败 |
| 03-niuniu-replay-check.md | 新进程对同一冻结结果复算成功 |
| native-events.json | 三轮真实工具调用与返回值 |
| materialized-daily-inputs.csv | 完整976行输入，不是交易信号表 |
| supervisor-check.json | 11,712字段、955数值、21空值的独立核对 |
| replay-mismatch-diagnosis.json | 排序缺陷证据，原始输出不重写 |
| workspace/_research_spec_tests/ | 唯一输入任务、原始字节、976行Parquet与校验结果 |
| final-regression.log | 151项最终回归 |
| verification.json / delivery.json | 监督结论与实际Git推送回执 |

本轮不操作可见客户端，不下载新行情，不建立持续研究/采集授权，不创建真实订单，不注册正式因子。完成后普通推送origin/main并核对远端SHA；原始源数据不提交Git。
